# Stock 项目 - 当前状态快照
**最后更新**：2026-07-06（US-short offline Batch5→Batch4 weekend E2E bridge + official-context assembly settled；其余 settled 状态见 §0。**实时工作流 gate 以 `docs/SESSION_LOG.md` 顶部为唯一来源**）

**文档定位**：跨会话接续的短 snapshot。完整路由见 `docs/README.md`；过程、review verdict 和 rejected alternatives 见 `docs/SESSION_LOG.md` 顶部 1-3 条；历史 phase 细节见 `docs/handoff/README.md`。

---

## 0. Latest Delta
- 2026-07-06 **US-short offline Batch5→Batch4 weekend E2E bridge is settled in repo state**: `runners/us_short_batch5_to_batch4_weekend_e2e.py` consumes a gitignored local Batch5 source packet, writes official context components, overlays an explicit batch4 template + provider-health + private account/root, and runs the supported Batch4 weekend runner in-process to private `weekly_report.md`/`action_table.csv` under a provably-private root. This proves only the offline local-source-packet→private-report bridge; `--run-mode live` fails closed on offline data (no authoritative/live claim), and CLI/subprocess/no-residue hardening, the live/real-provider data path, DataHub/production/provider-selection/ship-gate/forward evidence, and A-share remain separate work.
- 2026-07-06 **US-short Batch5 resolved-source official-context assembly is settled in repo state**: `runners/us_short_batch5_data_context.py::assemble_official_context_components_from_resolved_pass2_sources` emits Batch4-aligned `data_context`, `per_ticker_analysis`, and source-ref-bound `run_provenance` from already resolved local Batch5 source artifacts; `runners/us_short_batch5_data_context_source_packet.py` can optionally write those gitignored context components. This proves only offline/local resolved-source assembly; the live/real-provider-resolved per_ticker/provenance data path beyond reviewed local sources, subprocess E2E, DataHub/production/provider-selection/ship-gate/forward evidence, and A-share remain separate work.
- 2026-07-06 **US-short `run_fetch` consumed completed bankruptcy 8-K screen output into Pass1 status provenance and now records provider-health/fallback run state**: `runners/us_short_universe_fetch.py` accepts repeated gitignored `--bankruptcy-screen-path` inputs and merges the first candidate shard, shard02, and round03 resume screens before provider fetch. The user-authorized canonical run (`decision_date=20260706`, `price_basis_date=20260702`, `generated_at=2026-07-06T00:00:00+08:00`) wrote tracked summary `docs/us_short_universe_fetch_summary_20260706.json` with `status_source_outcome` all ok, `bankruptcy_8k_source=injected_bankruptcy_screen`, `bankruptcy_8k_input_symbol_count=2630`, `bankruptcy_8k_screen_file_count=3`, `eligible=2404`, `ineligible=5237`, `fmp_rescued=10`, and `provider_health.overall_run_state=usable_with_fallback` because FMP market-cap fallback is opportunistic, not provider-readiness evidence; raw/candidate artifacts remain gitignored. This proves only Pass1 status-provenance consumption plus `run_fetch` summary run-state wiring; broader provider stability, DataHub/production/provider-selection/ship-gate/forward evidence, and A-share remain separate work.
- 2026-07-06 **US-short bankruptcy 8-K candidate-universe scan/fetch is completed in repo state**: `runners/us_short_batch5_bankruptcy_8k_candidate_resume_scan.py` executed the user-authorized resumable round03 window after round02. Round03 covers shard indices `72..105` (`830` additional eligible symbols), records `831/831` SEC public endpoint calls as HTTP 200, writes raw under gitignored `provider_samples/us_short_batch5_bankruptcy_8k_candidate_resume_scan_20260705/round03/raw/`, writes source packet + bankruptcy screen under gitignored `state/us_short/`, and writes tracked no-secret summaries `docs/us_short_batch5_bankruptcy_8k_candidate_resume_scan_round03_summary_20260705.json` + `docs/us_short_batch5_bankruptcy_8k_candidate_resume_scan_round03_consumer_summary_20260705.json`. Result: manifest `completed_shard_count=106`, `completed_symbol_count=2630`, `remaining_shards_after_round=0`, `round_runs_recorded=3`; resume aggregate screen `screen_symbol_count=2580`, `bankruptcy_8k_positive_count=0`, `screened_no_filing_count=2580`, plus the first two precompleted 25-symbol candidate shards also had positive=0. This proves only candidate-universe SEC submissions scan/fetch + local bankruptcy screen production; `run_fetch` consumption is tracked above, while broader provider health/fallback, DataHub/production/provider-selection/ship-gate/forward evidence, and A-share remain separate work.
- 2026-07-05 **US-short bankruptcy 8-K candidate-universe resume round02 is settled in repo state**: `runners/us_short_batch5_bankruptcy_8k_candidate_resume_scan.py` executed the user-authorized resumable round02 window after round01. Round02 covers shard indices `37..71` (`875` additional eligible symbols), records `876/876` SEC public endpoint calls as HTTP 200, writes raw under gitignored `provider_samples/us_short_batch5_bankruptcy_8k_candidate_resume_scan_20260705/round02/raw/`, writes source packet + bankruptcy screen under gitignored `state/us_short/`, and writes tracked no-secret summaries `docs/us_short_batch5_bankruptcy_8k_candidate_resume_scan_round02_summary_20260705.json` + `docs/us_short_batch5_bankruptcy_8k_candidate_resume_scan_round02_consumer_summary_20260705.json`. Result: cumulative resume screen `screen_symbol_count=1750`, `bankruptcy_8k_positive_count=0`, `screened_no_filing_count=1750`; round03 is tracked separately above. This proves only resume round02; `run_fetch` consumption is tracked above, while broader provider health/fallback, DataHub/production/provider-selection/ship-gate/forward evidence, and A-share remain separate work.
- 2026-07-05 **US-short bankruptcy 8-K candidate-universe resume round01 is settled in repo state**: `runners/us_short_batch5_bankruptcy_8k_candidate_resume_scan.py` executed the user-authorized resumable round01 window after excluding `NVDA`/`MSFT`/`AAPL` and after the first two precompleted shards. Round01 covers shard indices `2..36` (`875` eligible symbols), records `876/876` SEC public endpoint calls as HTTP 200, writes raw under gitignored `provider_samples/us_short_batch5_bankruptcy_8k_candidate_resume_scan_20260705/round01/raw/`, writes source packet + bankruptcy screen under gitignored `state/us_short/`, and writes tracked no-secret summaries `docs/us_short_batch5_bankruptcy_8k_candidate_resume_scan_round01_summary_20260705.json` + `docs/us_short_batch5_bankruptcy_8k_candidate_resume_scan_round01_consumer_summary_20260705.json`. Result: `bankruptcy_8k_positive_count=0`, `screened_no_filing_count=875`. This proves only resume round01; round02 and round03 are tracked separately above, and `run_fetch` consumption is tracked above while broader provider health/fallback, DataHub/production/provider-selection/ship-gate/forward evidence, and A-share remain separate work.
- 2026-07-05 **US-short bankruptcy 8-K candidate-shard SEC scans are settled in repo state**: `runners/us_short_batch5_bankruptcy_8k_candidate_scan.py` executed the authorized first two 25-symbol Pass1-eligible shards after excluding `NVDA`/`MSFT`/`AAPL` (`52/52` SEC public calls across the two shard runs, all HTTP 200), wrote raw under gitignored `provider_samples/us_short_batch5_bankruptcy_8k_candidate_scan_20260705/`, wrote source packets + bankruptcy screens under gitignored `state/us_short/`, and wrote tracked no-secret summaries for shard01 (`docs/us_short_batch5_bankruptcy_8k_candidate_scan_summary_20260705.json` + `docs/us_short_batch5_bankruptcy_8k_candidate_scan_consumer_summary_20260705.json`) and shard02 (`docs/us_short_batch5_bankruptcy_8k_candidate_scan_shard02_summary_20260705.json` + `docs/us_short_batch5_bankruptcy_8k_candidate_scan_shard02_consumer_summary_20260705.json`). This proves only those first two candidate-shard runs; the completed resume scan and later `run_fetch` consumption are tracked separately above, while broader provider health/fallback, DataHub/production/provider-selection/ship-gate/forward evidence, and A-share remain separate work.
- 2026-07-05 **US-short bankruptcy 8-K bounded candidate SEC source-packet producer is settled in repo state**: `runners/us_short_batch5_bankruptcy_8k_source_packet_producer.py` executed the authorized NVDA/MSFT/AAPL candidate sample (`4/4` SEC public calls, all HTTP 200), wrote raw under gitignored `provider_samples/us_short_batch5_bankruptcy_8k_source_packet_producer_20260705/`, wrote the source packet + bankruptcy screen under gitignored `state/us_short/`, and wrote tracked no-secret summaries `docs/us_short_batch5_bankruptcy_8k_source_packet_producer_summary_20260705.json` + `docs/us_short_batch5_bankruptcy_8k_source_packet_from_producer_summary_20260705.json`. This proves only bounded selected-candidate SEC submissions -> local source-packet -> bankruptcy-screen wiring; full candidate-universe bankruptcy 8-K scan/fetch and later `run_fetch` consumption are tracked above, while broader provider health/fallback, DataHub/production/provider-selection/ship-gate/forward evidence, and A-share remain separate work.
- 2026-07-05 **US-short bankruptcy 8-K local source-packet screen layer is settled in repo state**: `runners/us_short_batch5_bankruptcy_8k_source_packet.py` consumes a gitignored local SEC submissions source packet, validates scope/path/schema, writes a gitignored `bankruptcy_screen` payload for existing status-source consumers, and writes tracked counts-only summary `docs/us_short_batch5_bankruptcy_8k_source_packet_summary_20260705.json`. This proves only the local source-packet to screen leg with fixture data; full candidate-universe provider scan/fetch and later `run_fetch` consumption are tracked above, while broader provider health/fallback, DataHub/production/provider-selection/ship-gate/forward evidence, and A-share remain separate work.
- 2026-07-05 **US-short full theme source-packet producer is settled in repo state**: `runners/us_short_batch5_theme_source_packet.py` executed the authorized reviewed-local-plan packet for AAPL/MSFT/JPM with FMP stable profile rows plus Massive adjusted daily prices (`14/18` calls, all HTTP 200), wrote raw under gitignored `provider_samples/us_short_batch5_theme_source_packet_20260705/`, wrote the full source packet/projection under gitignored `state/us_short/`, and wrote tracked no-secret summaries `docs/us_short_batch5_theme_source_packet_summary_20260705.json` + `docs/us_short_batch5_theme_source_summary_20260705.json`. This proves only bounded reviewed-plan full theme source wiring; automated/broader peer-theme discovery, provider health/fallback, live/real-provider-resolved `per_ticker_analysis`/`provenance` data path beyond reviewed local sources, subprocess E2E, DataHub/production/provider-selection/ship-gate/forward evidence, and corporate-action reconciliation remain separate work; no A-share crossing.
- 2026-07-05 **US-short bounded live Pass2 source-packet wiring is settled in repo state**: `runners/us_short_batch5_live_source_packet.py` already connects an authorized <=3-symbol SEC/FMP/Massive live source packet to `runners/us_short_batch5_data_context_source_packet.py`, and `docs/us_short_batch5_live_source_packet_summary_20260704.json` records the AAPL/MSFT/JPM run with source packet + local `data_context` written under gitignored `state/us_short/`. This proves only the bounded source-packet→local `data_context` leg. Broader provider-health/fallback, automated/broader peer-theme discovery, live/real-provider-resolved `per_ticker_analysis`/`provenance` data path beyond reviewed local sources, subprocess E2E, DataHub/production/provider-selection/ship-gate/forward evidence, and corporate-action reconciliation remain separate `SR-PROVIDER-001` work; no A-share crossing.
- 2026-07-03 **US-short run_fetch 现可消费 injected SEC submissions bankruptcy screen**: `run_fetch` 新增 `bankruptcy_submissions_by_ticker` 参数,透传已审的 `build_live_status_records` / `build_bankruptcy_screen_from_sec_submissions` seam,把 provider-fed SEC 8-K Item 1.03 筛选喂进 Pass1 `status_provenance` + candidate artifact;default/live 路径仍零 8-K 抓取、bankruptcy 保持 positive-detection-only unscreened;candidate artifact/raw 落 gitignored,tracked summary 只记 scan_performed/source/input_count/disclosure 元数据(无 raw accession)。真 SEC per-issuer fetch 仍 SR-PROVIDER-001 gated。no A-share crossing.
- 2026-07-03 **US-short live `run_fetch` full candidate-universe path landed after Massive pacing repair**: root cause was free-tier per-minute grouped-daily rate limiting, not a universally invalid key. `fetch_massive_window` now paces Massive grouped-daily requests and retries HTTP 429; the canonical 20260706 summary is the current `docs/us_short_universe_fetch_summary_20260706.json` tracked above. Broader provider-health/fallback evidence, DataHub, production evidence, ship-gate evidence, and forward evidence remain separate work; no A-share crossing.
- 2026-07-03 **US-short status-source `run_fetch` wiring built**: `runners/us_short_universe_fetch.py::run_fetch` now builds Pass1 `status_records` from the SEC active-listing reference plus Nasdaq current halt feed before `apply_pass1`; halt-feed failure leaves `halted` unknown and Pass1 rejects conservatively; all-critical status-source failure no-emits before candidate artifact. Default `run_fetch` bankruptcy remains zero-call/unscreened unless an explicit bankruptcy screen or submissions packet is injected. Remaining batch5 work is broader provider-health/fallback evidence, DataHub, production evidence, ship-gate evidence, and forward evidence; no A-share crossing.
- 2026-07-02 **US-short 进入 deferred-content 执行相位 + sequencing 封条 unseal (user A)**: offline engineering track（batch1-4 + batch5 offline halves + Cut 6 seams + batch5→batch4 data_context source assembly）complete；后续 = 逐刀执行此前 deferred 的 live/provider 内容。**SR-PROVIDER-001 不变**：每次真实 provider fetch 仍是 user-run per-execution gated action（非盲发网络调用），本轮未解除。register 的 ① sequencing 封条（MOMENTUM-PIT / PASS1-STATUS-HEALTH R1-remainder / BATCH4-PIPELINE payload-digest）已标 superseded=cleared to build。**工作约定**：执行某个 previously-deferred register 项的那一刀，须在同刀 reconcile 该项 deferral/status 注（lift/supersede + 重述剩余 SR-PROVIDER-001 gate），避免 built 项残留 stale「deferred/do-not-build」注（即 `R-USSHORT-BATCH5-DATA-CONTEXT-ASSEMBLY-...` 抓到的那类漂移）。no A-share crossing。
- 2026-07-02 **US-short batch5→batch4 data_context source assembly + score-block seam closed**: `assemble_data_context` now consumes a validated Massive+SEC candidate artifact + explicit Pass2 signals + Cut 6-d score composition and emits Batch4 runtime `data_context`; it strips provider row clocks/lineage from universe rows and validates Pass2-clean score targets. Cut 6-a/b/c/d cover momentum projection, full-pool theme/industry projection, catalyst projection, and the score composer that emits same-source `selection_inputs` + analysis `score_blocks` / `risk_downgrade`. The supported batch4 offline context packet builder/runner path also exists and is guarded for private output, holdings 1:1 reconciliation, schema validation, and subprocess `PYTHONPATH`. Remaining batch5 work is broader provider-health/fallback / DataHub / production evidence / ship-gate evidence, all still separately gated; no A-share crossing.
- 2026-06-30 **US-short round-2 theme-track producers closed**: new pure/offline `engine/us_short_industry_heat.py` and `engine/us_short_provisional_theme_heat.py` produce GICS industry heat and provisional cross-sector theme heat inputs for the 35% theme block. Non-positive closes fail closed; provisional volume confirmation uses the full member denominator, so thin volume coverage cannot self-confirm. Provider/GICS/discovery data layer, live/provider/DataHub/production remain separately gated; no A-share crossing.
- 2026-06-30 **US-short 轮2 slice 2 catalyst block 已建并审查通过**：新 `engine/us_short_catalyst.py` + `presets/us_short_catalyst_governance_20260630.json` + schema/tests，把 §4.2 catalyst 25% 分量做成纯/离线规则映射块（realized earnings surprise / strict-int analyst revisions / 8-K / semantic advisory → 0-100；future/unverified excluded；missing neutral fallback）。`analyst_revision_net` 只接受严格整数计数，fractional / float / bool / string 不再推动分数。验证：focused 63 OK，全离线 `*us_short*` 2728 OK，doc-route guards 52 OK。真实信号抽取、provider-fed `data_context`、live Pass2/status-source、DataHub/production 仍需单独授权与审查，不交叉 A 股。
- 2026-06-28 **US-short 批4官方输出与离线入口补强**：Pass1/Pass2/Top15 未入选记录由 selection 保留并单源派生 §11.4 剔除摘要；batch4 ship-gate 经现有引擎固定为 paper-only，action_table 会覆盖调用方伪造的 full-size 字段，周报删除自由文本 ship-gate/exclusion 注入；新增 `runners/us_short_weekend_batch4.py`，从本地 context packet 加载 calendar/governance/account，派生 bucket/cash、对账账户持仓、支持幂等 lifecycle bootstrap 与无持久输出 dry-run。live/provider/真实数据仍由 batch5 独立硬门控制。
- 2026-06-27 **US-short 轮2 slice 1 momentum block 已建**：新 `engine/us_short_momentum.py`——`compute_momentum_features`(日线序列→1m/3m/5d/10d 收益 + rel SPY/QQQ 1m + 量比 vol_surge) + `momentum_block`(每子特征横截面分位→等权合成→全池分位 0-100，供 `core_score` 40% momentum 块)；缺史票不伪中性、入 `insufficient_history` 让 core_score 走 §4.2 缺分量中性规则。纯/离线（消费已抓日线，数据层接 Massive 在后续 slice）。26 tests + 全量 2387 OK + boundary 8 OK。**轮2 后续**：赛道块(35%，需 sector 分类)、多日数据层接 Massive、Pass2 编排。不交叉 A 股。
- 2026-06-27 **US-short batch5 轮1 universe fetch 完成（用户授权 SR-PROVIDER-001 数据获取）**：最终数据源 = **Massive grouped-daily + SEC（全免费、纯 HTTP、无券商）**。`runners/us_short_universe_fetch.py`：SEC `company_tickers_exchange`(ticker+CIK+交易所) → Massive `/v2/aggs/grouped` **1 次调用**取全美股收盘价+成交量(ADV=量×收) → SEC frames 流通股(市值=股×收) → FMP profile 兜底(SEC 缺股的多股权名,bounded 240) → Pass1 `cheap_eligible`。实跑(date=2026-06-25,12324 只)→ **eligible=2323 含全部巨头**；raw+candidate gitignored,tracked summary `docs/us_short_universe_fetch_summary_20260626.json` 无价格/无密钥。23 runner tests + 2361 全离线 OK。**探索已废**：FMP Basic(日限不足)、IBKR(Fundamental 被封+延迟量按时机不稳)均淘汰,对应 runner 已删;因不用券商,"不接券商"红线保持原版未改(register BROKER-BOUNDARY=withdrawn)。`SR-PROVIDER-001` DataHub/production/SEC parser/ship-gate 仍 open。不交叉 A 股。
- 2026-06-26 **US-short 批5 离线工程全闭合 + register 修复**：`R-USSHORT-BATCH5-SEC-SOURCE-ARTIFACT-TRACEBACK-SCHEMA-DRIFT` 已 resolved——Codex PASS 2026-06-25 确认修复在 `781b5679`（schema 现 `maxItems:6` + 6 条 source ref 全 const-pin `artifact_id`/`path`/`role`）；10/10 SEC traceback schema tests + 92 batch5 target suites OK 2026-06-26 核实。**批5 离线 8 刀全 Codex PASS + 提交（按逆提交序）**：① SEC EDGAR parser/field-family binding（`781b5679`）② license/storage/retention decision（`73259af6`）③ incident-log writer（`9b973448`）④ incident-log storage contract（`34a9f8dc`）⑤ fallback/incident stability binding（`44dd96a4`）⑥ post-probe disposition（`460c937b`）⑦ provider live probe runner（`1484bbe4`）⑧ provider live readiness packet（`49ca24e5`）。全部 no-provider/no-network/no-raw：离线 schema-first 契约 + offline private writer。**provider/live 真实边界（FMP 全市场 / SEC parser / DataHub / production / ship-gate）仍 GATED（SR-PROVIDER-001，需单独授权）**。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-25 **US-short 批4 slice 4d-ii-o 端到端编排器已审查通过并提交**：新 `engine/us_short_weekend_orchestrator.py::run_weekend_pipeline(now_et, pipeline_context, *, run_mode)`（batch4 round-2 改签：sessions 由 validated calendar 派生、live 门留批5）串 4d-i 选股→4d-ii-a..j 决策链→K machine_record→L lifecycle（先于 render）→m2 weekly_report→N private write；`decision_date` 单一穿线到 K/L/N，盘中 out-of-window no-emit 不产 artifact。selection→analysis seam 现在按 admitted∪holdings canonical union 对账：admitted-only=`top15_candidate`，holding-only=`holding_pass2_only|holding_account_only`，admitted∩holdings=`holding_in_top15`，同时继续拒 payload ticker mismatch、错 row_source、缺/陈旧分析、admitted 内重复和 holdings 内重复。验证：orchestrator 20 OK，adjacent weekend suites 388 OK，全离线 `*us_short*` 2233 OK，doc/route/README guards 52 OK，nonempty O probes 可写 admitted-only 与 `holding_in_top15` 私密 artifact。**批4周末 pipeline 离线工程闭环完成；批5 provider/live/DataHub/Skill/production evidence 仍 gated（SR-PROVIDER-001，需单独授权）**。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-25 **US-short 批4 slice 4d-ii-n 幂等私密写 + §18.0 P0 守卫 已提交**（`fa7656b6`，Claude 起草→Codex `审查 FAIL`（官方输出门 3 同类 gap + handoff-commit drift）→修→Codex 硬化 price-clock parser→`审查 PASS`；2 Required resolved）：新 `engine/us_short_weekend_private_write.py`：`write_run_private(*, decision_date, machine_record, weekly_report_md, runs_private_root, weekly_private_root)` 把 run 官方 artifact 写到 gitignored 私密目录——机器层（flatten 后 §10 record）→ `runs_private/<dd>/machine_record.json`；§11.1 weekly_private（**只** weekly_report.md + action_table.csv）→ `weekly_private/<dd>/`。**§18.0 P0 守卫**：三目标路径**写前全预检** `reject_nonprivate_output_path`（相对/非 gitignored in-repo/git-check 失败拒、mixed root 失败**零落盘原子**）；action_table.csv 复用 renderer `write_action_table`（同 guard + render flatten 后填充）。**幂等**（dd 键、重跑覆盖非追加）。**多 artifact 对账同一 run**：dd==machine_record.as_of + 解析渲染 **price-clock 行**（恰 1 行、`decision_date=\d{8}`==canonical、非 substring，旁注/重复日期不蒙混）。**§11.1 only-two-files** fail-closed（weekly_private/<dd> 预存非官方文件拒、2 官方名覆盖）。private_write 16 + 全离线 `*us_short*` **2213 OK** 零回归。**Batch4 4d-ii-o is now closed; weekend pipeline offline engineering closure completed**。批5 = provider/live（GATED、SR-PROVIDER-001）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-25 **US-short 批4 slice 4d-ii-m2 §11.2 周报 report_data 装配 + render 已提交**（`ab64b152`，Claude 起草→Codex 2 轮 `审查 FAIL`（source-reconciliation 跨周/不一致 + 残留 module docstring 双源契约）→修→`审查 PASS`；1 Required resolved）：新 `engine/us_short_weekend_report.py`：`build_weekly_report(machine_record, lifecycle_result, *, report_context)` 装配 §11.2 report_data（13 节 + 诚实横幅①③④⑤[②可选] + lifecycle 数量对账）再 `render_weekly_report`。从 K 机器记录（m1 `flatten_machine_record`）派生行级节（§5 操作表/§6 持仓/§7 Top15/§8 观察池）+ banner①（observe 聚合 `aggregate_observe_split`）+ §3 regime；接 L readiness（§12 + section_1/12 数量对账 + banner③）；wire formatter（§9 exclusion / §11.5 coverage / banner⑤ hot_excluded）；注入 closed-world `report_context`（price_clock④ + 编辑节 §2/§4/§10/§11 + theme_opportunity③ + macro②）。**多 artifact source-reconciliation fail-closed**（首个同时持多份官方 artifact 的边界须对账同一 run）：① canonical decision_date 对账（price_clock/lifecycle/readiness.as_of/exclusion_data.as_of 全 == machine `as_of`，§2.1）；② readiness 单源 `_assert_readiness`（due_count/ids/upgrade⊆due 全契约、非仅 shape）；③ hot_excluded **单源**（banner⑤ 与 §9 同源 `build_exclusion_summary`、删 report_context.hot_excluded_summary 双源、§18.1#19 横幅==明细）。report 19 + 全离线 `*us_short*` **2197 OK** 零回归。**批4 4d-ii capstone 余 N（幂等私密写 + §18.0 P0 守卫）→ O（端到端编排器）= 周末 pipeline 工程闭环**。批5 = provider/live（GATED、SR-PROVIDER-001）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-25 **US-short 批4 slice 4d-ii-m1 §11.3 action_table 投影 已提交**（`559d3125`，Claude 起草→Codex `修复 + review PASS`；1 Required resolved）：**实测发现 M 须拆 m1/m2**——K 机器记录携带嵌套 rich layer（`price.action_fields`/`sizing` dict），但 §11.3 CSV 渲染器按扁平列名 `r.get(col)` 取值→列**全空**（直接探针证）。新 `engine/us_short_weekend_action_table.py`：`flatten_machine_record`/`build_action_table` 把 rich layer 投影到扁平 §11.3 列（`price.action_fields` 键=列名直升 + `sizing.desired_model_shares`→`model_position_size_shares`），无 v1 源列留空（不编造）→ CSV 填充。升列**前** `_validate_price_projection` 按行类 value-validate §6 价格契约：任何 present 价格/RR 列须有限数（拒非数值/NaN）；executable 行须 `_BUILD_REQUIRED`（candidate=support_atr_engine + 合法 sub_mode + entry/order/价位/RR/status 全非空）/ `_HOLDING_REQUIRED`（holding_exit_engine + sub_mode None + stop + post_round_status）——空/partial 不得渲染成「有动作+股数却空 entry/stop/RR」；非 executable 观察行保留 §6 honest partial（仅数值检查）。投影后 re-validate §10-clean。单源复用 `PRICE_ENGINES`/`PRICE_SUB_MODES`/`row_source_to_context` + required ⊆ engine 列三角校验；入 action/reason guard。action_table 26 + 全离线 `*us_short*` 2178 OK 零回归。**批4 4d-ii capstone 余 m2（§11.2 内容无关周报 report_data 装配 + render）→ N（幂等私密写 + §18.0 守卫）→ O（端到端编排器）**。批5 = provider/live（GATED、SR-PROVIDER-001）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-25 **US-short 批4 slice 4d-ii-k machine_record 装配 + 4d-ii-l lifecycle eval 阶段 已提交**（`6893d506`，Codex `review PASS`；K 经两轮 consumer-validation 整类修复[malformed-evidence → position-trace]后 PASS、L 一轮无 blocker；2 Required resolved）：**K** `engine/us_short_weekend_machine_record.py::assemble_machine_record(ranked_result, *, as_of)` 把 4d-ii-j 排名结果装配成 §10 机器记录（每行 field_records 全核心字段落点[硬 veto→final_action / price·selection→调信心→price·action_rank / sizing·regime·theme_probe→降仓→position_size / clean·display→仅标签 shadow] + decision_trace + rich 机器层 carry + **per-row market_risk_regime traceback**）再跑批3 `validate_machine_record`、not-clean→raise。发 field_records 前 **fail-closed value-validate 全部 carried evidence**（veto/price/score/sizing/theme_probe **forced-min 契约**/action_group·rank/selection_rank/regime 值），单源复用 `_validate_evidence_row`（4d-ii-a 契约）+ `action_rank.action_group`（§9 组）+ cost_floor probe 契约常量 + `REGIMES`（regime 词表）+ `MIN_EXECUTABLE_SHARES`，无第三份词表。**L** `engine/us_short_weekend_lifecycle_stage.py::run_lifecycle_eval_stage(*, decision_date, register_path, readiness_out_path)` 接线 `load_lifecycle_register`(§18.0 私密 guard + §2.1/§18.1#20 stale fail-closed)→`build_lifecycle_readiness`(脱敏 due scan)→`lifecycle_banner`(GBK-safe)，**先于 render**；返回 `{decision_date, readiness, banner}` 供周报渲染 + §11.2 数量对账；upgrade 仅 surface、需用户决定不自动切生产；register 写=accumulate 另一路。K 入 action/reason guard。K 53 + L 14 + guard 15 + 全离线 `*us_short*` **2151 OK** 零回归。**批4 4d-ii capstone 余 M（§11.2 内容无关周报 render + action_table CSV）→ N（幂等私密写 + §18.0 守卫）→ O（端到端编排器）**。批5 = provider/live（GATED、SR-PROVIDER-001）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-25 **US-short 批4 slice 4d-ii-h 周末 pipeline theme_probe 成本地板 cost-floor + consumer-validation 整类根治 已提交**（多轮：Claude 起草→Codex 连点同类 consumer-validation gap[§9 action/reason → probe-identity → forced-min sizing]→Claude 整类根治 + Codex 补 forced-min sizing→`审查 PASS`，4 Required resolved）：新 `engine/us_short_weekend_cost_floor.py`：`apply_probe_cost_floor(basket_result, *, cost_inputs)` 后置 stage 对 4d-ii-g 已促回 theme_probe（行带 `theme_probe`）跑 `apply_cost_floor`——shares=forced-min、entry=`valid_entry_high`、tp1=`take_profit_reduce_price`(盈一)、注入佣金/滑点/点差；profit-盈一 ≤ 往返成本×安全倍 → 观察(`cost_inefficient_min_size`)、build_count 重算。**consumer-validation 全套 fail-closed**：§9 单源 `action_reason_error`（入 decision、全 weekend stage[sizing/basket/cost-floor]改用 + producer emit 自检 + 补 4d-ii-c 漏的同 gap）、canonical ticker（非规范/dup 拒、emit 大写）、probe trace 验值（risk_tag 单源/entry_mode triangulation）、forced-min sizing 契约（desired==MIN_EXECUTABLE_SHARES + reason + pre_probe trace）、cost_inputs canonical 恰覆盖。**位置无关 guard** `tests/test_us_short_weekend_action_reason_contract.py`（结构单源 + planted-failure + 各 stage 行为）防该类**跨 stage 复发**。cost 值=离线 fixture（live 成本模型=批5）。cost_floor 34 + action/reason guard 12 + 全离线 `*us_short*` 2055 OK 零回归。**批4 4d-ii capstone 余 cash 分配 + §9 action_rank**。批5 = provider/live（GATED）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-25 **US-short 批4 slice 4d-ii-g 周末 pipeline 建仓门 theme_probe 强赛道额外席 已提交**（Codex 起草→`审查 FAIL`(promoted probe 保留风险仓却 tag min_size)→修(强制 min)→re-`FAIL`(test docstring 残留 keep-size)→修→re-`审查 PASS`，1 Required resolved）：扩 `resolve_build_capacity` 第③段——base 排名+capacity 后，capacity-deferred 的强赛道建仓按 §8 #27 席位 `theme_probe_seats(regime, theme_opportunity_state)`（防御≤1/进攻+极强≤2/极度防御0）促回 建仓，资格复用 `theme_probe_decision`（lifecycle+high_confidence+coverage 非 restricted、hard-zero），仍受 同主题 cap（probe 计入主题、席位 budget break + 同主题 continue 双拦）；促回行 tag `theme_probe_min_size` + 防御档 entry_mode_constraint + **强制 `desired_model_shares=MIN_EXECUTABLE_SHARES`**（§8 强制最小可执行仓 / governance `forced_min_executable_size`、原风险仓存 `pre_probe_risk_shares` trace——非「保留风险仓」）。basket_context 扩 account `theme_opportunity_state` + per_ticker[ct].theme_probe（closed-world、值由 theme_probe_decision fail-close）。**cost-floor 闸（line 232→cost_inefficient_min_size）= 4d-ii-h**（需佣金/滑点/点差执行成本契约、`apply_cost_floor` 未接入任何 stage）；cash+§9 rank 后续。basket 28→**56** + 全离线 `*us_short*` 2009 OK 零回归。批5 = provider/live（GATED）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-25 **US-short 批4 slice 4d-ii-f 周末 pipeline 建仓门 portfolio_guard/symbol_cooldown 新建阻断 已提交**（Codex 起草→`审查 PASS` 一轮、无 Required）：扩 `engine/us_short_weekend_basket.py::resolve_build_capacity`——账户 portfolio_guard `block_new_entry`（**泛化消费** effects 表、非硬编码 cooldown、malformed effect→fail-closed block）或 symbol_cooldown∈{entering_cooldown,in_cooldown} 命中的 provisional 建仓 → 观察(`risk_cooldown`)、**排名前移除**（不占 §8 周/同主题 slot、腾位给下排、selection_rank None、precedence over capacity_or_budget_deferred）。basket_context 闭世界扩 `{per_ticker, portfolio_guard_status}`、per_ticker[ct] 扩 `{theme, symbol_cooldown_status}`、全 fail-closed（坏/缺 guard·cooldown raise）；symbol_cooldown 状态集本地定义 + triangulation 钉 engine（不改已提交 engine）。**「建仓门收尾」拆为 4d-ii-f（本刀阻断、排名前正交）+ 4d-ii-g（theme_probe 额外席=min-size 重定仓+cost floor）**；caution/recovery graded 效果（减仓/减每周/只高置信）+ cash + §9 rank 留后续刀；pre-existing `4d-ii-d` slice 编号 drift（兄弟模块 docstring，非本刀 ripple）noticed-未修。basket 28→41 + 全离线 `*us_short*` 1994 OK 零回归。批5 = provider/live（GATED）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-24 **US-short 批4 slice 4d-ii-e 周末 pipeline 建仓数量闸（§8 周建仓上限+同主题 cap）+ SESSION_LOG 孤儿守卫 已提交**（Codex 起草→`审查 FAIL`(consumer-validation + SESSION_LOG 孤儿)→修→re-`FAIL`(残留 observe-reason)→修→re-`审查 PASS`，2 Required resolved）：新 `engine/us_short_weekend_basket.py`：`resolve_build_capacity(sized_result, *, basket_context)` 按 core_score 排 selection_rank、应用 §8 **BASE** 周建仓上限(进攻3/震荡2/防御1/极度防御0)+ 同主题≤2——超额建仓→观察(`capacity_or_budget_deferred`、上刀加的值首个发射者)、无 promotion。**两遍 VALUE-validation fail-closed**：final_action∈冻结词表、observe_reason_type⟺final_action 一致(观察须冻结 reason / 非观察须 None)、ticker 经 `canonical_us_ticker` 规范唯一+emit UPPERCASE、建仓须真 sized build(sizing.status==sized+desired≥1)、per_ticker canonical 恰覆盖建仓集、同主题 cap 用 strip 后 theme（白空格不绕）。**theme_probe 额外席 + guard/cooldown 阻断 + cash + §9 rank = 4d-ii-f**。**doc-governance guard 硬化**：每 `## ` SESSION_LOG 块 ≤1 Verdict/Action（header-type 无关，根治"prepend 吃标题致 Codex PASS bullets 孤儿"类——位置无关 guard、机器拦不靠记）。basket 28 + doc-governance 27 + 全离线 `*us_short*` 1981 OK 零回归。批5 = provider/live（GATED）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-24 **US-short 批4 observe_reason_type 加 `capacity_or_budget_deferred`（schema-first 契约 + emitter 闭合）已提交**（Codex 起草→`审查 FAIL`(emitter+active "7" docs 未闭)→修→re-`FAIL`(残留 cost-only prose)→修→re-`审查 PASS`，1 Required resolved；用户经 AskUserQuestion 批准加值）：§9 observe_reason_type 冻结词表加第 8 值 `capacity_or_budget_deferred`（过闸可执行但被每周建仓上限/同主题/极度防御仓位上限挤出名额→转观察、非系统不看好；§11.2 honesty 横幅分桶用）。8 面同序加（action_table preset+schema、action_governance preset+schema、2 schema test、design §9）。emitter 闭合：`us_short_weekend_sizing` below-min 降级分流——`position_cap==0`(极度防御仓位上限)→capacity_or_budget_deferred、其余(真 under-min/折扣/cap)→cost_inefficient_min_size（两 reason 不合并、2 测证）。active "7"→dynamic 全扫 6 面 + §11.2 ① delegate 冻结词表。新 guard `test_us_short_observe_reason_capacity_contract`：enum 含新值 + 无 active 面 claim "7" + sizing reason split co-described（防 stale-count 与 stale-reason 两类回归）；observe_split 归 capacity 为 genuine deferral（非 sizing artifact）。schema+sizing+observe_split+guard 全绿 + 全离线 `*us_short*` 1953 OK 零回归。**该新值的 emitter = 4d-ii-e build-count（周建仓上限/同主题挤出时发）**。批5 = provider/live（GATED）。纯 docs/preset/schema/engine/test、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-24 **US-short 批4 slice 4d-ii-c 周末 pipeline 仓位（§8 削减叠法）已提交**（Codex 起草→`审查 FAIL`(sizing 输入契约 fail-open)→修→re-`FAIL`(残留 ticker-emit)→修→re-`审查 PASS`，1 Required resolved）：新 `engine/us_short_weekend_sizing.py`：`size_rows(decision_result, *, sizing_context)` 对每行 provisional 建仓跑 §8 削减叠法（复用 `us_short_position_sizing`、无新数学）：base=risk_based(short_bucket, valid_entry_high, stop_clear_price) × regime cap × harshest 注入折扣 × min(单票 cap=⌊bucket×10%÷entry⌋, 注入流动性 cap)；<最小可执行→降观察(极度防御 cap0=capacity_or_budget_deferred、其余 cost_inefficient_min_size)。非建仓 carry-through sizing=None。**两遍 value-validation fail-closed**：final_action∈冻结词表(`FINAL_ACTIONS` 从 decision 单源)、建仓须 candidate+executable+合法价位、ticker 经 `canonical_us_ticker` **canonical-unique 且每行输出归一 UPPERCASE**(注入 `aapl`/` AAPL `→`AAPL`、防一股两行/两拼写绕单票 cap)、`per_ticker` 恰覆盖建仓集(缺/陈旧 raise)。SINGLE_TICKER_CAP_FRAC=0.10 本模块 §13#4 prior。v1 不自动决 加仓/减仓/清仓-止盈。sizing 25 + 全离线 `*us_short*` 1950 OK 零回归。**批4 capstone 余 3 刀**：4d-ii-d 基组（周建仓上限/同主题/theme_probe 成本地板/现金分配 + §9 action_rank）｜4d-ii-e §10 machine_record 装配+no-dangling｜4d-ii-f lifecycle-eval(先于 render)+内容无关周报 render+action_table CSV。批5 = provider/live（GATED、SR-PROVIDER-001）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-24 **US-short 批4 slice 4d-ii-b 周末 pipeline 动作决策 已提交**（Codex 起草→`审查 FAIL`(证据 value-validation fail-open)→修→re-`审查 PASS`，1 Required resolved）：新 `engine/us_short_weekend_decision.py`：`decide_actions(analysis_result)` 消费 4d-ii-a 证据，按 §6 优先级链定每行 §9 `final_action`+`observe_reason_type`——holding→position_hard_veto 清仓-事件 / 非 executable 观察(price_not_executable) / breached 清仓-止损 / else 持有；candidate→entry_hard_veto 否决/避开 / event_data_gap restricted 观察(data_restricted) / 非 executable 观察(price_not_executable) / else **建仓(provisional)**。建仓暂定（下游 §8 sizing / 周建仓上限·同主题 / 现金 在 4d-ii-c/d 可降观察）。**证据 value-validation fail-closed**：`_validate_evidence_row` 校 veto_tier∈§5 词表(`VETO_TIERS`∪`NONE` 从 hard_veto 单源)、context 一致+不兼容硬 tier 拒、executable 真 bool、executable holding 的 breached 真 bool、event_gap status∈冻结 4 值——坏证据绝不变 clean 动作。final_action/observe_reason 仅从冻结 `design_locked_enums` 出 + 与 action_group 兼容。**v1 不自动决** 加仓(§6 dual-engine)、减仓/清仓-止盈(§6.1 active scale-out §13#34)。decision 38 + 全离线 `*us_short*` 1925 OK 零回归。**批4 capstone 余 4 刀**：4d-ii-c §8 sizing（reduction_stack+折扣+单票/流动性 cap+below-min→观察）｜4d-ii-d 基组（周建仓上限·同主题·现金分配+§9 action_rank）｜4d-ii-e §10 machine_record 装配+no-dangling｜4d-ii-f lifecycle-eval(先于 render)+内容无关周报 render+action_table CSV。批5 = provider/live（GATED、SR-PROVIDER-001）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-24 **US-short 批4 slice 4d-ii-a 周末 pipeline 分析证据段 已提交**（Codex 起草→`审查 FAIL`(sub_mode consumer-validation fail-open)→修→re-`审查 PASS`，1 Required resolved）：新 `engine/us_short_weekend_analysis.py`：`analyze_rows(rows, *, market_axis_regimes, prior...)` 把 4d-i 选股结果按**每行**跑 §5 `classify_hard_veto`+§6 路由价格引擎（candidate→`support_atr_engine`/holding→`holding_exit_engine`）+§8.1 forward/event-gap+§4.2 `core_score`，§7 `compute_market_risk_regime` **算一次**附结果级。**仅证据**：不决策 final_action/不跑削减叠法 cap/不分配现金/不排名(=4d-ii-b)、不装配 §10 machine_record(4d-ii-c)、不渲染(4d-ii-d)。§8 防御档 breakout→pullback 守门（极度防御 never、唯一例外=caller 真-True probe；min-size/slot/in-band cap 留 4d-ii-b）。**consumer-validation fail-closed**：sub_mode 缺省→pullback、非法/None/非str→raise（复用 price engine `PRICE_SUB_MODES` 单源）、probe present 须真 bool、row_source/profile/ticker/forward 非法→raise、ticker 复用 `canonical_us_ticker` 一股一行。analysis 41 + 全离线 `*us_short*` 1887 OK 零回归。**批4 capstone 余 3 刀**：4d-ii-b 决策+基组（§6 链→final_action、§8 削减叠法+cap+cash、§9 action_rank）｜4d-ii-c §10 machine_record 装配+no-dangling｜4d-ii-d lifecycle-eval(先于 render)+内容无关周报 render+action_table CSV。批5 = provider/live（GATED、SR-PROVIDER-001）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-24 **US-short 批4 slice 4d-i 周末 pipeline 选股前段编排 已提交**（Codex 起草→`审查 FAIL`(data_context 验证 fail-open)→修→re-`审查 PASS`，1 Required resolved）：新 `engine/us_short_weekend_pipeline.py`：`run_selection(now_et, sessions, data_context, *, eligibility_governance)` 把 §18.2 批4/2/3 原语串成首个离线选股编排——`resolve_canonical_asof`(4a)→[盘中死区 `OutOfWindowError`→**no-emit**(无候选/下游不产)、decision_date 穿线进结果]→universe→Pass1 `cheap_eligible`+`inject_catalyst_recall`(4c-ii)→Pass2 `pass2_safety_admit`(复用 §5 hard_veto：候选 entry_hard_veto 排除、持仓**强制入** veto 仅 surfaced §4.0)→候选集+admitted+holdings。**consumer-validation 全 fail-closed**：governance 经 `validate_eligibility_governance`、data_context closed-world、Pass2 信号键经 `canonical_us_ticker`(gate 新公开别名)规范化 + **精确覆盖候选集**(缺/陈旧/非规范/重复 raise、去 `.get` 默认)、持仓 ticker 同一规范身份(拒 A 股码/重复、发 canonical)。`data_context` 全注入(batch5 同 seam 填真 provider)。pipeline+gate 76 + 全离线 `*us_short*` 1846 OK 零回归。**批4 capstone 余半 = slice 4d-ii**：分析链(hard_veto→price_engine→regime→sizing→forward_events→action_rank)→§10 machine_record 装配→no-dangling→lifecycle eval(**先于** render)→内容无关 §11.2 周报 render。批5 = provider/live（GATED、SR-PROVIDER-001）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-24 **US-short 批4 slice 4c-ii-b Pass2 audit-safety-gate + catalyst_recall 注入槽 + 候选 ticker 规范身份 已提交**（Codex 起草→`审查 FAIL`(catalyst base 去重)→修→re-`FAIL`(候选 ticker 规范性)→修→re-`审查 PASS`，2 Required resolved）：`engine/us_short_eligibility_gate.py` 加 `pass2_safety_admit`（Pass2 **复用 `classify_hard_veto` 不重写**：候选 entry_hard_veto→不入 Top15、持仓强制入 veto 仅 surfaced 供 §9）+ `inject_catalyst_recall`（catalyst 注入槽：feed=None 不伪造覆盖、有效 list→merge、真 feed=批5）+ `_canonical_us_ticker`（strip+大写+US 符号形、拒 A 股码、**镜像 runner `_parse_us_ticker`** + triangulation 防漂移）一致用于 cheap_eligible 输出 + inject base/recall → 候选集**规范唯一身份**（`AAPL`/`aapl`/` AAPL ` 同一、语义重复拒/去重、保 BRK.B）。gate tests 56 + 全离线 `*us_short*` 1826 OK 零回归。**批4 gate 逻辑（Pass1+Pass2+catalyst）全齐 → 剩 slice 4d 编排器**（把 resolver/日历/gate/批2-3 原语串成周末 pipeline）。批5 = provider/live（GATED）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-24 **US-short 批4 slice 4c-ii-a Pass1 cheap-eligibility gate + governance runtime validator 已提交**（Codex 起草→`审查 FAIL`(缺失状态旗 fail-open)→修→re-`审查 PASS`，1 Required resolved）：新 `engine/us_short_eligibility_gate.py`：`cheap_eligible(row, governance)` Pass1 谓词（交易所∈白名单 + price/ADV/market-cap ≥ floor + disqualifying 状态旗）**fail-closed**——任一字段缺/非有限/bool/str → 显式 reason + 不合格（缺 = 未知→保守 §3.3、绝不冒充干净；缺→fail-closed 全字段统一）；+ `load/validate_eligibility_governance`（runtime re-enforce 4c-i 冻结 v1 const、drift fail-closed、triangulation const==preset）。gate tests 33（穷举坏形 + 统一-缺失探针零 fail-open）+ 全离线 `*us_short*` 1803 OK 零回归。**批4 后续 slice**：4c-ii-b Pass2 hard_veto 包装 + catalyst 注入槽｜4d 编排器。批5 = provider/live（GATED）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-24 **US-short 批4 slice 4c-i eligibility governance 冻结契约已提交**（Codex 起草→`审查 FAIL`(治理语义未 const-pin)→修→re-`审查 PASS`，1 Required resolved）：新 `presets/us_short_eligibility_governance_20260624.json` + schema = Pass1 cheap-eligibility 契约（exchange_whitelist NYSE/NASDAQ + price/ADV/market-cap floor + 基础状态 disqualifier + 3 个 §13.1 calibration 锚 #2/#19/#21）。治理 v1 语义**全 const-pin**（白名单 / 3 阈值 / disqualifier 集 / 3 锚），**校准 = reviewed schema+preset 版本变更**（非静默改、不可漂移削弱）；阈值 = 初始 §13.1 #2 prior、非验证 alpha、不蕴含 ship-gate。Pass2 安全闸复用 hard_veto 不在此、catalyst feed = 批5、runtime validator 归 4c-ii。schema-test 21（drift 全 reject + 锚交叉核对解析 §13 registry title）+ 全离线 `*us_short*` 1770 OK 零回归。**批4 后续 slice**：4c-ii Pass1/Pass2/catalyst 谓词（消费本契约）｜4d 编排器。批5 = provider/live（GATED）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-24 **US-short 批4 slice 4b 静态冻结 NYSE 日历 + session-builder 已提交**（Codex 4 轮 `审查 FAIL`→修→re-`审查 PASS`，3 Required resolved）：新 `engine/us_short_market_calendar.py`（`load_market_calendar` + `build_sessions`：冻结假期/半日市表 → 喂 resolver 的 sessions，weekday 过滤 + 假期跳过 + 半日市 close）+ 冻结 preset `presets/us_short_market_calendar_2026_2027.json`（NYSE 假期×20 + 半日市×3）+ schema。`validate_market_calendar` = runtime consumer-validation 边，fail-closed 结构校验**整类闭合**（顶层闭世界 / real-date / 范围 / 重复 / holiday∩half-day / 半日市恰 13:00 / provenance 闭世界 + status / holiday+half_day 须工作日；28-shape 对抗扫描背书）。日期数据为 offline rules-derived、**未经权威核对**（须官方 NYSE 日历交叉核对后才可 live，SR-PROVIDER-001 / 批5）。builder 29 + 全离线 `*us_short*` 1749 OK 零回归。**批4 后续 slice**：4c eligibility 门 + gate 谓词｜4d 编排器（stage 序列 + decision_date 穿线 + consumer-validation + lifecycle-eval-先于-render + 内容无关输出装配 + 幂等私密写）。批5 = provider/live（GATED）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-24 **US-short 批4 启动 + slice 4a canonical 决策日解析器已提交**（`c8e9097`，Codex 2 轮 `审查 FAIL`→修→re-`审查 PASS`，4 Required resolved）：周末 pipeline 接线首刀——`engine/us_short_canonical_asof.py::resolve_canonical_asof` 纯函数（注入 US 市场日历→decision_date），**美股两边窗口**（上一 session 收盘起 / 目标 session 9:30 开盘止）+ **盘中死区 raise `OutOfWindowError` fail-closed**（A 股单边、无死区）；真日历日门（strptime 拒不存在日期）+ 拒重复 session date + 硬编码 RTH；canonical-only、恒 live、不分类 explicit historical（镜像 A 股 `resolve_canonical_asof` 工程骨架）。§18.2 加 build-vs-wire note：实测仓内无 universe/eligibility/catalyst_recall/Pass2-safety-gate 模块或 governance preset → 批4 自建 gate 谓词 + 新 eligibility preset；**现有 52 engine + 批1 转换器不改**（编排器只注入 as_of 调用）。40 tests + 全离线 `*us_short*` 1716 OK 零回归。**批4 后续 slice**：4b 静态冻结 NYSE 日历 + session-builder｜4c eligibility 门 + gate 谓词｜4d 编排器（stage 序列 + decision_date 穿线 + consumer-validation + lifecycle-eval-先于-render + 内容无关输出装配 + 幂等私密写）。批5 = provider/live（GATED）。另：AGENTS.md `审查` 最终回复格式简化（`b8fcc6d`，独立 scope，已 Codex PASS）。纯/离线、不交叉 A 股、无 provider/live/DataHub。
- 2026-06-24 **README 反膨胀 route-row 行长 guard 已提交（机械根治膨胀/漂移）**(`8a905c77`,Codex 起草→`审查 FAIL`(转义管道切列 fail-open)→修→re-`FAIL`(行检测漏无空格/缩进)→修→`审查 PASS`,起草+2 轮修复):新 `tests/test_readme_route_row_length.py` 焊死 README 路由行——描述列 **col1>350 字即 CI 红**(位置无关、col2 文件列**不**限);当前 over-cap 稳定历史行(**33**:5 A-long+26 A-short+1 Phase+1 Production,**机器扫描**得非口头假设)经整行 sha256 **grandfather** 放行,改它即须压(增量清理)。**robust Markdown 解析**(两轮修闭整类):按未转义 `|` 切列(转义 `\|` 是字面竖线不误切)+ `lstrip().startswith("|")` 行检测(容缩进+容无空格)。压 us_long 路由行成薄指针;AGENTS.md 加一行薄指针原则(细节去 docstring/owner-doc/tests/register)。Required `R-README-ROUTE-ROW-LENGTH-{ESCAPED-PIPE,TABLE-ROW-DETECTION}-BYPASS`(均 resolved@`8a905c77`)。**新 README 行靠机器 guard 焊住、不再靠记性。** 纯 docs+test、不动 code、不交叉 A 股。
- 2026-06-24 **README us_short 路由行压缩已提交（docs-only，反 doc-drift）**(`bbd1f7a1`,Codex `审查 PASS` 一轮):`docs/README.md` 的 68 行 `| US-short` 路由行由 ~4-5K 字 prose 压成**薄指针**（批/§ topic: 入口函数 + 一句话 + 关键不变式 | 文件;us_short 块 142721→22303 字符 −84%、中位行 324）。**只压 col1 描述、col2 文件指针逐行字节保留**（HEAD vs 现 col2 diff 空=零指针丢失,定论）。删的另有权威:多条不变式→docstring、no-provider/无 A 股 blanket→设计 §18、Tests 枚举→test 文件、hash/review→SESSION_LOG/git。**新 us_short 路由行应保持薄指针**（topic+文件+一句话,细节去 owner doc/docstring/tests/register）。仅 us_short lane;a_short/a_long 设计已完成·文档不动,us_long 待压。**反膨胀根治=机械 guard**（route-row col1 行长 cap + grandfather 旧长行 by-hash,采 Codex 方案,为后续 docs 刀;光靠文字规范必漂移）。**纯 docs、不动 code/test、不交叉 A 股。**
- 2026-06-24 **US-short 批3 fresh re-audit 2 个同族 fail-open 缝修已提交**(`3863c5ff`):三代理独立重审批3(其余 27 模块 CLEAN)抓到上一刀防自欺族的 2 个漏网——① P2 `paper_ledger._validate_record` 加 outcome⇔gross 符号门（filled_stopped gross<0 / filled_tp_exit gross>0），堵持久化 `paper_performance.json` 把止损记成赚;**只在 label 持久化处加**（scorecard 用 net 符号分类、不传 label 故不加,留 NOTE）。② P2 `build_paper_scorecard` 嵌 de-id 排序 `realized_legs`（{net,cost}/closed、无票名$），`validate` 从 legs 重导 win/loss/total_cost（严格类型拒 bool）+net_basket → source-traceable（镜像 nav/multiweek;堵伪造 net_basket 流进 nav、翻 #24;全自洽 re-forge 系统级限）。+9 测试、全离线 `*us_short*` 1676 OK 零回归;docstring + README route 同步。Required `R-USSHORT-BATCH3-{NET-RESULT-OUTCOME-SIGN,SCORECARD-MAGNITUDE-TRACEBACK}-GAP`（均 resolved@`3863c5ff`）。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-24 **US-short 批3 fail-open 守门类 whole-class 修已提交**(`1dc20976`):两类一次修——① P1 `paper_net_result._validate_fill_shape` 加 v1-long 几何门（filled_stopped exit<fill / filled_tp_exit exit>fill / 拒 exit==fill,堵"止损记成赚/止盈记成亏"）+ 删重复 `_COST_KEYS`;② P2 三 validator（`paper_scorecard_comparison` delta+marginal、`shadow_compare` overlap_count、`upgrade_gate` upgrade_review_due）加严格类型门拒 bool 等值绕过 `False==0`（镜像 `multiweek_comparison`），whole-class 扫余下裸 `!=` 数值重导无第 4 处残留。+7 测试、全离线 `*us_short*` 1667 OK 零回归;4 面 docstring + README route 同步。Required `R-USSHORT-BATCH3-{PAPER-NET-FILL-GEOMETRY,VALIDATOR-NUMERIC-BOOL-BYPASS-CLASS}`（均 resolved@`1dc20976`）。来源 = cc_r1 + codex_r3 共识（Codex 抓 paper_net_result 几何=我 cc_r1 漏项、我抓 bool 绕过类——同类不同实例 → whole-class 修）。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-24 **US-short 批3 §11.2 输出渲染收尾（coverage §11.5 + hot_excluded ⑤ section formatter）已提交 → ▍批3 工程完成**(`523befa5`,Codex `审查 PASS`(起草一轮过、无 Required)):`engine/us_short_coverage_honesty.py` 加 `render_coverage_section`(per-row `build_row_coverage` 列表 de-id 聚合成 §11.5 节：逐条 re-validate + 每 coverage_status 行计数[冻结序零显] + 跨行 distinct gap_tags[无票名];空/全 full 非空白);`engine/us_short_hot_excluded.py` 加 `render_hot_excluded_banner`(§11.2 banner ⑤ 一行,只渲 de-id `public_heat_count`、**绝不渲 holdings 私密票名**;非 dict/非 int/bool/负 count 拒)。两者纯/离线/de-id,镜像已有 `render_exclusion_section`/`render_observe_split`;`render_weekly_report` 保持 content-agnostic(Codex 独立确认:组装=批4)。§18.2「输出渲染」四交付物(price_clock/exclusion/hot⑤/coverage)现全齐。target formatter/renderer 92 + 全离线 `*us_short*` 1660 + doc guard 39 全绿零回归。**无 Required**。**▍批3 工程完成**:§18.1 #3/#8/#9/#10/#11/#12/#13/#19/#20/#21/#24 全部模块+测试+输出渲染 formatter 齐(逐项 Explore 审计核实;#11 提醒机制·覆盖保险 guard 在 `us_short_lifecycle_eval`、#20 lifecycle 家族 eval+store+render+readiness)。**「批3 完成」= 离线工程 scope,≠**:① 周报端到端真渲染这些节(需批4 pipeline 把 formatter 接进 report_data 才跑得出)② §12.2 ≥12 周对比 / ship-gate 12 月**前向时间门**(攒真实数据、非工程能完成)。**批3 后续门**:批4 周末 pipeline 编排(canonical 决策日解析器 + universe→Pass1→Pass2→engine→output 编排,纯/离线/fixture)/ 批5 provider·live(GATED、单独授权、SR-PROVIDER-001)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-24 **US-short 批3 §12.2 multi-week 4 档 balanced-vs-shadow 对比（双向诚实 capstone）已提交**(`1743ff06`,Codex `审查 PASS`(起草一轮过、无 Required;前置 multiweek round-2 的 delta 严格类型门教训→一轮过)):新 `engine/us_short_paper_multiweek_comparison.py`:`build_multiweek_comparison(multiweek_by_profile)` 配对冻结 4 档(`core_score.PROFILE_NAMES`,各 re-validate)的 `build_multiweek_scorecard` 聚合 → 嵌 4 档全口径 de-id 聚合并排(shadow 多买亏损/成本/现金拖累/坏票率 **+ 回撤**都可见,§12.2 双向全口径)+ `vs_balanced[shadow]` per-metric `delta=shadow−balanced`(final_cumulative_net/max_drawdown/overall_bad_pick_rate/cum_total_cost_fraction/cum_loss/cum_unfilled_cash/cum_win,任一 None→None §12.1 不虚高)+ `theme_weight_marginal_net`=balanced−theme_off 的 final_cumulative_net(#24 窗口级)。**周窗对齐不变式**(§12.2 PIT 同窗+固定 TopN):4 档须同 as_of 序+每周同 selected_total,错日/混 TopN fail-closed。closed-world 从聚合**重导每 delta** + **delta 严格类型门**(None 或 finite、拒 bool 等值绕过 `False==0.0`——前置 multiweek 教训)+ 嵌入聚合 re-validate + ship-gate 隔离 paper-only boundary。镜像单周 comparison。target 20 + 全离线 `*us_short*` 1653 + schema 481 + doc guard 39 全绿零回归。**无 Required**。**§12.2 双向诚实证据链现 end-to-end:周 scorecard → 多周聚合 → 4 档对比 + 升级闸(数周)**;批3 余项 = weekly 各节 enrichment(§11.2 section 渲染接线)/ de-id 汇总(可选);weekly pipeline=批4。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-24 **US-short 批3 §12.2 per-profile 多周聚合（全口径累计 + 嵌 nav_drawdown 回撤,source-traceable）已提交**(`165cceba`,Codex 起草→`审查 FAIL`(cumulative 非 source-traceable + n_weeks 等值 float)→修→re-`FAIL`(非计数数值字段 cost/bad_pick bool 钻过 `==` 整类)→修→re-`FAIL`(register 旧 residual FAIL 段未标 superseded、与 round-2 resolved 矛盾=文本交接)→修→re-`审查 PASS`,起草+3 轮修复):新 `engine/us_short_paper_multiweek_scorecard.py`:`build_multiweek_scorecard(period_scorecards)` 把一档按 as_of 周序的 scorecard 聚成多周摘要——**两口径诚实分离**:`cumulative`(所有周全口径仓位累计 filled/亏损/现金拖累/held/成本/坏票率,分母=跨周仓位)+ `nav_drawdown`(realized 篮子曲线 max_drawdown/累计净值,分母=realized 周,open 周计覆盖不上曲线 §12.1 不虚高)。**source-traceable 反自欺**:artifact 带 de-id 周级 `period_source`(每 `{as_of,scorecard}` 脱敏计数),`validate_multiweek_scorecard` closed-world **从源重算** nav(==`build_nav_drawdown(period_source)`)+ cumulative(==`_derive_cumulative`,forged 自洽全 0 tally 拒)+ **全数值字段严格类型门**(n_weeks+7 计数 strict 非负 int / cost finite 非负 / bad_pick finite-或-None,拒 `False==0.0`/`True==1.0`/`2.0==2` 等值绕过)。**不接 4 档 comparison**(下一刀)。target multiweek+nav+scorecard 88 + 全离线 `*us_short*` 1633 + schema 481 + doc guard 39 全绿零回归。Required `R-USSHORT-BATCH3-MULTIWEEK-{CUMULATIVE-TRACEBACK,REGISTER-STATE-DRIFT}-GAP`(全 resolved@`165cceba`)。教训:数值字段值 `==` 下 bool/float 等值会绕过(整类须每字段先严格类型门);评审轮 register 别让旧 FAIL 段排在新 resolved 后不标 superseded。**回撤 → 多周聚合完**;批3 余项 = 4 档多周 balanced-vs-shadow 对比(嵌本建块 + drawdown delta,下一刀)/ weekly enrichment(batch4-ish)/ de-id 汇总(可选)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-24 **US-short 批3 §12.2 NAV 路径/回撤原语（双向诚实回撤指标,#8 解锁）+ later-cut forward-ref 整类零残留 已提交**(`39f6df66`,Codex `review PASS`(起草一轮过、无 Required)):新 `engine/us_short_paper_nav_drawdown.py`:`build_nav_drawdown(period_scorecards)` 把一档按 `as_of` 周序的 `{as_of,scorecard}` 列复利成**周度 realized 净值曲线** → `max_drawdown`(非正、镜像 a_long `max_drawdown_on_levels` 工程约定·借工程不借市场规则)+ `final_cumulative_net` + 覆盖对账(`n_realized`/`n_unrealized`)。判定:单周 scorecard **path-INDEPENDENT**,回撤是**跨周**指标 → 立原语(非塞进单周 scorecard);NAV 路径用周度 realized 复利而非每日盯市,因 §12.1 不虚高(未实现持仓不 mark 进净值)+ §12.2 本就周度 forward。**反自欺/不虚高**:`as_of` 严格递增防 look-ahead/重排/重复周、再验每张 scorecard(不信上游)、未实现周(`net_basket`=None)计覆盖但**不 impute**、`net≤−1`(NAV≤0,止损多头不可能)fail-closed、冻 paper-only boundary(镜像 scorecard)、`validate_nav_drawdown` closed-world 从内嵌 realized nets 重算。**不接 comparison**(单周 path-independent;多周 wiring=下一刀)。**doc-only B-ripple 整类零残留**:scorecard/comparison/shadow_compare{,_store,_summary}/paper_fill 的 in-file docstring + README 把已落模块(two-way comparison/#8/`paper_eval_gate`/`upgrade_gate`/`paper_ledger`/summary/store/perf accounting)由「later cut」改为指向各已落模块(位置无关、含 in-file docstring);legit deferred(multi-week wiring/weekly enrichment/#34 time-stop/vocab/batch-4 pipeline)全留;README 加 nav_drawdown 路由行。target NAV 25 + 全离线 `*us_short*` **1603** + tests/schema **481** + doc guard 39 全绿零回归。**无 Required**(Codex 起草一轮 PASS)。**#8 → 回撤原语完**;批3 余项 = 回撤接进多周 balanced-vs-shadow comparison(下一刀)/ weekly 各节 enrichment(batch4-ish)/ de-id 汇总(可选)/ banner README Optional。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-24 **US-long Cut 0 Top20 计数合同修复（Codex re-审查 PASS）已提交**（fix `a0487891` + 本 closeout）：Codex `审查 FAIL` 指出设计权威内部计数冲突——§5「硬阈值幸存者 <20 报实际只数、不硬停」vs §11/§17 写死 20 行、`not_recommended=20−推荐数`（selected<20 时公式即错）。修为单一**实际数量合同**：§5 `selected_count=min(20,幸存者)`，下游派生 `recommended≤min(5,selected)`/`not_recommended=selected−推荐数`，run_manifest 记 `selected_count`+`top20_shortfall_reason`，§11 文件清单 + §17 验收 #4/#6/#11 全对齐（§5 单一来源；§6/§17#5 recommended「≤5 可少于」本就一致、不动）。docs-only（normal-20+<20 survivor fixture 留 Cut 6 输出契约刀）。`R-USLONG-CUT0-TOP20-COUNT-CONTRACT-DRIFT` resolved@`a0487891`。Cut 1（account_state+CSV 转换器+私密守卫，复用 us_short）仍为后续门、不在本修复内启动。
- 2026-06-24 **US-long Tier 2 启动 + v3 设计权威入 repo（Cut 0·doc-only）已提交**：桌面 `v3_us_long_design.md` verbatim 移植入 repo 作 `docs/us_long_system_design.md`（美股中长线 us_long 系统**单一设计权威**，对标 `docs/us_short_system_design.md`；设计 substance D1-D11/§1-§19 字节不动，仅 header + §20 状态随入 repo 更新），桌面稿删除退役；路由镜像 us_short 入 `AGENTS.md`（Reference framework policy 权威声明 + 文件索引）/ CURRENT lane-owner specs / `docs/README.md` routing 表。Tier 2 = 机器骨架（样例数据）：schema + 治理 preset + 纯引擎 + runner + CSV 转换器 + 私密/排他守卫，样例端到端只写 staging/mock、不写 results_private、不联网；foundation-first，真实数据 / provider / adapter / DataHub / 联网 gated 到 Tier 4+。**开放项（Cut 1+ reconcile）**：既存 `presets/us_long.yaml`（result_dir=result/us_long、reserved_phase_9）+ `research/results/us_long/README.md`（lane_output_root）结果路径与设计 §14「state/us_long/results_private/」不一致（§14 已明确 override），待私密/排他守卫刀统一并翻 us_long.yaml active。Cut 1 = account_state 2 schema + CSV 转换器 + 私密路径守卫（复用 us_short）+ `.gitignore` 私密区 + tests（空持仓表也跑 D9）。不交叉 A 股、不 auto-push。
- 2026-06-23 **US-short 批3 §12.1 #8 多日平仓 + paper-fill order geometry 硬化已提交**(`7b010ebe`,Codex 起草→`review FAIL`(multi-day 几何 P0)→修复→re-`review FAIL`(same-day producer 同类 P0)→修复→re-`review PASS`,起草+2 轮修复):新 `engine/us_short_paper_multi_day_exit.py`:`simulate_multi_day_exit(held_position, day_bars)` 把 filled_held 仓位按 §12.1 同款保守规则**逐日**解平仓(首个 low<=stop→止损[STOP 优先]、否则首个 high>=tp→止盈、窗口内无命中→仍 filled_held 未实现[v1 无时间止损/主动减仓 #34、§12.1 不虚高]),出 fill_result 同 paper_fill 形喂 `paper_net_result`(其 `_STATUS_EXIT_REASONS` 改 set-valued 接 same_day|multi_day、向后兼容;ledger/scorecard 消费 net_result 无 exit_reason 不受影响)。**order geometry 硬门(两 P0、同 geometry 类)**:multi-day + 同日 `simulate_fill` 都校 passive levels 须 **bracket 入场** `stop_clear_price < fill < take_profit_exit_price`(严格)+ pullback `limit_order_price` 须在 `[valid_entry_low,valid_entry_high]` 带内;inverted/equal/越带 在任何 bookable 输出前 fail-closed(否则"止损"记成赚、"止盈"记成亏);breakout cap/Step0/STOP 优先/v1 expiry 不动。target paper-fill/multi-day/net 61 + 全离线 `*us_short*` 1578 全绿零回归、boundary+doc+route 47、import heavy=NONE。Required `R-USSHORT-BATCH3-{MULTIDAY-EXIT-LEVEL-RELATION,PAPER-FILL-ORDER-GEOMETRY}-GAP`(全 resolved@`7b010ebe`)。**#8 完** → 回撤(NAV 路径,消费多日实现的 net)可做;批3 余项 = 回撤 / weekly 接线(batch4-ish)/ de-id 汇总(可选)/ banner README Optional。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 §12.2 upgrade-gate forward-obs discover（①③ artifact 层 + live-forward provenance）已提交**(`d4225ace`,Codex 起草→`审查 FAIL`(缺 live-forward provenance、12 backfill 桶推到 due)→修复→re-`审查 PASS`,起草+1 轮修复):新 `engine/us_short_upgrade_obs_discover.py`:`discover_forward_observations(run_as_of, *, buckets_dir)` 扫私密 shadow_compare 桶,每桶经 `store.load_shadow_comparison(path, expected_as_of=run_as_of)`→ §18.0 guard + §12.2 比较契约 + 桶名==内容 + 陈旧门(晚于 run_as_of=look-ahead §12.2 ①③)全生效;返回 de-id `{as_of}` 升序+唯一喂 `build_upgrade_eval`;镜像 a_short discover。**live-forward provenance**(§12.2 ①,镜像 a_short `concept_membership=='forward'`):store 记录契约加 `observation_kind`(冻结集、`_validate_record` 校;record 现 `{as_of, comparison, observation_kind}`、write 默认 `LIVE_FORWARD`),**discover 仅数 `observation_kind==LIVE_FORWARD`**,replay/backfill/manual/无标记/坏标记 SKIP(date ordering 分不出真 live 与今日补写历史桶,12 backfill 桶不能假装 12 live 周)。纯 IO/离线复用 store、无 jsonschema/provider。target store+discover 40 + 全离线 `*us_short*` 1559 全绿零回归、boundary+doc+route 47、import heavy=NONE。Required `R-USSHORT-BATCH3-UPGRADE-OBS-DISCOVER-LIVE-PROVENANCE-GAP`(resolved@`d4225ace`)。**§12.2 升级闸现 end-to-end:discover(live-only 输入)→ gate(决定)→ banner(露出 ④)。** 批3 余项 = de-id 汇总(可选)/ banner README Optional / #8 多日 held net(跨模块)/ 回撤(需 #8)/ weekly 接线(batch4-ish)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 §12.2 upgrade-gate runtime banner（④ 运行时醒目横幅、ASCII/GBK-safe）已提交**(`153f86aa`,Codex `审查 PASS`(起草一轮过、无 Required;1 Optional:README 措辞「each…caveat」后续对齐——仅 DUE 两态带 caveat、ACCUMULATING 不带,留下轮 `执行` 改)):新 `engine/us_short_upgrade_banner.py`:`upgrade_banner(eval_result, *, governance)` 把 `us_short_upgrade_gate` eval 渲染成**一行 ASCII(GBK-safe)运行时横幅**,让跑周报的人在 Windows GBK 控制台看见升级就绪态(不只靠周报文字)。**经 `validate_upgrade_eval` 复校**(governance-aware re-derive):自授权(当前无 margin 下 `comparison_win_margin_frozen` 改 True)/畸形/look-ahead eval → fail-closed 到保守 UNAVAILABLE,绝不渲误导 ready。逐态:ACCUMULATING N/min;DUE-但-margin-未冻 → 不授权;DUE+margin-冻 → 可**提请**复审,DUE 两态带「USER decides、绝不自动切生产」声明(§12)。最终 `.encode('ascii')` 兜底;只 import 纯 `us_short_upgrade_gate`(无 jsonschema)。target 7 + upgrade_gate+banner 37 + 全离线 `*us_short*` 1546 全绿零回归、boundary+doc+route 47、import heavy=NONE。批3 §12.2 升级闸续刀 = forward-obs discover/accumulate(扫私密桶)+ de-id tracked 汇总;另 #8 多日 held net(跨模块)、回撤(需 #8)、weekly 接线(batch4-ish)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 §12.2 anti-self-deception upgrade gate（pure readiness decision、§12.2 升级闸 capstone）已提交**(`0b7f0868`,Codex 起草→`审查 FAIL`(自授权 readiness / look-ahead / 夹带 obs / 非dict gov)→修复→re-`审查 PASS`,起草+1 轮修复):新 `engine/us_short_upgrade_gate.py`:`build_upgrade_eval(forward_observations, *, as_of, governance)` 数 forward(live 决策当日·PIT·无 look-ahead)周度两轨对比观测 vs 冻结 `min_comparison_weeks`(12),由 `margin_frozen` 闸住(**§12.2 ②**:胜出 margin 仅当 governance 有数值有限 `comparison_win_margin` 才算冻;**当前 governance 无 → 绝不授权升级**,防"先看数据再定胜出线")。`decision_status` ∈ accumulating / review_due_margin_pending / review_due_ready(后者=可**提请**复审、用户决定、**绝不自动切生产** §12);镜像 A 股 `runners/a_short_overlay_eval`。**`validate_upgrade_eval(summary, *, governance)` RE-DERIVE 每可变闸自权威**(禁自授权 readiness:`comparison_win_margin_frozen==margin_frozen(governance)` + `min`==governance 值);obs 须 EXACTLY `{as_of}`(de-id)、`<= eval as_of`(§12.2 ① 无 look-ahead)、严格升序+唯一(③);非dict gov fail-closed;closed-world + non-production boundary。**不改 governance preset**(margin 经未来 reviewed 冻结);纯/离线无 jsonschema/provider。target 30 + 全离线 `*us_short*` 1539 全绿零回归、boundary+doc+route 47、import heavy=NONE。Required `R-USSHORT-BATCH3-UPGRADE-GATE-SELF-AUTHORED-READINESS-GAP`(resolved@`0b7f0868`)。批3 §12.2 升级闸续刀 = forward-obs discover/accumulate + de-id 汇总 + 运行时横幅;另 #8 多日 held net(跨模块 exit_reason)、回撤(需 #8)、weekly 接线(batch4-ish)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 §12.2 two-way scorecard comparison（balanced-vs-shadow 双向诚实 + fixed-TopN 同分母）已提交**(`bf9847eb`,Codex 起草→`审查 FAIL`(fixed-TopN 同分母漏校)→修复→re-`审查 PASS`,起草+1 轮修复):新 `engine/us_short_paper_scorecard_comparison.py`:`build_scorecard_comparison(scorecards_by_profile, *, as_of)` 配对冻结 4 档(`core_score.PROFILE_NAMES`,各 re-validate)的 `build_paper_scorecard` 输出 → **嵌入 4 份全口径 de-id scorecard**(§12.2 双向全口径:shadow 多买亏损/成本/现金拖累/坏票率都可见)+ `vs_balanced[shadow]` per-metric `delta=shadow−balanced`(任一 unrealized→None §12.1 不虚高)+ `theme_weight_marginal_net`=balanced−theme_off(#24 NAV 级赛道边际)。**fixed-TopN 同分母**(§12.2 固定 TopN/禁止挑样本):`_assert_same_denominator` 要求各档同 `selected_total`、build+validate 两侧调,混篮子(1-stock vs 3-stock)fail-closed。**冻结 ship-gate 隔离/paper-only boundary**;`validate_scorecard_comparison`=closed-world(exact 键集 + boundary + primary==balanced + strict as_of + 每嵌入 scorecard re-validate + 同分母 + delta/marginal 重导自校);de-id 无票名;纯无 jsonschema/provider。target 23 + 全离线 `*us_short*` 1509 全绿零回归、boundary+doc+route 47、import heavy=NONE。Required `R-USSHORT-BATCH3-SCORECARD-COMPARISON-FIXED-TOPN-DENOMINATOR-GAP`(resolved@`bf9847eb`)。批3 §12.2 续刀 = 回撤(NAV 路径,入各 scorecard 再入对比)→ 升级闸防自欺;多日 held net(#8)、weekly 接线独立。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 §12.2 paper scorecard（per-basket 全口径聚合 + basket-lineage 契约 + paper-only boundary + closed-world validator）已提交**(`9310119e`,Codex 起草→`审查 FAIL`(basket 覆盖)→修复→re-`审查 FAIL`(closed-world sibling)→修复→re-`审查 PASS`,起草+2 轮修复):新 `engine/us_short_paper_scorecard.py`:`build_paper_scorecard(net_results_by_ticker, *, selected_tickers)` 把一篮子逐仓 `paper_net_result` 输出聚合成 §12.2 全口径指标(确定性·路径无关·全仓计入)——filled/现金拖累/held 计数、win/loss/flat + `bad_pick_rate`=loss/filled(§12.2 坏票率)、`total_cost_fraction`(§12.1 #18)、`net_basket`=等权 realized 篮子净值(closed net + unfilled 0.0 现金,仅 fully_resolved[无 open]且非空落账否则 None,§12.1 不虚高)。**basket-lineage 契约**(§12.2 禁止挑样本/全量结构化):取冻结选股身份 + ticker-keyed map、失败关闭除非恰覆盖(winner-only 子集/漏 loser/塞 stale 行拒)。**冻结 paper-only boundary**(evidence_level=paper / full_size_ship_gate_allowed=False / ship_gate_evidence_level=paper_not_live_normalized);`validate_paper_scorecard`=**closed-world 门**(EXACT 12 键集,夹带 tickers/顶层 ship-gate/track/evidence 拒)+ nested boundary exact + count 一致;输出 de-id 计数-only。自校每条 net_result 全 per-outcome 形;PAPER only 绝不满仓 ship-gate(§12/§18.1 #27);无新 schema;纯(无 jsonschema/provider/`a_short`)。target 33 + 全离线 `*us_short*` 1486 全绿零回归、boundary+doc+route 47。Required `R-USSHORT-BATCH3-PAPER-SCORECARD-{BASKET-EVIDENCE-CONTRACT,VALIDATOR-CLOSED-WORLD}-GAP`(全 resolved@`9310119e`)。批3 §12.2 续刀 = balanced-vs-shadow 双向对比 + 回撤(NAV 路径)→ 升级闸;多日 held 平仓 net(#8)、weekly 接线独立。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 §12.2 比较轨 shadow 脱敏 tracked 汇总（schema-first de-id 选股分歧汇总）已提交**(`fa17cb53`,Codex `审查 PASS`(起草一轮过、无 Required)):新 schema `schemas/us_short_shadow_compare_summary.schema.json`(§11.6 tracked de-id 契约:`additionalProperties:false` 各层 + 整数-only counts + const track/primary/boundary,结构性保证无票名/$/表现可夹带,de-id 门)+ 新 engine `engine/us_short_shadow_compare_summary.py`:`build_shadow_compare_summary(comparison, *, as_of)` 从过 `validate_shadow_comparison` 的比较建 de-id 汇总(拒未验比较)——每 shadow 档**选股集分歧 COUNTS**(balanced_only/shadow_extra/overlap SIZES、无票名)+ top_n/pool_size/selected_count + 冻结 boundary;`_assert_summary` = schema de-id 门 + 跨字段一致(strict real as_of / selected==min(top_n,pool_size) / 各档 overlap+balanced_only==selected==overlap+shadow_extra,doctored count 过不了);`write_*` schema 门后写、**无 §18.0 guard**(可证 de-id=§11.6 tracked-safe)。**#24** theme_off 分歧非退化;jsonschema 仅 de-id 门用、不拉 lifecycle/provider/`a_short`;镜像 `lifecycle_readiness`。engine 16 + schema 9 + 全离线 `*us_short*` 1453 + schema 481 全绿零回归、doc/route/boundary 47。批3 §12.2 续刀 = paper-NAV 双向全口径成绩单(接 paper_fill/net_result 算各档 NAV)→ 升级闸防自欺。**纯-ish/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 §12.2 比较轨 shadow 私密持久化（首个 persister + §18.0 写读对称 + bucket/namespace 守卫）已提交**(`734ae001`,Codex 起草→`审查 FAIL`(bucket/namespace gap:filename↔content 日期 + 跨 lane 私密目录)→修复→re-`审查 PASS`,起草+1 轮修复):新 `engine/us_short_shadow_compare_store.py`:`write_shadow_comparison`/`load_shadow_comparison` 把 §12.2 shadow 比较(含票名·私密 §11.6)按 DATED record `{as_of, comparison}` 落 gitignored 私密路径——**§18.0 P0 guard 写读对称** + validate(strict real as_of + §12.2 `validate_shadow_comparison` 投影契约)。**bucket/namespace 守卫**(`_check_bucket`,超 §18.0 隐私 guard、写读两侧):in-repo 须 canonical `shadow_compare_private/shadow_comparison_<as_of>.json`(拒 `model_paper_private`/`lifecycle`/`a_short`/`us_long` 跨 lane 及 filename-date 不符,§2.1 桶名=as_of + A/US 隔离);external 非 canonical 放行但 canonical-looking 名须配 as_of。**陈旧桶 fail-closed**(持久 as_of 比决策日新→`StaleShadowComparisonError`;同日幂等+更早周前向放行)。`shadow_comparison_path(as_of)`=canonical dated bucket;内联日期门含 `isascii()`、jsonschema-free;无新 schema(record 包投影契约);镜像 `lifecycle_store`/`paper_ledger`。target store 27 + 全离线 `*us_short*` 1428 全绿零回归、boundary+doc+route 47、import heavy=NONE(无 jsonschema/a_short)。Required `R-USSHORT-BATCH3-SHADOW-COMPARE-STORE-BUCKET-NAMESPACE-GAP`(resolved@`734ae001`)。批3 §12.2 续刀 = 脱敏 tracked 汇总 schema → paper-NAV 双向全口径成绩单 → 升级闸防自欺。**纯 IO/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 §12.2 比较轨 shadow #13/#24 首刀（shadow scoring_profile 选股比较投影）已提交**(`b579a957`,Codex 起草→`审查 FAIL`(契约未锁 weights/未路由)→修复→re-`审查 FAIL`(scorer 漂移/SESSION_LOG 超模板)→修复→re-`审查 PASS`,起草+2 轮修复):新 `engine/us_short_shadow_compare.py`:`build_shadow_comparison(scored_pool, *, top_n)` 把同一 PIT-冻结合格池在**每个冻结 scoring_profile** 下复用批2 `core_score` 重打分、按 (分数降序, ticker 升序) **确定性 FIXED top_n** 选股 → per-profile selection + `vs_balanced` balanced↔shadow 集差(固定 TopN+确定序=禁止挑样本 §12.2)。**#24** theme_off(theme 权重=0)归因基准:`vs_balanced['theme_off']` = 赛道权重边际选股贡献、可算+非退化。**三层冻结档契约锁**(`_FROZEN_PROFILES` const-pin 逐档 weights+role+live_eligible+shadow_only):`_check_governance` 核加载 preset + 核 `core_score` scorer 自载权重(`profile_weights==frozen`、选股前 fail-closed,堵 scorer 依赖漂移)+ `validate_shadow_comparison` 核产出 artifact;锁 sole-primary + theme_off theme==0。**#13** ship-gate 隔离 boundary(production/is_buy_advice/shadow_counts_ship_gate/changes_primary_selection 全 False)+ balanced 唯一 primary/live。镜像 A 股 comparison-track 工程骨架(`track=comparison_non_production`)非 A 股市场规则。无新 schema(validator=契约、复用冻结 `scoring_profile` preset)。target shadow 37 + 全离线 `*us_short*` 1401 全绿零回归、doc/route/boundary guards 全绿、import heavy=NONE。Required `R-USSHORT-BATCH3-SHADOW-COMPARE-{CONTRACT-ROUTE,CORE-SCORE-DRIFT,SESSIONLOG-MINIMAL-TEMPLATE}-GAP`(全 resolved@`b579a957`)。批3 §12.2 续刀 = 私密持久化(shadow_compare_private + §18.0 guard)+ 脱敏 tracked 汇总 → paper-NAV 双向全口径成绩单 → 升级闸防自欺。**纯/离线、无 jsonschema/provider、不落盘、不交叉 A 股。**
- 2026-06-23 **US-short price_clock DATE-ASCII gap 修（内联日期门加 isascii;whole-class 收尾）已提交**(`bd06ac50`,Codex `审查 PASS`):`engine/us_short_price_clock.py` 内联 `_strict_yyyymmdd` 加 `s.isascii()`——Arabic-Indic/fullwidth 8 位数字现拒(isdigit 收但 isascii 不收、`int()` 本会 coerce),恢复 §21 严格 ASCII。**whole-class 收尾**:全 engine grep `len==8 and s.isdigit()` 无 isascii = **0**(ledger + price_clock 两份内联拷贝都修、canonical lifecycle 本有)。+1 测试、price_clock 14 OK、全离线 `*us_short*` 1364 全绿零回归。闭 `R-USSHORT-BATCH3-PRICE-CLOCK-DATE-ASCII-GAP`(resolved@`bd06ac50`;= 上刀 ledger DATE-ASCII 连带登记项)。**纯/离线、不交叉 A 股。**
- 2026-06-23 **US-short 批3 paper ledger §12.1 #8-后续（paper_performance 私密落盘 JSON + 全 net-result 契约锁）已提交**(`05b397b9`,Codex 起草→`审查 FAIL`×N(net-shape 弱契约 / import 拖 jsonschema / format .csv-vs-.json / doc-drift / date Unicode)→修复→re-`审查 PASS`,起草+5 轮修复):新 `engine/us_short_paper_ledger.py`:`write_paper_performance` / `load_paper_performance` 把 paper_performance 记录落 gitignored `model_paper_private/`——**§18.0 P0 私密路径 guard 写+读对称**、写前校;load 缺/坏JSON/坏记录/tampered-row fail-closed。`_validate_record` **全自校 paper_net_result 契约**:strict **ASCII** YYYYMMDD as_of(内联 `_strict_yyyymmdd` `isascii()+isdigit()`、不引 lifecycle/jsonschema)+ entries 精确6键闭世界 + 各 outcome 不变式(cash 全0 / open 全None / closed 有限 gross·net·非负 cost·`net==gross−cost`)。**§12.1 设计权威更新**(design-owner 决定):canonical artifact = `paper_performance.json`(嵌套 typed 条目 JSON 无损;CSV 须 type-coerce + 0条目丢 as_of;§12.1 旧列表已不配已提交 cost_fraction 模型);design+README+code+test 一个权威、active `.csv` grep 归零。target 18 + 全离线 `*us_short*` 1363 全绿零回归。Required `R-USSHORT-BATCH3-PAPER-LEDGER-{NET-SHAPE,IMPORT-COUPLING,FORMAT-DRIFT,DATE-ASCII}-GAP`(全 resolved@`05b397b9`);**连带登记** `R-USSHORT-BATCH3-PRICE-CLOCK-DATE-ASCII-GAP`(committed price_clock 内联同款漏 isascii、留独立 scoped 修复)。教训:内联 canonical helper 别简化掉守卫;改 validator 契约后 4 active 教学面(module+函数 docstring+test docstring+README)必同步。批3 续片 = paper 多日平仓 realized net、比较轨 shadow #13/#24、price_clock date gap 修(按 §18.2)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 paper 净结果口径 §12.1 #8-后续（单笔模拟成交确定性 net + 全 per-status 形状锁）已提交**(`27265575`,Codex 起草→`审查 FAIL`(只锁 status+closed 价、未锁全 per-status 形)→修复(加 `_validate_fill_shape` 全锁)→re-`审查 PASS`,起草+1 轮修复):新 `engine/us_short_paper_net_result.py`:`paper_net_result(fill_result, *, cost_prior)` 把 `simulate_fill` 输出转**可复现** §12.1 net(同输入→同数;**PAPER only、绝非 ship-gate**)。**cost 模型**(§13 #18 prior 往返 return-drag,无 $):`total_cost=commission_fee+spread_cost+slippage_bps/10000`、仅扣 realized-closed;not_filled→net 0(没买上不当收益);stopped/tp→`net=(exit-fill)/fill − cost`;**held→未实现**(net=None、不当 net,realized 留多日平仓后续刀,§12.1 不虚高)。**全 per-status 形状锁** `_validate_fill_shape`(出任何结果前):not_filled 无 fill/exit/reason、held 正 fill·无 exit/reason、closed 正 fill+exit·`exit_reason==status 专属`(same_day_stop/same_day_tp_exit,`_STATUS_EXIT_REASON` 镜像 sim);违例全 `PaperNetResultError`——函数自校不信任 sim。无新 schema。target 17 + 全离线 `*us_short*` 1345 全绿零回归。Required `R-USSHORT-BATCH3-PAPER-NET-FILL-SHAPE-GAP`(resolved@`27265575`;教训:自校契约要全锁 status⇔price⇔reason 别只锁一半,Pre-Codex 自评「已自校」却没核全=自评不实)。批3 续片 = paper 多日平仓 realized net + 私密 ledger writer、比较轨 shadow #13/#24(按 §18.2)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 复权/公司行动门 §12.1 / §18.1 #29（paper_performance 可评估 fail-closed 门;evaluability ≠ ship-gate）已提交**(`af8c92ec`,Codex 起草→`审查 FAIL`(evaluable 误蕴含 ship-gate eligible)→修复(拆门+恒定不变式)→re-`审查 FAIL`(doc-drift 残旧字段/overclaim)→修复(active 教学面清零)→re-`审查 PASS`,起草+2 轮修复):新 `engine/us_short_paper_eval_gate.py`:`paper_performance_evaluability(adjustment_context)` 判 paper_performance 能否进 **paper/reporting/shadow 评估**:三 §12.1 公司行动确认(adjustment_mode/split-dividend/除权价位一致)**全字面 True** 才 `evaluable`,否则 `not_evaluable` + `unconfirmed` + 局部因 `blocks_paper_performance_due_to_corporate_action`。**fail-closed**:非字面 True(缺/False/None/truthy 非bool)皆不确认 → 默认 not_evaluable(SR-PROVIDER-001 复权 reconciliation 未证);闭世界拒 typo 键。**evaluability ≠ ship-gate 许可**:输出恒带 §12/§27 不变式 `full_size_ship_gate_allowed=False` + `ship_gate_evidence_level="paper_not_live_normalized"`(不随确认变)——paper 仅设计迭代、**绝非 full-size ship-gate eligible**、只 live_normalized 毕业。无新 schema。target 10 + 全离线 `*us_short*` 1328 全绿零回归。Required `R-USSHORT-BATCH3-PAPER-EVAL-GATE-SHIP-GATE-PERMISSION-GAP`(resolved@`af8c92ec`;教训:可评估≠有资格,两道证据门别合一字段;doc-drift——改字段名/语义后被改文件自身函数 docstring+测试 docstring+README Returns 须同 grep 归零)。批3 续片 = paper 落盘/绩效(#8 后续)、比较轨 shadow #13/#24(按 §18.2)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 paper fill §12.1 #8（确定性单订单日成交模拟 + v1 order_expiry 门;R3 起步）已提交**(`c5e7ed4d`,Codex 起草→`审查 FAIL`(order_expiry v1 锁未强制)→修复(加 expiry 门)→re-`审查 PASS`,起草+1 轮修复):新 `engine/us_short_paper_fill.py`:`simulate_fill(order, day_bar)` 按 **§12.1 写死、可复现** 规则模拟日线成交(同输入同结果·无随机;**paper 仅设计迭代、绝不判满仓 ship-gate**)。**v1 order_expiry 门**:出任何成交前读冻结 `order_expiry` enum(action_table `design_locked_enums` 单源)、不在 v1 锁集(`first_regular_session_only`)即 `PaperFillError`(缺失/非str/GTC/未知全拒;多日 GTC 是 lifecycle 候选、v1 不实现)。确定性序:Step0 `open` 在 `[valid_entry_low, valid_entry_high]` 否则 not_filled;Step1 pullback `low≤limit`→@limit、breakout `high≥breakout`→@`min(max(open,breakout),valid_entry_high)`、否则 not_filled;**同日保守出场**(`low≤stop`→止损、否则 `high≥tp`→止盈,**止损优先** §12.1 ②,只低估不虚高)。无新 schema、无落盘(私密 csv/复权 not_evaluable 门/绩效=后续刀)。target 25 + 全离线 `*us_short*` 1318 全绿零回归。Required `R-USSHORT-BATCH3-PAPER-FILL-EXPIRY-GATE-GAP`(resolved@`c5e7ed4d`;教训:写 fill 规则漏读 order_expiry 这条 §18.1 #8 明列契约字段,契约字段须逐条核)。批3 续片 = paper 落盘/复权门/绩效(#8 后续)、比较轨 shadow #13/#24(按 §18.2)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 price clock 一致性 §11.2 ④ / §21（validator + 焊进官方 render_weekly_report）已提交**(`718092ec`,Codex 起草→`审查 FAIL`(renderer 不消费 validator、仍渲不一致时钟)→修复(wire 进 render)→re-`审查 PASS`,起草+1 轮修复):新 `engine/us_short_price_clock.py`:`validate_price_clock(price_clock, *, machine_as_of, machine_session)` = fail-closed §21 一致性门:精确冻结字段集 + `session_scope==RTH` + 三日期严格真实 YYYYMMDD + 序 `price_data_through < decision_date`(前一已收盘日;落决策日及之后=陈旧/前向拒)+ `news_window ∈ [pdt,dd]`;给机器层 as_of/session 时交叉核对。**焊进官方输出**:`render_weekly_report` 在 banner ④ 非空白校后调 `validate_price_clock`、`PriceClockError→WeeklyReportRenderError`——官方 §11.2 路径不再渲 same-day/未来价/未来 news/非RTH 时钟(validator 是被强制的输出门、非旁置 helper)。机器层 as_of/session 交叉核显式延后给 pipeline(batch4 canonical resolver 越界);日期门内联(jsonschema-free)。无新 schema。renderer+price_clock target 37 + 全离线 `*us_short*` 1293 全绿零回归。Required `R-USSHORT-BATCH3-PRICE-CLOCK-VALIDATOR-BYPASS-GAP`(resolved@`718092ec`;教训:gate 必须焊在产出 OUTPUT 处=render,别只放 standalone validator——本会话第 4 次同类)。批3 续片 = R3(纸面 #8 / 比较 #13/#24)/ no-dangling 证据反查+registry(#9)(按 §18.2)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 hot_excluded §11.4 #19（误杀审计 detector + 防绕过 summary 桥）已提交**(`ed44b441`,Codex 起草→`审查 FAIL`(summary 桥可绕 detector gate/threshold)→修复(改吃 raw、内部跑 detect)→re-`审查 PASS`,起草+1 轮修复):新 `engine/us_short_hot_excluded.py`:`detect_hot_excluded(excluded_rows, *, heat_threshold)` 返回审计视图——被剔票 `theme_heat_score≥threshold` 且落 安全闸/流动性/数据 gate(`AUDIT_ELIGIBLE_GATES` 源自冻结 governance criteria)= §11.4 误杀候选、喂 §13;高热但落 hard veto/其它 gate **绝不进**(不救回 veto);返回 shallow-copy、不改入参(**audit-only 不改准入**)。`hot_excluded_summary(excluded_rows, *, heat_threshold)` = 官方桥:吃 RAW 行、**内部跑 detect(唯一路径、无 bypass 面)**→ `{public_heat_count(非持仓·脱敏 tracked), holdings(持仓·{ticker,reason}·私密)}`,喂 exclusion_summary `hot_excluded` + banner ⑤ **单源**(计数与明细不背离;直接塞 hard_veto/低热行被过滤、不计)。heat cutoff 入参(percentile→cutoff forward 不 pin)。无新 schema。target 20 + 全离线 `*us_short*` 1274 全绿零回归。Required `R-USSHORT-BATCH3-HOT-EXCLUDED-SUMMARY-BYPASS-GAP`(resolved@`ed44b441`;教训:官方输出二级函数别信入参、必复用主路径自校契约——本会话第 3 次同类)。批3 续片 = R3(纸面 #8 / 比较 #13/#24)/ price-clock #21 / no-dangling #9(按 §18.2)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 honest-banner ① observe split §11.2（observe_reason_type 真/假观察聚合,完成 #10）已提交**(`b9aafa22`,Codex 起草→`审查 PASS`(无 Required,起草一轮过):新 `engine/us_short_observe_split.py`:`aggregate_observe_split(observe_reason_types)` 把本周逐行 observe 原因聚合成 §11.2 banner ① 真/假拆分 → `{total, per_reason(全冻结类·零显), sizing_artifact_count}`;observe_reason_type 读**冻结 `us_short_action_table_contract` `design_locked_enums`**(冻结值单一来源;未知/非str 闭世界拒)。`sizing_artifact_count` = fake/sizing 数 = `cash_or_account_missing`(冻结 banner ref + §11.2 ① 唯一点名:有信号但没账户/没现金、非系统不看好;窄安全=不夸大"只是 sizing")。`validate_observe_split` = fail-closed 门(per_reason 键集==冻结、非负 int、total==sum、sizing==sizing类计数和);`render_observe_split` 渲非空白脱敏 banner ① 串。无新 schema。target 16 + 全离线 `*us_short*` 1254 全绿零回归。**#10(coverage_status 诚实 + exclusion_summary + observe_reason_type)三件全完成。** 批3 续片 = hot_excluded 富化(#19)/ R3(纸面/比较)(按 §18.2)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 coverage honesty §11.5（逐行覆盖分类器 + fail-closed 诚实门 + gap_tag 契约/worst-of 一致性）已提交**(`f2361d84`,Codex 起草→`审查 FAIL`(validator 弱:只校 full⇔empty、不解析 tag/不校 worst-of)→修复→re-`审查 PASS`,起草+1 轮修复):新 `engine/us_short_coverage_honesty.py`:`build_row_coverage(row_source, data_checks)` 逐行分类 → `{row_source, coverage_status, coverage_gap_tags}`,row_source/coverage_status enum 读**冻结 `us_short_action_table_contract` `design_locked_enums`**(单一来源;列序=严重度 full<partial<restricted<blocked)。行须报齐 §11.5 三 gating 类(analyst/sec_parse/event,status∈{ok,missing,restricted,blocked};缺/多类拒——没查全不能评分);`coverage_status`=worst-of(镜像 provider-health),`coverage_gap_tags` 列非-ok 类。**`validate_row_coverage` = fail-closed §11.5 诚实门**:① `full ⇔ 无 gap`(缺数据不写 clean、降级须命名 gap);② 每 gap_tag 契约 `<gating类>:<非ok-status>`(类唯一);③ `coverage_status` 须 == worst-of tag 严重度(共享 `_coverage_rank`,deriver/validator 同一严重度源)——拒任意/`ok`/未知/重复 tag 与严重度 under/over 报。无新 schema(符合冻结 enum)。target 25 + 全离线 `*us_short*` 1238 全绿零回归。Required `R-USSHORT-BATCH3-COVERAGE-HONESTY-GAP-TAG-VALIDATOR-GAP`(resolved@`f2361d84`;教训:派生器诚实≠校验器诚实,standalone 门须自校契约+一致性别只信 deriver)。批3 续片 = hot_excluded 富化(#19)/ observe_reason_type(#10 余)/ R3(纸面/比较)(按 §18.2)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 exclusion_summary §11.4（隐私拆分 builder + 脱敏公开汇总 + §11.2 节渲染 + 私密明细契约）已提交**(`ee7c687f`,Codex 起草→`审查 FAIL`(私密侧无 shape 门)→修复→re-`审查 FAIL`(混合键 raw TypeError)→修复(整类 3 出口)→re-`审查 PASS`,起草+2 轮修复):新 `engine/us_short_exclusion_summary.py`:`build_exclusion_summary` 按 §11.4 隐私拆分——**公开** = 8 冻结类(读冻结 `us_short_exclusion_summary_governance` preset 单一来源)public-universe 计数 + hot_excluded 热票计数(覆盖 Pass-1 资格 + Pass-2 审计闸),脱敏门 = `schemas/us_short_exclusion_summary_public.schema.json`(additionalProperties:false + 整数 only,镜像 lifecycle readiness、可 tracked 无 §18.0 guard);**私密** = 真实持仓被剔明细,单一契约 `_validate_private`(strict as_of + 精确冻结类集 + 各类 holdings 非空白 ticker str + hot 行非空白 ticker+reason)焊进 build 出口 + `write_exclusion_private`(§18.0 guard 后写前,镜像 store)。未知类拒(闭世界)、缺省类→0、计数非负 int 整类 fail-closed;混合类型 key 诊断全 3 出口 `sorted(map(str,...))` → sanctioned `ExclusionSummaryError`(非 raw TypeError;= lifecycle governance-edge 同类)。纯 SUMMARY 不做准入/否决 → 结构上无法救回 hard veto(§11.4 hot_excluded 仅审计、喂 §13)。`render_exclusion_section` 渲 §11.2 本周剔除摘要节(脱敏、非空白、8 类 + total + hot 审计)。target 36 + 全离线 `*us_short*` 1213 全绿零回归。Required `R-USSHORT-BATCH3-EXCLUSION-SUMMARY-PRIVATE-DETAIL-CONTRACT-GAP`(resolved@`ee7c687f`;教训:de-id 拆分私密侧也须 shape fail-closed;`sorted()` on 外部 dict 键必 `map(str,...)`、被点名 1 处时 grep 同模式全出口别留兄弟)。批3 续片 = hot_excluded 富化(#19)/ 覆盖诚实(#10/§11.5)/ R3(纸面/比较)(按 §18.2)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 weekly_report.md 渲染器首刀（§11.2 13 节骨架 + 诚实横幅 + 数量对账）已提交**(`0e55bb23`,Codex 起草→`审查 FAIL`(空白/空/负 surface 漏过)→修复→re-`审查 PASS`,起草+1 轮修复):新 `engine/us_short_weekly_report_renderer.py`:`render_weekly_report(report_data)` 从**冻结 `us_short_weekly_report_contract`**(批1)读 13 节集/序 + 5 诚实横幅 + price_clock 字段 + lifecycle-count 规则(**单一来源、零硬编码**),渲 §11.2 markdown。**三道 fail-closed 渲染不变式(整类非空白·非负)**:① **④ price_clock 恒显**(缺/不全/空白字段拒——读者必见用了哪些价/日期);② **§11.2 lifecycle 数 第1节(本周运行状态)==第12节(字段·模块生命周期提醒) 且非负**(= lifecycle **2c 末片·数量对账**;负数不能伪装匹配);③ 13 节每节**非空白内容**(空/空格/空list/blank-item/None 拒)。可选 ①②③⑤ 非空白才显。无新 schema(符合冻结 §11.2 契约,镜像 R2a)。renderer 18 + 全离线 `*us_short*` 1177 全绿零回归。Required `R-USSHORT-BATCH3-WEEKLY-REPORT-SURFACE-INVARIANT-GAP`(resolved@`0e55bb23`;教训:fail-closed 校「有内容」别只查 None/'',要查 strip 空白+空容器+负值整类)。批3 续片 = 各节 enrichment(exclusion_summary #10 / hot_excluded #19 / 覆盖诚实 #10)+ R3(纸面/比较)(按 §18.2)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 E+F 并轮（provider 健康检查离线 #3 + 边界回归 #12）已提交**(`d89e245e`,Codex 起草→`审查 FAIL`(scope 只 engine)→修复→re-`审查 FAIL`(漏 tda)→修复→re-`审查 PASS`,起草+2 轮修复):**E** 新 `engine/us_short_provider_health.py`:OFFLINE 分类器(零 live 探活/网络/provider import,live 探活=批5)——授权源(FMP+公开 SEC EDGAR,皆 critical)注入健康→§3.2 run-state(关键源 degraded→restricted / down·missing→blocked=**坏就不输出 clean**、overall worst-of);**§18.1 #3「绝不触达未授权源」结构性强制**——对任何非授权源(fmp_full_market/yfinance/Web·X/sec_parser/付费)的 status 直接 `ProviderHealthError` 拒、未授权源恒 disabled_unapproved 不参与 clean;畸形 fail-closed。**F** 新 `tests/test_us_short_boundary_regression.py`:#12 硬边界回归扫**整个 us_short 可执行面**(engine+runner,31 文件)——不交叉 A 股 / 不接券商·全手动(15 broker token 含 tda)/ ship-gate 不放松;检测 helper 扫源文本零副作用 + 正控证非 no-op·非误报。E+F 20 + 全离线 `*us_short*` 1159 全绿零回归;无新 schema。Required `R-USSHORT-BATCH3-EF-BOUNDARY-REGRESSION-SCOPE-GAP`(resolved@`d89e245e`;教训:边界守护扫整可执行面非只 engine、删 guard token 前先核实子串假设)。批3 续刀 = lifecycle 2c 末片 + weekly_report 渲染器簇(price_clock/exclusion_summary/hot_excluded/覆盖诚实)+ R3(纸面/比较)(按 §18.2)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 R2 lifecycle-eval slice 2c 续刀（readiness artifact 脱敏 tracked 汇总）已提交**(`0ad2d7af`,Codex 起草→`审查 FAIL`(as_of 只校 8 位 pattern、漏真实日历日)→修复→re-`审查 PASS`,起草+1 轮修复):新 `schemas/us_short_lifecycle_readiness.schema.json`(§11.6 **tracked 脱敏契约**:§13.1 项编号 + 聚合计数 ONLY,`additionalProperties:false` + 整数项字段**结构性保证无票名/$/表现可夹带**——与私密 lifecycle_register 相反)+ 新 `engine/us_short_lifecycle_readiness.py`:`build_lifecycle_readiness(register)`(拒 not-clean→evaluate raises)→脱敏 readiness;`write_lifecycle_readiness` 写前跑 **schema=脱敏门**;`_assert_readiness` 校 schema + 跨字段(due_count==len/id∈[1,total]/upgrade⊆due)+ **as_of strict-real-date**(复用 eval `_strict_yyyymmdd`——schema 8 位 pattern 拦不住 20260231);**无需 §18.0 私密 guard**(可证脱敏=§11.6 允许 tracked)。readiness 16 + 全离线 `*us_short*` 1139 + schema 472 全绿零回归。Required `R-USSHORT-BATCH3-R2-LIFECYCLE-READINESS-ASOF-REAL-DATE-GAP`(resolved@`0ad2d7af`;教训:tracked 脱敏靠 schema additionalProperties:false 结构保证、日期校验整类含 as_of)。批3 续刀 = lifecycle slice 2c 末片(周报 lifecycle 节/顶部横幅 + §11.2 数量对账,配 weekly_report 渲染器)+ E+F 并轮(provider 健康检查离线 #3 + 边界回归 #12)→ weekly_report 渲染器(按 §18.2)。**纯-ish/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 R2 lifecycle-eval slice 2c 首刀（GBK-safe 运行时横幅）已提交**(`198aff9b`,Codex 起草→`审查 FAIL`(横幅信任畸形 eval 输出 + GBK 漏)→修复→re-`审查 PASS`,起草+1 轮修复):新 `engine/us_short_lifecycle_render.py`:`lifecycle_banner(eval_result)` 把 `evaluate_lifecycle` 到期扫描渲染成**一行 ASCII(GBK-safe)运行时横幅**,露出本轮达复核线项数/编号/§12.2② 可升级项 +「upgrade 需用户决定、绝不自动生产」声明(**只露出、不触发**)。**渲染前 `_validated` 校全契约**(total 正 int / due_count==len(due_items) / id 唯一正 int∈[1,total] / upgrade⊆due / as_of 缺或 strict ASCII 日期)→ 任一违反成 `UNAVAILABLE`(非误导 0-due 横幅);**最终 `.encode('ascii')` 兜底**,畸形非-ASCII as_of/id 绝不漏到 GBK 控制台。模块 **jsonschema-free** + 集成测试懒导入 eval(缺则 skip)→ 横幅套件最小运行时可跑。无新 schema(纯投影)。render 13 + 全离线 `*us_short*` 1116 + schema 465 全绿零回归。Required `R-USSHORT-BATCH3-R2-LIFECYCLE-BANNER-FAILCLOSED-GBK-GAP`(resolved@`198aff9b`;教训:横幅入契约别只校被点名两腿、整类校 + 输出 ASCII 保证)。批3 续刀 = lifecycle slice 2c 续片 **readiness artifact**(脱敏 tracked 汇总,schema-first)+ 周报 lifecycle 节/顶部横幅 + 数量对账(配 weekly_report.md 渲染器,§11.2)→ weekly_report 渲染器(按 §18.2)。**纯/离线、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 R2 lifecycle-eval slice 2b（首个 lifecycle 持久化 + 私密路径 guard 写读对称 + 陈旧桶 fail-closed）已提交**(`bc74b36f`,Codex 起草→`审查 FAIL`(读侧 load 私密路径 guard 漏)→修复→re-`审查 PASS`,起草+1 轮修复):新 `engine/us_short_lifecycle_store.py`(IO 层,eval 纯层不碰 IO,镜像 R2a 渲染器分文件)——`write_lifecycle_register`(**首个 lifecycle 落盘者**)§18.0 P0 私密路径 guard 先于 validate/写(相对/仓内非 ignored 拒、gitignored 仓内 `state/us_short/lifecycle/`/仓外放行)+ 拒落盘 not-clean register;`load_lifecycle_register(*, expected_as_of)` **§18.0 guard 对称作读侧地板**(私密 artifact 只从 provably-private 源读、写读同抛 `PrivatePathError`)+ 重校验 + **陈旧/错位 fail-closed**(不可读/坏 JSON/not-clean,或持久 as_of 比决策日新→`StaleLifecycleArtifactError`,§2.1 桶名≠decision_date / §18.1 #20;同日幂等 + 前向放行)。无新 schema(符合 slice-1 register 契约)。store 16 + 全离线 `*us_short*` 1103 + schema 465 全绿零回归。Required `R-USSHORT-BATCH3-R2-LIFECYCLE-LOAD-PRIVATE-PATH-GUARD-GAP`(resolved@`bc74b36f`;教训:私密路径地板写+读两端对称,别只守写端)。批3 续刀 = lifecycle slice 2c(横幅文本 + readiness artifact + 周报 reconcile,消费 eval 输出 + 持久 register)→ weekly_report.md 渲染器(按 §18.2)。**纯 IO、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-23 **US-short 批3 R2 lifecycle-eval slice 2a（幂等 accumulate + dated forward_observations 台账 + governance 容器整类 fail-closed）已提交**(`5efb4f0a`,Codex 起草→`审查 FAIL`(accumulate 闭世界键 + 权威畸形)→修复→re-`审查 FAIL`×2(governance-edge 标量校准 + outer-container)→修复→re-`审查 PASS`,起草 + 3 轮 governance-edge 修复 + README 计数退役):register 演进为 **dated forward_observations 台账**(decision_date→contribution),`live_forward_count` 改 DERIVED(台账求和)——裸计数不可自授权(同 slice-1「可变态不可自授权」下移一层);**新 `accumulate_lifecycle_observation`**:per-item `forward_observations[decision_date]` **SET-not-ADD 幂等**(§2.1 重跑不重复计数)、纯(deepcopy 返回新)、clean-in→clean-out、closed-world update 键(未知键 raise)、weeks 类每 decision_date 贡献 0/1 反伪造。`validate`:**`_as_dict`/`_as_list` 入口一次性归一全 governance 容器**(结构性 fail-closed,非逐 call-site)+ 运行时 calibration/authority schema 校验 RAW 输入 + 不可哈希/混合键守 + 严格日期。README us_short 路由行**精确测试计数退役**(§18.1 #11「不在别处复述条数」)+ doc 守护 absence guard 防计数爬回。lifecycle 71 + 全离线 `*us_short*` 1087 + schema `*us_short*` 465 + doc/route 25 全绿零回归。Required 3 条 `R-USSHORT-BATCH3-R2-LIFECYCLE-{MALFORMED-INPUT-EDGES,GOVERNANCE-EDGE-FAILCLOSED-GAP,README-TEST-COUNT-DRIFT}`(全 resolved@`5efb4f0a`;教训:malformed 容器整类别逐腿补[3 轮 whack-a-mole]、入口归一一次关死;路由文档复述派生计数→退役 + absence guard,非手同步)。批3 续刀 = lifecycle slice 2b(运行时落盘:accumulate live-forward 持久化 + 接 §18.0 P0 私密 guard + 陈旧桶/as_of 错位 fail-closed)→ 2c(横幅/readiness/周报 reconcile)→ weekly_report.md 渲染器(按 §18.2)。**纯/离线、不落盘(persister 在 2b)、不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-22 **US-short 批3 R2 lifecycle-eval slice 1（lifecycle_register 契约 + 完整性校验器 + due 扫描 + §13.2 阈值权威）已提交**(`ceb79483`,Codex 起草→`review FAIL`(自授权阈值 + 畸形抛错)→修复→re-`审查 FAIL`(权威同形漂移)→修复→re-`审查 FAIL`(顶层描述漂移)→修复→re-`审查 PASS`,起草 + 4 轮修复):机读 §13.2 per-category 阈值(const)+ 整张 39 项 §13.1 item→category map(全 const-pin)的**阈值权威**(新 `schemas/us_short_lifecycle_threshold_authority.schema.json` + `presets/...20260622.json`)+ per-item 累加器台账(新 `schemas/us_short_lifecycle_register.schema.json`,`additionalProperties:false` 拒夹带阈值→可变台账不能自授权复核门槛)。`engine/us_short_lifecycle_eval.py`:`validate_lifecycle_register`(jsonschema 结构闸 + 覆盖保险[全部 §13.1 项、动态计数非硬编码 39]+ §13.1 title 交叉引用 + §13.2 七类成员 + **due==count≥权威min AND secondary 焊死不变式** + **运行时拿权威 schema 校验载入/注入的权威**[降阈值/同形重映射当场 `clean=False`]+ PIT as_of)+ `evaluate_lifecycle`(扫 due + reconcile + §12.2② `upgrade_eligible`;拒 not-clean;畸形 fail-closed 不抛)。**纯/离线、不落盘**(persister + §18.0 P0 私密路径 guard 随 slice 2 首个真落盘者接)。lifecycle 33 + 权威 schema 12 + 全离线 `*us_short*` 1049 + schema `*us_short*` 465 全绿零回归。Required 4 条 `R-USSHORT-BATCH3-R2-LIFECYCLE-{THRESHOLD-SELF-AUTHORING-BYPASS,MALFORMED-INPUT-RAISES,AUTHORITY-SAME-SHAPE-DRIFT-BYPASS,AUTHORITY-DESCRIPTION-DRIFT}`(全 resolved@`ceb79483`,详 register;教训:「reviewed 权威」只有被冻结 const-pin + 运行时强制才算数,同一缺陷逐层上移[register→权威→权威同形→描述]全堵死)。批3 续刀 = lifecycle slice 2(运行时阶段:accumulate live-forward + 横幅文本 + 周报 reconcile + 落盘[接私密 guard]+ 陈旧桶/as_of 错位 fail-closed)→ weekly_report.md 渲染器 → R3 证据子系统(按 §18.2)。**不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-22 **US-short 批3 R2a（action_table.csv 渲染器 + 首个落盘者接私密路径 guard）已提交**(`37188a55`,Codex 起草→review FAIL(validate=False 后门)→修复→re-`审查 PASS`):机器层记录 → §11.3 冻结 51 列 CSV(列序读冻结契约单一来源、省略列空格、list/bool 归一);**渲染前永远跑 §10 校验、not-clean 拒渲染、无 opt-out**(闸焊死、不留旁路);`write_action_table` 作**首个真落盘者**先接 §18.0 P0 fail-closed 私密路径 guard(相对/仓内非 gitignored 拒、仓外/ignored 放行)再 mkdir + 写。新 `engine/us_short_action_table_renderer.py` + 16 测。无新 schema(符合冻结契约)。**纯/离线、不交叉 A 股、无 provider/live。** 批3 续片:weekly_report.md 渲染器 → lifecycle-eval 阶段 → R3 证据子系统(按 §18.2)。
- 2026-06-22 **US-short 批3 cut1（§10 no-dangling/证据反查/字段 registry 校验器 + 机器层记录契约）已提交**(`5c399107`,Codex 多轮 review→re-`审查 PASS`,起草 + 4 轮修复):机器层 clean gate——正向不悬空 + 核心字段命中 impact_target(否则 shadow/drop)+ 反向证据反查(claim→provider row/SEC filing/source_id @PIT as_of)+ registry 完整 + run-level 真日期 PIT + action-table 冻结词表成员 + **任意** 硬否决覆盖 kill/exit 动作 + lifecycle_item_id §13.1 解析;**读冻结 field_registry/action_table/lifecycle preset(零硬编码词表)+ 跑机器层 schema 作 jsonschema 结构闸**;坏输入 fail-closed 不抛。新 `engine/us_short_no_dangling_validator.py` + `schemas/us_short_machine_record_contract.schema.json` + 92 测(75 validator + 17 schema:每条 §10 不变式 + 每个 bypass 类[vocab/PIT/必填字段/结构闸/lifecycle/任意硬否决]对抗 fixture + 正反向控)。**纯/离线不落盘**——§18.0 P0 私密路径 guard 随首个真落盘刀(批3 续刀 R2)接。Required 6 条 `R-USSHORT-BATCH3-*`(全 resolved@`5c399107`,详 register;4 轮 Codex review:vocab/PIT → 必填字段 → schema-gate 完整性 → README 计数)。批3 续刀 = R2 输出子系统 / R3 证据子系统(按 §18.2 并轮)。**不交叉 A 股、无 provider/live/DataHub。**
- 2026-06-20 **A-short 全系统自审 + Codex 桌面审查复核修复批已提交**(`d1857ef1`,Claude 6 段自审 `Desktop/a_short_review_cc.md` + Codex 桌面审查 `Desktop/a_short_review_codex.md` Slice1-6 逐条复核 + Codex re-`审查 PASS-with-P2`,起草+修复):双轮审查发现并修 **10 条真问题(3 P0 + 7 P1)**——P0:egs_main import `ts.set_token`→`ts.pro_api(TOKEN)`(消除 import 写 `~/tk.csv` 沙箱副作用)、weekly `.md` sibling + account converter 输出私密 guard(`--account` 真实持仓泄漏面 fail-closed)。P1:`filter_l0` 改 strict `is_a_share_main_board` inclusion(堵 B股 900/200 + 畸形码,原排除式前缀漏排)、`get_holder_reductions` 删未来 30 日第二段 stk_holdertrade(--as-of look-ahead)、M6.7 held-state 不变式 + 市场否决 held 例外(持仓遇 IV/收缩期降级非硬否决)、`_is_valid_date` 严格化(拒 `202606 5` 等宽松 strptime)、`validate_account_state` 主板校验、registry schema `operation_impact_target` 去 `both` + existing_holding⟹holding_row_impact/private 不变式。+ P2 hermetic(phase6 注入 dummy `TUSHARE_TOKEN`)。新增 `tests/phase6/test_egs_main_board_and_holder_pit.py` + 扩 4 个 a-short 测文件;a-short discover 1399 OK、phase6 66 OK、零回归。**全 research-only/advisory/comparison-only 边界内、无生产评分/真钱/选股/否决/v14.2 frozen spec 改动**。Required 10 条 `R-ASHORT-*`(全 resolved,full detail 见 `docs/system_risk_register.md` Hot Queue 单一来源)。**顺带 closeout**:A-long forward-paper 2 条 Required status `open`→`resolved`(code 早 @e5bd1902)。主板/V14.2 frozen。
- 2026-06-20 **A-long value-yield forward-paper 数据层 + 编排 brain 已建+提交 = forward-paper 工程 end-to-end COMPLETE**(`e5bd1902`,Codex 3 轮 re-`审查`(① list_status origin threading + pre-broad calendar guard → ② D-origin consumer 出口贯穿 + 空 universe + data_through/path 规范化 → PASS,起草+3 轮修复):兑现 `dcb33dd` 标的「capture live-fetch 数据层 = 后续单独 slice」——新 **lean top-500 gated 数据层** `runners/a_long_large_cap_value_yield_forward_paper_data_layer.py`(`fetch_forward_panel` 月末 PIT 抓数 call-plan 契约[explicit window + min-field + call 对账]+ `assemble_forward_inputs` 内存 PayloadStore/SignalContext 喂冻结 bf/base) + 接线进 **brain** `..._capture.py`(`build_forward_accumulator`/`run_forward_capture`/main)。**复用冻结 bf/base pure 函数不变、不改冻结 batch runner**。硬化:list_status origin 贯穿 context+scoring+delist lineage(D-origin 缺 delist_date fail-closed、已退市不入新篮子) · pre-broad 月末+entry-anchor 守门(非月末 as_of 1 次 trade_cal 即拒、不白拉 broad) · 空 universe / noncanonical data_through / 输出路径 traversal 全 fail-closed · 缺 SW 成员优雅排除(不 abort) · per-construction matured 计数校验 · frozen-entry PIT backfill 锚 + 实际 exit-policy lineage。Required `R-ALONG-VY-FP-DATALAYER-INDUSTRY-CLASSIFICATION-CONTRACT-GAP` + `R-ALONG-VY-FP-FORWARD-CAPTURE-EVIDENCE-INTEGRITY-GAP`(均 resolved)。全量 discover 2747 OK(零回归)。**paper-only / research-only out / gated(本提交不跑 live 抓数)**。**工程 COMPLETE → 进入月度捕获执行期**:首笔 `as_of=20260630`(`START_FLOOR`)、之后每月末收盘后 gated(TUSHARE_TOKEN + 逐笔授权);累积 ≥12 matured cohort(~1 年)→ paper-read 评估;首笔 = 真 provider 形状现写现验点。cadence 见 memory `project_a_long_vy_forward_paper_monthly_cadence`。主板 frozen。
- 2026-06-19 **A-long value-yield forward-paper capture slice 已提交**(`dcb33dd`,Codex 3 轮 re-`审查`(schema promotion-gate → 残留身份/覆盖/source-ref → frozen-horizon 元数据)→ PASS,起草+3 轮修复):兑现 reviewed prereg `a_long_large_cap_value_yield_forward_paper_tracking_20260609` 授权的独立 capture slice——前向 rolling-relative-NAV(冻结 `bf.rolling_relative_nav_drawdown` 逐字镜像 + checkpoints 参数化,parity-pinned)+ paper-read 路由(唯一 primary 合成 `value_yield_composite_cf_sales`;<12 matured cohort→insufficient;promote **不授权真钱**、仅可提新 reviewed real-money prereg)+ value-yield 等权合成 + accumulator 输出契约。**accumulator 契约硬化**:schema(`const`/`required`/`minItems-maxItems`/`if-then`/`propertyNames`/`uniqueItems`:promotion_role↔id、matured⟹非空、interim 集 const=[21,63,126,252]、每 construction 必含 504 horizon、每 block 恰 3 构造)+ `validate_accumulator_consistency`(draft-07 表达不了的跨字段/跨行:read↔routing promote/insufficient 门、primary==composite、no pre-start、cohort as_of 唯一、as_of_latest_capture==最新 cohort、3 构造身份唯一全覆盖、basket↔size、matured 计数==实际、source_refs 精确路径)。复用冻结 bf/base pure 函数,**不改冻结 batch runner**。Required `R-ALONG-VY-FP-ACCUMULATOR-SCHEMA-PROMOTION-GATE-GAP`(resolved)。全量 discover 2675 OK(+37 模块测,零回归)。**commit-safe 但 execution-gated**:`dcb33dd` 本身不抓数/不跑 capture/不花 singleton ledger/不碰真钱/非 production/不满足 ship-gate/不做 best-of-three;live month-end PIT 抓数仍需每笔单独授权,累积 ≥12 monthly cohort 后才有 paper-read。
- 2026-06-19 **A-short S3b R4b(跨周持久收紧 ratchet)已提交 = S3b 整线(R1+R2+R3+R4a+R4b)收官**(`9e5c10c`,Codex `审查 FAIL`(sidecar duplicate-key PIT bypass)→修复→`re-审查 FAIL`(null-stop finite-gate invariant gap)→修复→`re-审查 PASS`,起草+2 轮修复):新 **gitignored 私密 sidecar** `state/a_short/holding_ratchet/`(per-`(ts_code,entry_date)`),每周 load→单向只升不降 ratchet→写回——**ratcheted_stop = max(本周 max(S3a止损,R4a保本), 上周)** 作建议保护止损(只升不降;**不改 plan.stop/table.损**)、**disposition 跨周 severity-max**(只升档不降)、**滚动到价**(现价≥上周减仓价→reduce / ≤ratcheted_stop→clear)、**re-entry(新 entry_date)重置 + bootstrap + 同周 re-run 幂等 + PIT 拒未来态**。**全 advisory:不自动卖/不接券商/不改 EGS·TopN·选股·否决/操作 enum 不扩/主板;仅 --account 真持仓 run**。engine `_holding_ratchet`/`_ratchet_report_error`(单一来源:validate + _apply + engine write_m67 共用,**本周有效 stop 时 ratcheted_stop 必非空有限**)+ pipeline `load/_apply/save_holding_ratchet`(**dict 折叠前拒重复 key + load/save PIT envelope**,git check-ignore 私密守门)+ schema(新 sidecar + m67 machine.ratchet)+ render(`_active_alert_line`/`_ratchet_line`)+ registry 2 行 S3b 收官 + `.gitignore`。Required `R-ASHORT-S3B-R4B-RATCHET-{SIDECAR-DUPLICATE-PIT-BYPASS,INVARIANT-GUARD-GAP}`(均 resolved)。全量 discover 2638 OK(+37 R4b 测,零回归)。**边界:语义生产硬否决需单独 PIT-source review;外资单票不做(hk_hold 停发);4.2 转正治理见 `docs/a_short_gap_data_promotion_governance_20260619.md`**。主板/V14.2 frozen。
- 2026-06-19 **A-short S3b R4a(到价提示 price_cross + 移保本 move_to_breakeven within-week advisory)已提交**(`5e28156`,Codex `审查 PASS`(无新 Required),起草+0 修复):持仓行 within-week advisory——**到价提示** `machine.price_cross`(reduce_review & 现价≥减仓价[盈一 plan.t1]→reduce_price_reached / clear_review & 现价≤清仓价[损 plan.stop]→clear_price_reached / else none)+ **移保本** `machine.move_to_breakeven`(浮盈≥1R[**1R=成本价−S3a 系统止损 plan.stop**,仅 plan.stop<成本价 即 R>0]→ triggered + breakeven_price=成本价,建议移止损到成本价)+ **价格钟** `machine.current_close`(=inp.close,validator provenance bind 到 现价与成本)。**全 advisory:不改 disposition/操作 enum/不自动卖出/不改 plan.stop(table.损 仍=S3a 止损)· 仅持仓行 · 主板 · 与 S3a 损/盈一/盈二 + R3 减仓价/清仓价共存(引用同值)**。engine(`_holding_active_alerts` 单一来源[_apply 设值 + validate 独立重算]+ `_apply_holding_disposition` 末尾算 + build_m67/build_holding held 行注入 current_close + `validate_m67_consistency` 持有分支必带三字段·current_close provenance bind·独立重算比对,非持有按键存在拒三字段)+ schema(machine +current_close/price_cross/move_to_breakeven,move_to_breakeven additionalProperties:false)+ render(`_active_alert_line`)+ registry 2 行 R4a。**诚实点**:减仓价=plan.t1 恒>现价(S3a holding_levels 构造)→到价减仓周内基本不触发,跨周由 R4b 激活;到价清仓=S3a 破位;移保本仅 plan.stop<成本价(R>0)时触发。全量 discover 2601 OK(+24 R4a 测,零回归)。**R4b 已收官;边界:语义生产硬否决需单独 PIT-source review;外资单票不做(hk_hold 停发)**。主板/V14.2 frozen。
- 2026-06-19 **A-short S3b R3(减仓价/清仓价/减仓比例 advisory 价位,复用 S3a)已提交**(`8869e8d`,Codex `审查 FAIL`(显式 null/键存在性 guard)→`re-审查 PASS`,起草+1 轮修复):用户批准 R3 + 选定派生「复用 S3a 价位」。R1+R2 持仓处置 升级带具体 advisory 价位:**clear_review→清仓价=S3a 损(plan.stop)、reduce_review→减仓价=S3a 盈一(plan.t1)+ 减仓比例(固定档 1/3 advisory)**。**advisory 不自动下单 · 仅持仓行+仅 reduce/clear disposition · 操作 enum 不扩 · 不接券商 · comparison-only · 主板 · 与 S3a 损/盈一/盈二 两维共存(引用同值不重算)**;S3a 未算出/破位→价位**显式 null**(不伪造);主动到价动作/止损触发/移保本/跨周 ratchet=R4(单独批准)。schema(table 减仓价/清仓价/减仓比例 + machine reduce_price/clear_price/reduce_ratio,加性)+ engine(`_apply_holding_disposition` 扩派生[pop-then-set 幂等、复用 mc.plan stop/t1]+ `validate_m67_consistency` 持有分支按 disposition 焊死 **R3 键集**[clear→{清仓价}/reduce→{减仓价,减仓比例},缺键/多带含显式 null 即拒]+ 值独立比对 S3a plan + machine↔table + 非持有行**按键存在**拒 R3/R1R2 machine 价位[present-null 也算泄漏])+ render(`_disposition_line` 显价位 + 不自动下单 caveat)。registry 2 行 减仓价 R3 已实现·pending→R4;6 处「S3b 已承接减仓价/清仓价/到价提示/ratchet」stale 文本更新。Required `R-ASHORT-S3B-R3-EXPLICIT-NULL-PRICE-GUARD-GAP`(resolved)。全量 discover 2578 OK。**R4a/R4b 已收官;边界:语义生产硬否决需单独 PIT-source review;外资单票不做(hk_hold 停发)**。主板/V14.2 frozen。
- 2026-06-19 **A-short S3b R1+R2(持仓处置 结构化列 + severity 合并引擎 + 持仓效应 scope guard)已提交**(`6a7ce92`,Codex `审查 FAIL`(merge/validator 没焊持仓侧 scope)→`re-审查 FAIL`(round1 只焊 forward_event source-class、generic 仍漏)→`re-审查 PASS`,起草+2 轮修复):**用户已批准进入主动持仓管理**(§11.4/§3.5「主动持仓管理需明确批准」门)。为持仓行(操作=持有)加结构化 `持仓处置`(持有/持有警戒/建议减仓复核/建议清仓复核/立即人工复核)+ `禁止加仓` 列 + `machine.holding_management_signal/blocked_add_required`(均加性,候选行省略=向后兼容)。**操作 enum 不扩(决策1,持仓处置是独立列)· comparison-only advisory · 不自动卖出 · 仅持仓行 · 不接券商/不改 EGS·TopN·选股·股数·否决**;减仓价/清仓价=R3、主动动作+到价减仓+跨周 ratchet=R4(**各需单独批准**)。引擎:`_is_held_signal`(holding_row_impact/existing_holding/private,**= 持仓处置合并唯一合法输入·单一来源判据**)+ `_merge_holding_disposition`(severity-max=anti-rescue + blocked OR,仅持仓侧信号参与)+ `_apply_holding_disposition`(键于 操作=持有、幂等);build_m67/build_holding/pipeline attach 各调一次。**scope guard(R-ASHORT-S3B-HOLDING-DISPOSITION-SCOPE-GUARD-GAP,resolved)**:validate_m67 持有分支独立重算比对 + 非持有行拒 table/machine 持仓字段 + **全局 ⑧.5 持仓效应 shape 闭合**(任何带 holding_effect/blocked 的 impact 必 `_is_held_signal` 且 position_state==held,source-class 无关,补 ⑪⑬⑭ 缺口)+ forward_event ⑪ held/非held shape 绑定。与 S3a holding_levels(被动止损/止盈价位:损/盈一/盈二)是两个维度、共存不冲突。全量 discover 2564 OK。**R3/R4a/R4b 已收官;边界:语义生产硬否决需单独 PIT-source review;外资单票不做(hk_hold 2024-08 停发,memory)**。主板/V14.2 frozen。
- 2026-06-18 **A-short 4.2 财报质量趋势②③④⑤(financial_trends 框架 + 行业基本面)已提交**(`c00509d`,Codex `审查 FAIL`(provider 字段覆盖)→`再审查 FAIL`(coverage-key/realized-period-PIT/⑤ rollup·row-impact/PIT-filtered 多轮)→`re-审查 PASS`,连续起草 4 刀 + 多轮修复):「做全」财报质量+行业基本面剩余 4 刀,统一 **type-agnostic 财报报表框架**(镜像 forward_events,加第 N 类只扩 map+provider+red-flag fn+schema enum)。**comparison-only · candidate-only · 绝不 hard_veto/非生产/不改 EGS·TopN·选股·股数·否决 · 不新设阈值(决策4:tushare 自有分类+同期方向比较)· 真取数 gated --confirm · held 排除(持仓财报趋势是后续刀)**。② forecast(pro.forecast:type 负类/p_change_max<0)③ income(pro.income:归母净利<0 / 营收·毛利率·净利率 YoY 下滑)④ balancesheet(pro.balancesheet:资产负债率/应收占比/存货占比↑、商誉减值,全 YoY)⑤ 行业基本面(零新取数,聚合③④按 SW L2 行业,summary_only · candidate-scope · 无 operation_impact)。engine guard ⑯(financial_trend_ source-class candidate-only)+ ⑰(禁 industry_fundamentals 逐票 impact);unknown-not-clear 焊全(provider 缺分析列→fail-closed、非空但无 PIT-valid 评估基础→unchecked、真空[]→checked)、双向 no-dangling、(ts_code,statement_type)唯一覆盖键、income/bs realized-period PIT(forecast 业绩预告可指未来期豁免)。Required `R-ASHORT-GAP42-FINANCIAL-TRENDS-PIT-FILTERED-COVERAGE-UNKNOWN-GAP` + `R-ASHORT-GAP42-INDUSTRY-FUNDAMENTALS-SUMMARY-ONLY-ROW-IMPACT-GUARD-GAP`(均 resolved;再审查中 provider-field-coverage/coverage-key/realized-period 已并入这两条)。**「做全」5 刀全完成**(①财报质量复用 `8fadd7a` + ②③④⑤ `c00509d`)。financial_trends 真接口(pro.forecast/income/balancesheet)已 live 验证(2026-06-18 主板蓝筹 ×5:字段契约全一致、builder 拉通 status=checked、provider 无需改)、gated --confirm。**边界:S3b 已收官;持仓财报趋势不在当前 candidate-only 财报线;外资单票(hk_hold 2024-08 后停发→不做,memory);语义生产硬否决需单独 PIT-source review**。主板/V14.2 frozen。
- 2026-06-18 **A-short 4.2 财报质量①(复用 financial_quality)已提交**(`8fadd7a`,Codex `审查 FAIL`(guard ⑮ 没强制候选 only)→修复→re-`审查 PASS`,起草+1 轮修复):复用 egs_main 已取的 fina_indicator 派生(扣非净利/同比/ROE/经营现金流质量/ESP-Q 旗标),**零新取数**;候选行红旗(EGS 既有 ESP-Q 或 扣非净利同比<0)→ advisory priority_down operation_impact + 落 精简结论区.风控触发「财报质量对照」。**comparison-only:绝不 hard_veto/非生产/不改 EGS/TopN/选股/股数/否决**;red-flag 复用 EGS 既有判据+自然符号,**不新设阈值**(决策4);无红旗不发(避噪声)。normalize_candidate 透传 financial_quality;`_financial_quality_operation_impacts` + build_m67 候选(not has_position)接线;**guard ⑮ 焊死候选 only**(structured/非生产/veto none/绝不 hard_veto/holding none + candidate_row_impact/new_entry/public_tracked + held 报告拒 + 风控触发 marker no-dangling);registry +financial_quality 行。无新 schema/provider/render。Required `R-ASHORT-GAP42-ROUND5-FINANCIAL-QUALITY-CANDIDATE-SCOPE-GUARD-GAP`(resolved)。**用户「做全」财报质量+行业基本面 5 刀的第 1 刀**;决策5 已探明 forecast/income/balancesheet 全可得、外资 hk_hold 2024-08 后停发→不做(memory)。**后续链路已由财报质量趋势②③④⑤收官**。主板/V14.2 frozen。
- 2026-06-18 **A-short 4.2 Round5 大宗交易第三刀(折价率 discount)已提交**(`d98db25e`,Codex `审查 FAIL`(price 覆盖半成)→修复→re-`审查 FAIL`(builder 溯源漏)→修复→re-`审查 PASS`,起草+2 轮修复):折价率 discount=(price−当日**未复权** close)/close(负=折价/抛压,正=溢价),**comparison-only 不变**(不改 EGS/TopN/选股/股数/操作/否决,无阈值)。**单位口径**:block price 原值/未复权 → 独立 `_fetch_daily_close`(pro.daily raw close,gated --confirm)取当日未复权收盘,绝不复用前复权 price_series。`_attach_block_discount` 逐交易日 join close 算逐笔 discount + event.close + 顶层 discount_status/unchecked_discount_dates(unknown-not-clear,镜像席位覆盖);render 大宗表加「折价率(最大笔)」列 + caveat;attach 文本带最大笔折价率。**price 覆盖/溯源 no-dangling**(列→键→溯源 3 面焊全):① `_fetch_block_trade` fail-closed 要求 price 列;② validator checked 折价日每 party 必带 price+discount 键;③ builder 按 price 溯源建 party(仅源行带 price 键才落,缺键不补→validator 拒,绝不伪造 checked);行带 `price=None`(空白 cell)合法 vs 行无 price 键拒。Required `R-ASHORT-GAP42-ROUND5-BLOCK-TRADE-DISCOUNT-PRICE-COVERAGE-GUARD-GAP`(resolved)。**大宗交易线(成交事实/买卖方/折价率三刀)全齐**。**边界:外资单票不做(hk_hold 停发);财报质量与 S3b 当前可完成链路已收官;语义生产硬否决需单独 PIT-source review**。主板/V14.2 frozen。
- 2026-06-18 **A-short 4.2 Round5 大宗交易第二刀(买卖方营业部 parties)已提交**(`ece634c4`,Codex `审查 FAIL`(parties 覆盖/no-dangling 半成)→修复→re-`审查 PASS`,起草+1 轮修复):block_trade `buyer/seller`(与 amount 同一 fetch)→ `event.parties[{buyer,seller,amount}]` 逐笔;render 大宗表加「买卖方(最大笔)」列 + 板块资金事件文本带最大笔 买→卖。**parties 覆盖/no-dangling**:`_fetch_block_trade` fail-closed 要求 buyer/seller 列(缺→该日 unchecked,不伪造 ?→?);schema `parties` required + validator checked event⟹`len(parties)==trade_count`。comparison-only 不变(parties 是 event 富化,无新 operation_impact、guard ⑬⑭ 不动)。**买卖方未经 --confirm live 验证(fail-closed 兜底)**。Required `R-ASHORT-GAP42-ROUND5-BLOCK-TRADE-PARTIES-COVERAGE-GUARD-GAP`(resolved)。**折价率/财报质量/S3b 已由后续切片收官;边界:外资单票不做(hk_hold 停发);语义生产硬否决需单独 PIT-source review**。主板/V14.2 frozen。
- 2026-06-18 **A-short 4.2 Round5 大宗交易第一刀(block_trade)已提交**(`606bd225`,Codex `审查 FAIL`(trade-event 覆盖/持仓隐私 guard 半成)→修复→re-`审查 PASS`,起草+1 轮修复):候选+账户持仓近5交易日大宗成交事实+成交金额(amount 当日合计+笔数)落 板块资金事件「大宗交易对照」+ operation_impact(block_trade_appearance,**comparison-only**),镜像龙虎榜全套(provider `_fetch_block_trade` gated --confirm · 按(票,日)聚合 · 双向 no-dangling · 持仓 holding/private · Tier-3 掩面放行 · registry 候选/持仓拆行)。**trade-event 写时 guard 补强**(block_trade + sibling dragon_list 同类):① 覆盖闭合(checked 须 ≥1 实际查成日,否则 unknown);② ⑬⑭ 合并单一 guard + held↔shape 不变式(held⟹holding/private、非held⟹candidate/public,运行时强制)。**大宗真取数已联网验证**(本轮真跑 block_trade 拉通、字段契约一致,provider 无需改)。Required `R-ASHORT-GAP42-ROUND5-TRADE-EVENT-COVERAGE-PRIVACY-GUARD-GAP`(resolved)。**大宗 parties/折价率、财报质量、S3b 已由后续切片收官;边界:外资单票不做(hk_hold 停发);语义生产硬否决需单独 PIT-source review;render 空结果文案「候选近N日」可后续微调为「候选+持仓」(非阻断)**。主板/V14.2 frozen。
- 2026-06-18 **A-short 4.2 Round5 龙虎榜第三刀(非候选持仓纳入)已提交**(`af8ff67e`,Codex `审查 FAIL`(registry both 混写)→修复→re-`审查 PASS`,起草+1 轮修复):账户持仓纳入龙虎榜/席位对照(第一二刀只候选)——dragon universe 从候选扩到 `weekly.reports`(候选 ∪ 账户持仓);`_attach` held 分支产 `holding_row_impact`(comparison-only/private_account,无任何持仓动作);`_card_field` 放行 Tier-3(account_position_only)的 板块资金事件「龙虎榜对照」(独立真取数、非 EGS 维度)。**registry 隐私治理**:`dragon_list_appearance` + `a_short_semantic_risk` 各拆候选行(new_entry/public/candidate)+ 持仓行(existing_holding/private/holding)+ 结构化 guard(禁 `operation_impact_target=both`、existing_holding 须 private+holding);semantic 候选 stale 一并修(Round3 done→implemented)。**龙虎榜真取数已联网验证**(本轮真跑 top_list/top_inst 拉通、字段契约一致,provider 无需改)。Required `R-ASHORT-GAP42-ROUND5-DRAGON-HOLDING-REGISTRY-PRIVACY-GAP`(resolved)。**大宗交易与 S3b 已由后续切片收官;边界:语义生产硬否决需单独 PIT-source review;holdings_manual_review 龙虎榜留后续**。主板/V14.2 frozen。
- 2026-06-18 **A-short 4.2 Round5 龙虎榜第二刀(席位分析 top_inst)已提交**(`842e016e`,Codex `审查 FAIL`(席位覆盖单向)→修复→re-`审查 PASS`,起草+1 轮修复):席位分析 enrich 第一刀 `dragon_list` event(**comparison-only 不变**,无新 operation_impact、guard ⑬ 不动)——`top_inst` 逐席位 `exalter/side/net_buy` + 机构净买 `inst_net_buy`(『机构专用』席位合计);候选 only。provider `_fetch_dragon_inst`(`top_inst`,fail-closed,gated --confirm)+ `_sum_inst_net` + `_attach_seats`(按(票,日)join,独立 unknown-not-clear:无 inst_provider 不加层/全失败 unknown/部分 `unchecked_seat_dates`)+ main 接 `dragon_list_inst_provider`。weekly schema 加性 `seats_status/unchecked_seat_dates/event.seats/inst_net_buy`;`validate_weekly_report` 席位覆盖**双向闭合**(seats_status ⟺ 逐 event seats/inst_net_buy);render 席位列+未核查;registry owner_ref。Required `R-ASHORT-GAP42-ROUND5-DRAGON-SEATS-COVERAGE-GUARD-GAP`(resolved)。**后续链路已由龙虎榜第三刀与大宗交易收官**。主板/V14.2 frozen。
- 2026-06-18 **A-short 4.2 Round5 龙虎榜第一刀 已提交**(`65a97ea`,Codex re-`审查 PASS`,起草+2 轮修复):§5.3/§11.5 决策5 优先级1,**analysis-only · comparison-only**——候选近5交易日上榜事实+净买卖落 `精简结论区.板块资金事件`(marker「龙虎榜对照」)+ `machine.operation_impact`(`dragon_list_appearance`)+ 周报全局 `dragon_list` 段;**绝不改 EGS/TopN/选股/股数/操作/否决**(engine guard ⑬ 比 forward_event 更严:`new_entry_effect∈{informational,none}`、`holding_effect=none`、`blocked_add=false`)。provider `_fetch_dragon_list`(`top_list`,fail-closed,真取数 gated --confirm·HTTP 已验可得·in-pipeline live 待 --confirm·mock 测)+ `_recent_trading_days`(trade_cal 近5交易日)+ builder(PIT `trade_date<=as_of`、unknown-not-clear、`unchecked_dates`、只收候选)+ `validate_weekly_report` 双向 no-dangling(forward landing + 反向 evidence guard)+ render 龙虎榜段。weekly schema 加性 `dragon_list` 字段(m67_report schema 零改);registry + `dragon_list_appearance` 行(`needs_new_provider_call=true` + 类级 guard 防 `top_list/top_inst/block_trade` 误标 false)。Required `R-ASHORT-GAP42-ROUND5-{DRAGON-LIST-REGISTRY-PROVIDER-FLAG,SESSION-LOG-MINIMAL-TEMPLATE}-GAP`(均 resolved)。**后续链路已由席位分析/非候选持仓龙虎榜/大宗交易收官**。主板/V14.2 frozen。
- 2026-06-18 **tushare 健康检查探针 + 实测确认 weekly 取数正常 已提交**(`695b862`,Codex `审查 PASS`,多轮修复):新 `runners/tushare_health_probe.py`(只读诊断:复用 weekly pinned `init_tushare_pro`、HTTPS-only 绝不明文 token、3 接口都健康才 OK、`CRITICAL-API-BROKEN` 区分特定接口坏 vs pin/网络)。**实测真跑 [OK]**:weekly pinned `trade_cal`/`share_float`(339)/`disclosure_date`(1) 全健康 → **确认 weekly 真取数正常**(价格/解禁/财报,含第2刀 `pro.disclosure_date` 实接口字段 ann_date/pre_date 已验),**无需升级 tushare**。tushare 1.4.29 默认 url(waditu/503)broken,但 `init_tushare_pro`→`_pin_tushare_base_url` 早 pin 到 `https://api.tushare.pro/dataapi` 绕过;此前一度误报"broken"系旧探针用 raw `ts.pro_api`(未 pin)所致(已纠正,无需升级)。Required `R-TUSHARE-HEALTH-PROBE-PLAINTEXT-TOKEN-AND-FALSE-OK-GAP`(resolved)。**下一步:Round5 真缺口(龙虎榜→大宗)**。主板/V14.2 frozen。
- 2026-06-18 **A-short 4.2 forward_events 第2刀 — 多事件框架泛化 + 财报预约披露(earnings_disclosure)已提交**(`b46323d`,Codex `审查 FAIL`→修复→re-`审查 PASS`,起草+1 轮修复):forward_events 框架泛化支持多 event_type,加第2类财报预约披露。**builder 多 provider**(unlock + earnings,per-(票,类));`_fetch_earnings_schedule`(`pro.disclosure_date` `pre_date`,fail-closed,**真取数 gated --confirm,仓库未测真接口**);per-type 元数据集中 4 个 map(加第N类只扩 map+provider+schema enum,核心 type-agnostic)。`_attach` per-(票,类) impact(`source_field=forward_event_{type}`)+ 文本/操作建议 per-code 汇总一次。**双向 no-dangling**:正向(每 checked event 必落 impact)+ **反向 evidence guard**(每 `forward_event_` impact 必有匹配 checked event:suffix∈允许枚举/同 ts_code+type/`evidence_ref` 对齐——堵伪造/空日历/未支持类型);engine ⑪⑫ guard 改 `forward_event_` 前缀(覆盖新类不漏);`unchecked_codes` 带 event_type。analysis-only 不变(production_effect_enabled=false/不 hard_veto·rescue/不改 EGS·TopN·选股·result)。Required `R-ASHORT-GAP42-FORWARD-EVENTS-IMPACT-EVIDENCE-GUARD-GAP`(resolved)。**后续链路已由 S3b 与 Round5 收官;第2刀真接口 `pro.disclosure_date` 验证待 --confirm**。主板/V14.2 frozen。
- 2026-06-17 **A-short 4.2 forward_events 第1刀 — 未来已知事件日历(限售解禁)+逐票三面落地+analysis-only 守护 已提交**(`cceb3dc4`,Codex full+4 轮 re-`审查 PASS`,起草+5 轮修复):§4.4 第一刀,**只做限售解禁 limit_unlock / production_effect_enabled=false / 不改 EGS·TopN·选股·result**。周报全局 `upcoming_events`(PIT `observed_at<=as_of<=event_date<=as_of+21d`;**unknown-not-clear**:provider 不可用→`unknown_or_unavailable`、**部分票失败→`unchecked_codes`** 不静默当无事件)+ **逐票三面落地**(候选/持仓):每 checked event 按 ts_code 落对应 report `operation_impact`(候选 `candidate_row_impact`/持仓 `holding_row_impact`)+ `风控触发` + **`操作建议`**(用户主看面;候选→人工复核/谨慎建仓、持仓→持有观察/谨慎加仓);`holdings_manual_review` 旁路持仓落 `reason`;**`validate_weekly_report` 写时强制每 checked event 必落地**(不靠 main 顺序)。守护:engine `validate_operation_impact_no_dangling` **⑪ forward_event source-isolation**(永久非生产/veto none/非 hard_veto/holding∈{none,hold_watch})+ **⑫ advice-landing**(操作建议必含未来事件提示)。Required `R-ASHORT-GAP42-FORWARD-EVENTS-{ROW-LANDING-GUARD,PARTIAL-UNKNOWN,SOURCE-GUARD,ADVICE-LANDING}-GAP`(全 resolved)。**后续链路已由 S3b 与 Round5 收官;第2刀财报预约披露需 --confirm 验 `pro.disclosure_date`、指数纳入 defer、股东大会 drop**。主板/V14.2 frozen。
- 2026-06-17 **A-short 4.2 S2 — 持仓 semantic 数据接入 + holding_row_impact emit 已提交**(`2026ae6`,Codex `审查 PASS`,起草+3 轮修复):让持仓也抓 cninfo/web 语义(零新取数 / 真 fetch gated --confirm / 非生产 / 主板)。**semantic scope 依 `position_state`(不依哪个 builder)**:持仓(任何 Tier,build_m67/build_holding)official 证据齐全 high→`holding_row_impact` clear_review、web→hold_watch(blocked_add、m67_advisory_veto、`production_effect_enabled=false`、private_account、**操作恒「持有」不否决**、S3b 已承接减仓价/清仓价/到价提示/ratchet);候选行不变(official→否决/web→priority_down);production hard veto(ST/停牌/减持)持仓仍否决(只动 semantic)。provider 加 cap(持仓全覆盖绕候选 Top15;cninfo 因 `build_summary_from_fetches` 内部二次 Top15 按 15 分批合并);`_build_holdings`/main 接持仓 provider。render:`_has_semantic`(trace 非全 unknown)+ engine `sem_checked` 同一判据(unknown 不写「已核查」)、已跑语义显 S2 状态、无语义仍 S1「未核查」。私密:持仓 semantic→private_account + 带 `--account` 自动 weekly_private。Required `R-ASHORT-GAP42-S2-HOLDING-SEMANTIC-TOPN-RENDER-DRIFT`(resolved)。**后续链路已由 S3b 与 Round5 收官;转正治理见 `docs/a_short_gap_data_promotion_governance_20260619.md`**。主板/V14.2 frozen。
- 2026-06-17 **A-short 4.2 第3轮 — semantic 复用 → advisory operation_impact + semantic-isolation guard 已提交**(`224ab22`,Codex `审查 PASS`,含 Claude 接管 Codex 越界修复):4.2 第三刀,**零新取数 / schema 零改 / 非生产**。§11.3:复用现有 `a_short_semantic_risk`(不另造第二套语义层),把语义证据统一成 advisory `operation_impact`——helper `_semantic_operation_impacts`(候选/持仓共用):official 证据齐全 high→`m67_advisory_veto`(仍落 操作=否决,标「非生产 advisory」与生产硬否决物理区分)、web/LLM downgrade→`priority_down`(`veto_class=none`,绝不 hard_veto),全程 `production_effect_enabled=False`。`build_m67_report` 候选行(not has_position)发;持仓 `holding_row_impact` 能力+guard 就位(构造输入测试),**持仓 semantic 数据接入已由 S2 承接**(涉真实持仓私密路由,单独审)。guard 扩+整合单一 **semantic-isolation block**(source-class 级:`field_class==semantic_advisory` 或 `source_field` `semantic_` 前缀 ⟹ 非生产∧非 production_hard_veto;official_high⟹m67_advisory_veto;web_llm⟹none∧非hard_veto;防改 veto_class/丢分类绕过)+ ⑦/⑨/⑩ + weekly visibility exclusivity。Required `R-ASHORT-GAP42-ROUND3-SEMANTIC-ADVISORY-PRODUCTION-GUARD-GAP`(resolved)。**后续链路已由 S3b/Round5 收官;转正治理见 `docs/a_short_gap_data_promotion_governance_20260619.md`**。主板/V14.2 frozen。
- 2026-06-17 **A-short 4.2 第2轮 — exclusion_summary 上游过滤批次级摘要 + evidence_ref lineage 绑定 已提交**(`2d2ac28`,Codex `审查 PASS`,3 轮修复):4.2(缺口数据接入)第二刀,**零新取数 / 不改 egs_main / 非生产**。决策3:上游(L0/L2/Stage3)过滤、无 M6.7 个股行的批次级风险落**周报全局加性可选字段 `exclusion_summary`**(复用 `analysis_input.universe_summary.excluded_counts`,egs_main `filter_l0` 已记)——`as_of/total_excluded/by_reason[source_field·stage·veto_class·count·pit_basis·production_effect_enabled·privacy_class]/m67_text/evidence_ref`。`_build_exclusion_summary` 4 个 L0 键→by_reason(counts-only→`public_tracked`、total==0→None、**count>0 未映射键 fail-closed raise** 绝不静默丢)+ render「本轮上游过滤摘要」区 + `validate_weekly_report` guard(total==Σ / 零计数不入 / **evidence_ref 真绑受审 lineage**:kind 仅 `lineage_key`、value 精确 == 受审 dotted path、as_of==报告日期、`run_lineage.analysis_input` 非空)。`artifact_path` 在真实可解析 artifact 实现前从 schema 移除(4.2.md §6.2/§10.2)。隐私:仅计数不含个股/持仓→`public_tracked`(运行级私密路由仍按 `--account`)。Required `R-ASHORT-GAP42-ROUND2-EXCLUSION-SUMMARY-CONTRACT-GAP`(resolved)。**后续链路已由 Round3/S3b/Round5 收官;转正治理见 `docs/a_short_gap_data_promotion_governance_20260619.md`**。主板/V14.2 frozen。
- 2026-06-17 **A-short 4.2 第1轮 — 缺口数据字段清单 + reduce_deduct operation_impact + no-dangling guard 已提交**(`c5b2ce81`,Codex `审查 PASS`,2 轮修复):4.2(缺口数据接入)第一刀,**零新取数 / 零生产行为改动**。新增字段清单 schema(每字段声明 owner/PIT/隐私/**当前 M6.7 落点**/**最终结构化落点**;no-dangling 焊进 schema:非 out_of_scope 必有 terminal、非 implemented 必挂后继 slice)+ governance(决策4:只 merge 铁律,未接入数据不预写空阈值)。`build_m67_report` 据 `event_risk.holder_reduction.active_plan` 发**非持仓候选行** production_hard_veto operation_impact 进 `machine.operation_impact`(仅命中加 key,向后兼容;既有 hard_veto→否决 动作派生零改)+ `validate_operation_impact_no_dangling`(落点/最终落点非空·逐票形态·evidence_ref 可解析+as_of 绑报告日期·非implemented挂后继slice·advisory不冒充生产·production_hard_veto⟹否决)接进 `validate_m67_consistency`。Required `R-ASHORT-GAP42-ROUND1-{HOLDING-SCOPE-DRIFT,EVIDENCE-REF-GUARD-GAP,VISIBILITY-SHAPE-GUARD-GAP}`(resolved)。桌面设计 `4.2.md`(§0.5=5 决策)。**后续链路已由 Round2/Round3/S3b/Round5 收官;转正治理见 `docs/a_short_gap_data_promotion_governance_20260619.md`**。非生产/主板/V14.2 frozen。
- 2026-06-16 **A-short 持仓恒列入 S3a — 系统算持仓止损/止盈(被动)+ account_state v1.1 已提交**(`d79174e`,Codex re-`审查 PASS`):= 用户 §14 核心(系统算决策、用户只填事实)。引擎新 `holding_levels`:**跟踪止损(ratchet)= 近20日高 − ATR×倍数**、止盈复用 exit_and_size、side-aware tick、破位/缺数据不伪造、不算 entry/shares;`build_m67_report`/`build_holding_report` 持仓行接入(action 仍「持有」、被动显示系统位、入/股数 null);`validate_m67_consistency` 加「持有」分支 + **state-bound advice 护栏**。account_state **v1.1**(`enum[1.0.0,1.1.0]` + draft-07 if/then:旧 1.0.0 严格可加载、1.1.0 stop 可选手填参考);**转换器 stop 留空合法 → 用户不再手填止损**。render 持仓显系统位。系统级永久。**不做主动减仓/加仓(S3b)、不动「禁止自动加仓」、不改 egs_main/选股/建仓·观察·否决**。Required `R-ASHORT-S3A-HOLDING-LEVELS-MISSING-MANUAL-STOP-ADVICE`/`R-ASHORT-S3A-ACCOUNT-STATE-V11-ROUTE-DOC-DRIFT`(resolved)。设计 `docs/a_short_holdings_s3_system_levels_design.md`。
- 2026-06-16 **A-short 账户周报隐私输出路由固化 — 已提交**(`155e3ad`,Codex re-`审查 PASS`):带 `--account` 的 M6.7 周报含真实持仓(代码/成本/股数/止损),之前写死在 **git 追踪** 的 `research/results/a_short/<as_of>/` → 泄漏面。固化:① `.gitignore` 加 `state/*/weekly_private/`(scheme 覆盖 a_short/a_long/us_short/us_long);② `weekly_screening.ps1` 带 `-Account` → 输出落 gitignored `state/a_short/weekly_private/<as_of>/`,observation-only(无持仓)仍走标准 research lane(可留证据);③ pipeline 硬护栏 `_reject_nonprivate_account_output_path` 用 **`git check-ignore` 真值**判定(仓库内 且 git 未忽略 → fail-fast `SystemExit`;仓库外/已忽略放行;`--allow-nonprivate-account-out` 显式放行)—— 直接调 pipeline 绕过脚本也拦得住。不动 egs_main/选股/EGS分/引擎/语义/observation-only 路由。Required `R-ASHORT-HOLDINGS-WEEKLY-ACCOUNT-OUTPUT-PRIVATE-ROUTING`(resolved)。
- 2026-06-16 **A-short 持仓恒列入 M6.7 — S1 已提交**(`3e81200`,Codex `审查 PASS`,3 Required 全闭):账户持仓**无论是否进本周 EGS top-N 都恒进周报 M6.7**,三档 Tier 路由——top-N(复用 candidate)/ egs_full 复用(Tier-2,`engine/a_short_egs_full_adapter.py`,**只读 egs_full、不改 egs_main**)/ 粗筛未覆盖(Tier-3,`build_holding_report` **不跑 EGS 风险分类、不在缺失数据上伪造 veto/clean**)。**所有注入持仓 `coverage_status=partial`**(S1 不跑持仓语义,`full` 仅 top-N 候选)、**现价取价格钟**(非 egs_full 快照)、render 分区(候选/账户持仓)+ **每只持仓显式标「语义/新闻未核查(S1)」** + Tier-3 标 EGS 未覆盖 + 4.3-D 警告渲染;无价/停牌/价格陈旧 → `holdings_manual_review`、不伪造持有、不炸整轮。既有 `build_m67_report` 零改 → 候选/top-N 行为不变。设计 `docs/a_short_holdings_in_m67_design.md`。**不动 egs_main/选股/user-stop/主动止盈止损**;**S2(语义/新闻扩到持仓)/ S3(系统算阈值+主动持仓管理+schema v1.1)为后续**。
- 2026-06-15 **A-short 4.3 手工持仓表格 → account_state 转换器(Slice 4.3-A+B)已提交**(`e3b54f1e`,Codex `审查 PASS`,Required none):用户维护本地 CSV(account/positions/trades/manual_controls/portfolio_rule12)→ 转换器产 `a_short_account_state.schema.json`(4.3 时 v1.0.0;**S3a 已升 v1.1.0:`stop_loss` 可选、向后兼容 1.0.0**)+ 落盘过既有 `validate_account_state`,供周报 M6.7 现有 `--account` 消费。转换器=唯一自动推进层(Rule13 过期→pending_recheck、仅人工双确认→cleared;Rule12 过期→recovery_1 绝不→inactive)、validator 兜底;provenance 落 lineage 旁产物不进契约;CSV canonical 挡 Excel 强转;主板 only;`manual_allow` 禁。非生产 / 不接券商 / 不抓行情 / 不改 M6.7 行为。owner+设计见 `docs/a_short_account_state_manual_tables_4_3.md`。**4.3-C(M6.7 一览表「持仓/冷静」多标签列 + 逐票说明,纯渲染派生自 `machine.stateful_risk`、零引擎/schema 改动;重叠 Rule12+Rule13 并列不隐藏)已提交**(`6d3e30d`,Codex `审查 PASS`);**4.3-D(trades↔positions 一致性对账,advisory WARN-only、落 lineage `consistency_warnings`、绝不覆盖 positions)已提交**(`b10410d`,Codex `审查 PASS`,Required none)。**4.3 整条线(A/B/C/D)全部完成**;真实持仓 CSV 在 gitignored `state/a_short/account_state_csv/`(空模板已搭好,私密不入库)。
- 2026-06-15 **A-short V14.3 regime 这条线全做完并提交**(原 §5 P0-1「切片2」+ 性能优化 + 接 weekly + 初始 bootstrap):① compute 整 panel 向量化(`9d170818`,单次 104s→~2s/54x、逐字段 bit-identical,frozen-reference 对比测试 + PIT future-row cap 钉);② M6.7 周报价格时钟显式化 `--price-freshness-mode` + `run_lineage.price_freshness` 可审 lineage + `.md` 价格时钟 + 混合时钟 fail-closed(`8ad531c`,Claude 自审 PASS+用户授权、Codex 未独立审);③ regime 接进 `weekly_screening.ps1` Stage 5(`e478969`,Codex `审查 PASS`:旁路 sidecar、只实盘当天跑、非阻断、无 ledger→`--bootstrap`/有→increment、复用 IV、`-SkipRegime`;runner `_latest_settled_as_of` 收敛 as_of 到最新已结算交易日);④ 初始 regime ledger bootstrap 完成并提交(`e467f21`:252 行、coverage `20250530..20260612`、IV 223/252)。**现状=comparison-only 累积态**:以后一键跑实盘自动 increment,攒 ~12 周触发设计的升级审查提示。`v14_2=unknown` 是设计预期(生产未真算 V14.2 M1,设计文档第51行),**非 gap**;V14.2-sourcing 微接线已 defer(memory `regime-v14_2-sourcing-deferred`)。weekly cadence 改「**周一(决策日)收盘前** `-AsOf <周一>`」(memory `a-short-weekly-run-cadence` + ps1 头);**收盘后跑选股会变周一池**(今天踩过)。
- 2026-06-09 the A-long large-cap pre-registered **BATCH + BH-FDR** multi-factor round (9 factors + family-equal-weight composite over the reviewed top-500 circ_mv universe + full-main-board PIT fundamentals; runner committed `50d94e3` after Codex re-`审查` PASS) was EXECUTED → **`batch_statistical_alpha_clue_research_only`** — **NOT dry: 2 research-only statistical-alpha clues, 0 tradeable**. `cash_flow_to_circ_mv` (free-float operating-cash-flow yield, HAC t `5.61`, p≈0) and `sales_to_circ_mv` (free-float sales yield, HAC t `3.99`, p `0.0001`) survive BH-FDR at **both** q=0.10 and q=0.05 with all robustness gates (both median sub-period halves positive, name-conc `0.012`, single-year-conc `0.289`/`0.326` ≤0.35, 56 cohorts ≥48); per-factor + FDR-audit table in `docs/SESSION_LOG.md` top + `research/results/a_long_large_cap_batch_factor_search_20260609/execution_summary.json`. Neither is a tradeable candidate (rolling relative-NAV max drawdown `-0.1805`/`-0.1710`, worse than the `-0.15` gate). `low_accruals` (t `3.28`) / `low_asset_growth` (t `2.09`) survive FDR but FAIL the single-year-concentration guard (`0.360`/`0.379` > `0.35`); `roa_ttm` (t `-0.81`) and `momentum_12_1` (t `-1.21`) are negative, `book_to_circ_mv` (t `1.19`) / `low_beta` / `low_max` / composite weak — i.e. only the free-float **cash-flow & sales yields** carry the A-large-cap value premium, classic book/market does not, and the profitability / low-risk / momentum families re-confirm the prior falsified quality / low-vol lines. Per-factor size-coverage gate fired (15 months excluded per fundamental factor → 56 primary cohorts); 76,072 return rows, 48 cells. Singleton ledger now SPENT (`tests_spent_count=1`, `active_no_new_test_authorized`, `spent_passed_research_continue_only`). This was the agreed **LAST structured candidate-generation round**; NOT dry → the stopping-rule downgrade does NOT trigger. The 2 clues are research-only (in-sample 2018-2025; the program-level **6th+** test — BH-FDR cannot uncount the 5 prior spent singletons) and route to **forward-live validation**, NOT production / ship-gate / full-size; NO rerun / factor-def / q / threshold / window / composite-weight change / drop-losers-re-FDR / diagnostic rescue without a NEW reviewed prereg+ledger or forward-live.
- 2026-06-08 the next A-long large-cap candidate — externally-literature-motivated single-factor `ep_value` (earnings-to-price: trailing-twelve-month net income attributable to parent / PIT circ_mv, the CH-3 China value factor that subsumes book-to-market in large/non-shell names) — was `起草` (drafted), Codex-`审查` PASSED, and committed as a design slice: `research/preregistrations/a_long_large_cap_ep_value_20260608.json` (review-passed) + bespoke schema + singleton ledger + 15/15 schema tests. It mirrors the cash_conversion (fundamental + restatement) and low_volatility (externally-motivated) design-slice patterns: reuses the reviewed full-main-board PIT income + the 1,504-group restatement exclusion list + the top-500 circ_mv universe (no new fetch), marginal 0.5 industry + 0.5 size neutral, 504d/CSI300 primary, two-tier verdict + the same frozen `-15%` relative-NAV drawdown gate; non-positive TTM earnings are excluded from EP scoring (the D(EP<0) separation). Provenance: externally-motivated (cleaner than cash_conversion), NOT in-sample-diagnostic-derived, a distinct value-factor family NOT covered by the full-main-board 4 families — but it is the **5th** program-level large-cap test, so multiple-testing risk is explicitly flagged and forward-live remains required; the circ_mv denominator is an earnings-to-free-float yield (no-new-fetch caveat). The signal-search RUNNER slice was committed (`d6bedfd`, after Codex re-`审查` PASS resolving `R-EP-SUMMARY-SCHEMA-INVARIANTS`) and EXECUTED → **`falsified_large_cap_ep_value_under_frozen_rules`** (NOT a clue, NOT tradeable): the primary 504d/CSI300 industry-size-neutral cell had positive mean net excess (`+0.1084`, 56 cohorts, 42/56 positive months, HAC t `2.17`≥2.0, p `0.030`, both median sub-period halves positive `+0.0899`/`+0.1269`) but **fails the single-year positive-return concentration guard** (`0.378` > `0.35`), so the apparent edge is too year-concentrated to count as a statistical-alpha clue; the tradeable risk gate would also fail (relative-NAV max drawdown `-0.1930` > `-0.15`; absolute `-0.2387`) but the clue tier failed first. EP excluded 2,288 loss-making + 7,165 insufficient-TTM observations (34,345 EP-scored, 73,492 return rows). Singleton ledger now SPENT (`tests_spent_count=1`, `active_no_new_test_authorized`, `spent_failed_outcome_threshold`). Research-only single in-sample (2018-2025); NO rerun / threshold / earnings-basis / denominator / horizon / benchmark / universe change / diagnostic rescue without a NEW reviewed prereg+ledger or forward-live. `ep_value` is the **5th** falsified/closed A-long line.
- 2026-06-08 the A-long `low_volatility` signal-search runner was committed (`20bd22e`, after Codex re-`审查` PASS) and EXECUTED. Result `research/results/a_long_large_cap_low_volatility_20260608/execution_summary.json` = **`falsified_large_cap_low_volatility_under_frozen_rules`** (NOT a clue, NOT tradeable): the primary 504d/CSI300 industry-size-neutral cell had positive mean net excess (`+0.0892`, 65 cohorts, 44/65 positive months) but **fails the statistical-alpha-clue gates** — HAC t `0.898` (< 2.0, p `0.369`) and sub-period robustness fails (1st half mean `-0.0382` / t `-0.33`, 2nd half `+0.2127` / t `+4.37`, i.e. the apparent edge is entirely second-half / regime-driven, not stable). The tradeable risk gate would also fail (relative-NAV max drawdown `-0.2507` > `-0.15`; absolute `-0.2319`) but the clue tier failed first. Diagnostics (idiosyncratic-vol-vs-CSI300, downside-semideviation) cannot rescue. Singleton ledger now SPENT (`tests_spent_count=1`, `active_no_new_test_authorized`, `spent_failed_outcome_threshold`). Research-only single in-sample (2018-2025) period; NO rerun / threshold / trailing-window / estimator / horizon / benchmark / universe change / diagnostic rescue without a NEW reviewed prereg+ledger or forward-live. The post-execution route-doc/guard gap `R-LOWVOL-ROUTE-CONTRACT-DRIFT-GUARD-GAP` was fixed by user-authorized Codex process/docs repair: remaining historical route rows were converted to stable closed-line pointers, and the guard now catches spent-line aliases through ledger/prereg/result/runner/schema/packet references, not only ledger filenames. No business runner/schema/result metric/ledger value was changed by this repair. `low_volatility` is the 4th falsified/closed A-long line.
- 2026-06-08 the A-long `cash_conversion` signal-search runner was built, committed (`527ac58`, after Codex PASS), and EXECUTED. Result `research/results/a_long_large_cap_cash_conversion_20260607/execution_summary.json` = **`statistical_alpha_clue_research_only`**: the primary cell PASSED the frozen statistical-alpha-clue gates (HAC t `2.47` vs CSI300, 68 cohorts, mean net excess `+0.0970`, both median sub-period halves positive, concentration OK) but is **NOT a tradeable candidate** — rolling relative-NAV drawdown `-0.1822` is worse than the `-0.15` gate. Singleton ledger now SPENT (`tests_spent_count=1`, `active_no_new_test_authorized`, `spent_passed_research_continue_only`). Research-only in-sample clue (vs-CSI300 single-sample); NO rerun / threshold-rescue / production / ship-gate / full-size without a new reviewed prereg+ledger or forward-live evidence. The result + spent ledger + the post-exec test/route repair (incl. the v2 route-doc convention) were **committed to local master** after Codex re-`审查` PASS (both Required findings resolved); the `cash_conversion` research line is now DONE (design `63f5048`, runner `527ac58`; the protocol that `批准修改` is no longer required between a Codex `审查` and a Claude `修复` was committed `f35f786` and is durable in `AGENTS.md`). Any further A-long candidate requires a NEW reviewed prereg + ledger.
- 2026-06-05→07 (history — superseded; full step-by-step detail in `docs/SESSION_LOG.md` + commit log) the first two A-long large-cap lines both committed and closed with **0 clues**: full-main-board frozen 32-cell signal search → `no_alpha_found_under_frozen_rules` (`research/results/a_long_signal_search_20260604/`, 417,407 return rows; methodology repairs R-SPLIT/R-BENCH close-to-close total-vs-total, Newey-West HAC-t, PIT name-veto, 1,504 restatement exclusions, schema-hard `jsonschema`), and large-cap pure-quality composite → `falsified_large_cap_pure_quality_under_frozen_rules` (`research/results/a_long_large_cap_pure_quality_20260607/`, `cfb4058`, HAC t `0.9057`; reviewed `000043.SZ`/`20191129` exclusion + R-NEUT-BUCKET 0.5/0.5 + R-SIZE-GATE-SCOPE repairs); both singleton ledgers spent (`spent_failed_outcome_threshold`). Do NOT re-route to either closed path without a new reviewed prereg+ledger.
- US operating model is active-only + forward-live validation; historical US backtests stay idea-only. A-short steady is `risk_filter_only`. A-share scope is main-board only: A-short already filters non-main boards before analysis, and A-long materialization / audit runners now reject non-main active symbols. The old A-long panel containing `300750.SZ` is historical only and cannot be used as the current alpha/data-readiness path.
---

## 1. 当前 Phase 与目标

- **当前 Phase**：A-share alpha validation。五条已冻结路线均已执行并入库,终判见 §0 + `docs/SESSION_LOG.md` 顶部 + 各自 `execution_summary.json`:全主板 frozen signal search `no_alpha`;large-cap pure-quality composite `falsified`(`cfb4058`);diagnostic-derived 单因子 `cash_conversion`(OCF/|NI|、两层判决、504d/CSI300)`statistical_alpha_clue_research_only`(NOT tradeable,`527ac58`);externally-motivated 单因子 `low_volatility`(price-only trailing-252d realized vol、两层判决、504d/CSI300)`falsified`(`20bd22e`);externally-motivated 单因子 `ep_value`(TTM净利归母/circ_mv、两层判决、504d/CSI300)`falsified`(`d6bedfd`)。五条线均**不得 rerun / 换阈值 / 换基准 / 换 universe / 换 trailing-window / 换 earnings-basis / 换 denominator / 诊断 rescue**,除非新 reviewed prereg+ledger 或 forward-live。
- **当前 P0 / P1 目标**：五条 A-long 大盘单因子线 + 一轮 pre-registered batch+BH-FDR 多因子轮均已执行入库,终判见 §0。batch **NOT dry** → 2 条 research-only clue(`cash_flow_to_circ_mv` / `sales_to_circ_mv`,均 NOT tradeable),batch singleton 已花;**下一步 = 这 2 条 clue 的 forward-live 验证;验证系统(forward-paper capture 数据层+brain)已建+提交(§0 `e5bd1902`)、进入月度捕获执行期(首笔 `as_of=20260630`,cadence 见 memory `project_a_long_vy_forward_paper_monthly_cadence`)**(任何样本内 follow-up 需新 reviewed prereg+ledger,不得 rerun / 换定义 / 换 q / 换阈值 / drop-losers 重跑 FDR / 诊断 rescue)。进度见 §0 + `docs/SESSION_LOG.md`(单一实时来源)。所有已入库/已花的线**均不得 rerun / 换阈值 / 换基准 / 换 universe / 换 trailing-window / 换 earnings-basis / 换 denominator / 诊断 rescue**,除非新 reviewed prereg+ledger 或 forward-live。
- **当前 P1 provider blocker**：US inactive / delisted historical coverage is user-accepted as scoped out for the current active-only forward model. `SR-PROVIDER-001` remains open for license / storage, active-symbol PIT if fundamentals are used, active price-adjustment / corporate actions if used, SEC parser / mapping if used, fallback / stability, provider selection, DataHub / runner consumption, and production readiness. US forward universes must be PIT-frozen at start and must capture real delisting / halt / merger / no-trade outcomes during the forward window; the 12-month forward-live ship-gate requirement is unchanged.
- **执行锁**：`ep_value`、`low_volatility`、`cash_conversion`、large-cap pure-quality、全主板五条线 + batch 多因子轮的 singleton ledger 均已花;**不得 rerun / 换阈值 / 换基准 / 换 universe / 换 trailing-window / 换 q / drop-losers 重跑 FDR / 诊断 rescue / production / ship-gate / full-size**,除非新 reviewed prereg+ledger 或 forward-live。各线终判与 ledger 状态见 §0 + `docs/SESSION_LOG.md` + 各自 `execution_summary.json`。
- **协作模式**：Codex = Executor+Fixer；Claude Code = Independent Reviewer+Committer；用户 = Final Approver(2026-07-02 current split；详 `AGENTS.md` §Role split and command ownership)。
- **后台线**：A-short Phase 6b 只保留 weekly forward capture、comparison-track accumulator、forward evidence accumulation；不扩无关小工具。

---

## 2. 最近已完成

- **A-share burst audit/spec downgrade**（2026-06-01）：`docs/phase7a_alpha_plausibility_audit.json` / `docs/burst_lane_spec.md` 已把 A-share minimal-data burst 从 `continue` 降为 `redesign_required`，并引用 failed outcome evidence。
- **A-share burst redesigned outcome**（2026-06-01）：`evidence_report.json` / `signal_events.csv` / `monthly_stats.csv` 已生成；同一 frozen prereg + patched benchmark-open cache 计算后失败 research-continuation thresholds，decision 为 `falsified_or_redesign_required`。
- **Forward-live aggregate evidence hardening**（2026-06-02）：`runners/aggregate_execution_reports.py` / `schemas/execution_aggregate_report.schema.json` now reject cross-context forward-live evidence, reject insufficient source-window coverage, and prevent two-month alpha / Sharpe diagnostics from passing ship-gate metric checks.
- **US EGS coverage-count / missing-field routing**（2026-06-02）：the approved FMP stable coverage smoke produced `docs/provider_evidence_p1_us_coverage_count_execution_summary_20260602.json`; `schemas/provider_p1_missing_key_metrics_resolution_plan.schema.json` / `docs/provider_evidence_p1_us_missing_key_metrics_resolution_plan_20260602.json` route `peRatio`, `revenuePerShare`, and `netIncomePerShare` without authorizing raw parse or derivation; `SR-PROVIDER-001` stays open.
- **DataHub Phase 7c contract batches**（2026-06-03）：`engine/datahub/job_spec_contract.py` enforces the resource-budget / job-spec contracts; `schemas/datahub_shared_layer_contract.schema.json`, `schemas/datahub_report_contract.schema.json`, `schemas/datahub_reproducibility_manifest.schema.json`, `schemas/datahub_data_quality_monitor_contract.schema.json`, and `schemas/datahub_minimal_a_share_read_path_plan.schema.json` define future shared-layer, report, manifest, quality-monitor, and minimal A-share read-path boundaries without implementation authorization.
- **A-short steady alpha re-audit outcome repair**（2026-06-03）：`runners/a_short_steady_alpha_reaudit.py` now re-derives same-anchor benchmark returns from local `result/a_short/backtest/cache/forward_daily.pkl` and uses old CSV excess only as an uncorrected control. Plain result: old 5d CSI1000 clue fails the corrected statistical gate; A-short steady remains risk-filter-only / research reference.
- **A-long Tushare route validation / gap repair**（2026-06-04）：route validation was partial, then route-gap repair passed small-sample field checks; neither authorizes alpha search.
- **A-long thin-slice materialization execution**（2026-06-04）：`runners/a_long_tushare_incremental_materialization_packet.py` executed the reviewed 2022-2023 three-symbol packet and wrote `docs/a_long_tushare_incremental_materialization_execution_summary_20260604.json`; 29/29 calls succeeded, raw rows stayed under gitignored `data/a_long/raw/tushare/materialization_thin_slice_20260604/`, but no data is ready for alpha.
- **A-long broader materialization / paced rerun**（2026-06-04）：the prior 2018-2025 run produced a shape-passed historical panel, but it contained `300750.SZ` (ChiNext), so it is no longer a valid current path for the user's main-board-only A-share scope.
- **A-long main-board-only guard**（2026-06-04）：`engine/data/a_share_board_scope.py`, `runners/a_long_tushare_broader_materialization_packet.py`, and `runners/a_long_materialized_full_period_data_integrity_audit.py` now reject non-main-board active symbols before A-long materialization/audit use.
- **A-long corrected 000666 supplement execution**（2026-06-04）：the 4-call corrected probe ran and found no usable SW membership source for `000666.SZ`; the approved bounded exception is now enforced by the audit runner.
- **A-long main-board fixed-panel materialization / audit / preregistration**（2026-06-04）：the current 2018-2025 main-board-only fixed panel (`600887.SH` replacing old `300750.SZ`) materialized successfully, the local audit passed with 11/11 self-tests and usable start year 2018, and the first signal-search preregistration was registered. This authorizes no signal run by itself.
- **A-long candidate-universe preflight**（2026-06-04）：the reviewed next step stopped before full alpha pull. Active SW membership can be supplemented by `index_member_all(ts_code=...)`, but 187 delisted main-board names still have no SW membership in the current route. No alpha search was run.
- **A-long SW coverage repair fix + boundary packet**（2026-06-05）：the manual SW 2021 patch was removed after Claude FAIL. The combined repair keeps the real 1,189 / 1,193 active supplement result, identifies the remaining four as `退市*` delisting-shell rows, and the 191-name boundary is now user-approved / committed.
- **A-long full main-board materialization / audit execution**（2026-06-05）：the old reviewed runner completed 2018-2025 full main-board raw materialization: 23,717 endpoint results, 23,677 success, 40 empty, 0 error. The old local-only full audit passed after route-A exclusion repair, but under the latest amended gates it is stale because it lacks PIT `namechange_2018_2025`, full-period H-code TR-close materialization, and the updated audit benchmark/status-source checks. It is not alpha evidence and cannot unlock signal search.
- **A-long signal-search runner package / latest repair**（2026-06-06）：`runners/a_long_full_main_board_signal_search.py` now records the blocked total-return-open probe and selects close-to-close total-vs-total measurement using split-safe stock adjusted closes and total-return benchmark closes. Future valid summaries must report 32 cells across CSI300 / CSI1000, require both benchmarks for any candidate clue, use HAC t-statistics, use same-period YoY earnings-stability, use PIT `namechange` for selection-time ST / delisting-name veto, align terminal benchmark exits to the actual stock exit date, enforce a `-15%` monthly excess drawdown gate, and report tail excess fields; no true signal result exists.
---

## 3. 当前有效策略结论

基于 24 月 v7.10 production、Tier1-only 主口径：

- 工程链路健康；Phase 2 工程签收。
- 5d CSI1000 true same-anchor net excess is positive but not enough: mean net excess `0.6158673222` pp, monthly clustered t `1.7623850474`, 14/23 positive months, Bonferroni-normal adjusted p `0.3120170532`。旧未校正 t `2.8769227582` was the measurement-basis artifact under review.
- 20d benchmark excess 仍不显著：`excess_csi1000 monthly t=0.172873488`、`excess_csi300 monthly t=0.5714019896`；5d CSI300 也偏弱（monthly t `1.3934659699`）。
- 直接 `momentum_std` regime 切片不可评估；size 只能用 CSI1000-CSI300 proxy，不能当完整 factor proof。
- 强负信号仍是重点风控证据：`OVERHEAT` flagged subset 表现差；Tier1 相对 Tier2 更稳，但这不等于 full-size alpha。
- 当前 A-short steady 只能保留为“风控 filter / research reference / evidence loop”，不得当作 production alpha 或 full-size lane。

失效旧结论：12 月 Top5 显著、12 期突破型反向信号、旧 `_cc.md` 整体结论、v7.9 前 completeness_score 分组结论均不可继续引用。

---

## 4. 当前关键文件

- `AGENTS.md` - 最高项目规则、固化决策、命令别名、启动顺序。
- `docs/README.md` - 完整 routing table 和文档维护规则。
- `docs/AI_REVIEW_PROTOCOL.md` - review/修复/提交 工作流的兼容指针;**权威角色分工(2026-07-02:Codex = Executor+Fixer,Claude Code = Independent Reviewer+Committer)、命令绑定、对抗式深审标准均以 `AGENTS.md` §Role split and command ownership 为准**。
- `docs/system_risk_register.md` - durable open-risk queue；`执行` / `审查` 必读。
- `docs/SESSION_LOG.md` - 最新 cross-LLM reasoning / review verdict；只读顶部 1-3 条。
- `docs/ALPHA_VALIDATION_ACTION_GUIDE.md` - Phase 7a+ 最高行动指南。
- `research/README.md` / `schemas/research_preregistration.schema.json` / `schemas/a_long_signal_search_preregistration.schema.json` / `research/preregistrations/a_long_signal_search_preregistration_20260604.json` / `research/ledgers/a_long_signal_search_program_test_budget_ledger_20260604.json` / `schemas/a_long_total_return_benchmark_access_probe_summary.schema.json` / `docs/a_long_total_return_benchmark_access_probe_summary_20260606.json` / `runners/a_long_total_return_benchmark_access_probe.py` / `runners/a_long_full_main_board_signal_search.py` / `schemas/a_long_signal_search_execution_summary.schema.json` / `schemas/evidence_report.schema.json` - research-only A-long signal-search owners.
- `runners/a_long_data_integrity_audit.py` / `research/preregistrations/a_long_data_integrity_audit_20260603.json` / `research/results/a_long_data_integrity_audit_20260603/audit_report.json` / `research/ledgers/a_long_data_integrity_audit_program_test_budget_ledger_20260603.json` / `schemas/a_long_tushare_data_route_repair_plan.schema.json` / `docs/a_long_tushare_data_route_repair_plan_20260603.json` / `runners/a_long_tushare_route_validation_packet.py` / `schemas/a_long_tushare_route_validation_execution_summary.schema.json` / `docs/a_long_tushare_route_validation_execution_summary_20260604.json` / `runners/a_long_tushare_route_gap_repair_packet.py` / `schemas/a_long_tushare_route_gap_repair_execution_summary.schema.json` / `docs/a_long_tushare_route_gap_repair_execution_summary_20260604.json` / `schemas/a_long_tushare_incremental_materialization_packet.schema.json` / `docs/a_long_tushare_incremental_materialization_packet_20260604.json` / `runners/a_long_tushare_incremental_materialization_packet.py` / `schemas/a_long_tushare_incremental_materialization_execution_summary.schema.json` / `docs/a_long_tushare_incremental_materialization_execution_summary_20260604.json` / `runners/a_long_materialized_thin_slice_data_integrity_audit.py` / `schemas/a_long_materialized_thin_slice_data_integrity_audit_report.schema.json` / `research/results/a_long_materialized_thin_slice_data_integrity_audit_20260604/audit_report.json` / `schemas/a_long_tushare_broader_materialization_packet.schema.json` / `docs/a_long_tushare_broader_materialization_packet_20260604.json` / `runners/a_long_tushare_broader_materialization_packet.py` / `schemas/a_long_tushare_broader_materialization_execution_summary.schema.json` / `docs/a_long_tushare_broader_materialization_execution_summary_20260604.json` / `runners/a_long_tushare_daily_price_route_diagnostic_packet.py` / `docs/a_long_tushare_daily_price_route_diagnostic_execution_summary_20260604.json` / `runners/a_long_materialized_full_period_data_integrity_audit.py` / `research/results/a_long_materialized_full_period_data_integrity_audit_20260604/audit_report.json` - A-long data-integrity gate, route-repair, route-validation, materialization, audit, and diagnostic owner files.
- `docs/provider_priority_benchmark_contract.md` - Phase 7a-3 provider evidence priority / provisional benchmark contract。
- `docs/provider_evidence_drift_monitor.md` / `schemas/provider_evidence_drift_monitor.schema.json` - Phase 7b provider evidence / drift monitor contract。
- `schemas/provider_p1_access_decision_plan.schema.json` / `docs/provider_evidence_p1_us_access_decision_sample_validation_plan_20260531.json` / `schemas/provider_p1_sample_validation_access_approval.schema.json` / `docs/provider_evidence_p1_us_sample_validation_access_approval_20260602.json` / `runners/us_egs_sample_validation.py` / `schemas/provider_p1_us_egs_sample_validation_summary.schema.json` / `docs/provider_evidence_p1_us_sample_validation_summary_20260602.json` / `schemas/provider_p1_fmp_stable_endpoint_retry_summary.schema.json` / `docs/provider_evidence_p1_us_fmp_stable_endpoint_retry_summary_20260602.json` / `schemas/provider_p1_remaining_blocker_resolution_plan.schema.json` / `docs/provider_evidence_p1_us_remaining_blocker_resolution_plan_20260602.json` / `schemas/provider_p1_coverage_count_access_packet_plan.schema.json` / `docs/provider_evidence_p1_us_coverage_count_access_packet_plan_20260602.json` / `schemas/provider_p1_coverage_count_access_packet_approval.schema.json` / `docs/provider_evidence_p1_us_coverage_count_access_packet_approval_20260602.json` / `runners/us_egs_coverage_count_packet.py` / `schemas/provider_p1_coverage_count_execution_summary.schema.json` / `docs/provider_evidence_p1_us_coverage_count_execution_summary_20260602.json` / `schemas/provider_p1_missing_key_metrics_resolution_plan.schema.json` / `docs/provider_evidence_p1_us_missing_key_metrics_resolution_plan_20260602.json` / `schemas/provider_p1_validation_authorization_packet.schema.json` / `docs/provider_evidence_p1_us_validation_authorization_packet_20260603.json` / `schemas/provider_p1_validation_execution_packet.schema.json` / `docs/provider_evidence_p1_us_validation_execution_packet_20260603.json` / `runners/us_egs_validation_packet.py` / `schemas/provider_p1_validation_execution_summary.schema.json` / `docs/provider_evidence_p1_us_validation_execution_summary_20260603.json` / `schemas/provider_p1_inactive_delisted_gap_resolution_plan.schema.json` / `docs/provider_evidence_p1_us_inactive_delisted_gap_resolution_plan_20260603.json` / `schemas/provider_p1_fmp_entitlement_corporate_action_no_access_diagnostic.schema.json` / `docs/provider_evidence_p1_us_fmp_entitlement_corporate_action_no_access_diagnostic_20260603.json` / `schemas/provider_p1_sivb_reprobe_execution_packet.schema.json` / `docs/provider_evidence_p1_us_sivb_reprobe_execution_packet_20260603.json` / `runners/us_egs_sivb_reprobe_packet.py` / `schemas/provider_p1_sivb_reprobe_execution_summary.schema.json` / `docs/provider_evidence_p1_us_sivb_reprobe_execution_summary_20260603.json` / `schemas/provider_p1_fmp_paid_tier_license_public_docs_review.schema.json` / `docs/provider_evidence_p1_us_fmp_paid_tier_license_public_docs_review_20260603.json` - Phase 7b-2 P1 access / sample / coverage-count / missing-field / validation / inactive-delisted / entitlement-corporate-action / SIVB re-probe / FMP paid-tier-license public-docs owner files。
- `schemas/provider_p1_readiness_review.schema.json` / `docs/provider_evidence_p1_us_readiness_review_matrix_20260529.json` - Phase 7b-2 P1 readiness review matrix（collection complete；Phase 7c / provider selection / broad data fetch blocked）。
- `docs/evidence_feasibility_controls.md` / `schemas/evidence_feasibility_controls.schema.json` - Phase 7a-4 burst promotion / evidence feasibility controls。
- `docs/evidence_report_schema_contract.md` / `schemas/evidence_report.schema.json` - Phase 7a-5 evidence report schema contract。
- `docs/alpha_plausibility_audit.md` / `schemas/alpha_plausibility_audit.schema.json` - Phase 7a-1 audit owner and contract。
- `docs/evidence_capital_policy.md` - paper vs live-normalized evidence owner。
- `docs/strategy_design_synthesis.md` - 总体策略架构 owner。
- `docs/burst_lane_spec.md` / `docs/us_short_system_design.md`（US-short 单一设计权威；旧 `docs/us_short_spec.md` 已降级归档指针）/ `docs/us_long_system_design.md`（US-long 美股中长线单一设计权威，Tier 2 active；真实数据/provider gated 到 Tier 4+）/ `docs/long_alpha_spec.md` - lane owner specs。
- `docs/provider_data_requirements_audit.md` / `schemas/provider_capability_catalog.schema.json` - provider requirements / capability contract。
- `docs/portfolio_allocation_policy.md` - 35/65、bucket、cash non-fungibility、manual-only capital policy。
- `docs/datahub_design.md` / `engine/datahub/job_spec_contract.py` / `schemas/datahub_local_resource_budget.schema.json` / `schemas/datahub_job_spec.schema.json` / `schemas/datahub_shared_layer_contract.schema.json` / `schemas/datahub_report_contract.schema.json` / `schemas/datahub_reproducibility_manifest.schema.json` / `schemas/datahub_data_quality_monitor_contract.schema.json` / `schemas/datahub_minimal_a_share_read_path_plan.schema.json` - DataHub / provider / factor-layer guardrails, job-spec enforcement, shared-layer/report/reproducibility/data-quality/minimal-read-path contracts。
- `presets/a_short.yaml` / `presets/a_short_screening_threshold_governance_20260602.json` / `schemas/a_short_screening_threshold_governance.schema.json` - A-short screening threshold governance parity owner。
- `docs/handoff/README.md` - phase handoff index；不要全量读 handoff。

---

## 5. 下一步

### P0 - A-short 收尾(执行顺序优先于下面的 A-long 遗留)
按用户 2026-06-11 定的顺序,A-short 建造收尾排在 A-long 遗留之前,依次:
1. ✅ **V14.3 切片2 + 性能优化 + 接 weekly + 初始 bootstrap — 已全做完并提交(2026-06-15,详 §0)**。现状 = comparison-only 累积态:`weekly_screening.ps1` Stage 5 每次实盘自动 increment regime ledger(`research/results/a_short/regime_daily_ledger.json`,已 252 行),攒 ~12 周触发设计的升级审查提示。**无新建代码待办,但有常驻提醒(见 register「comparison-only tracks — standing promote/retire reminder index」+ 下面第 5 条)**:≥12 周须复审(达标→升级提示 / 无改善→退役),V14.2-sourcing 微接线触发(12周复审 或 生产 market_regime 首次出非-unknown)已从 memory 固化进 register。
2. ✅ **A-short 4.3 持仓 CSV → account_state 联动 — 全线完成(2026-06-15,详 §0)**:4.3-A+B(转换器+模板+lineage+测试,`e3b54f1e`)+ 4.3-C(M6.7「持仓/冷静」多标签列,`6d3e30d`)+ 4.3-D(trades↔positions 一致性对账 advisory,`b10410d`)均已提交、Codex PASS。用户真实使用 = 在 gitignored `state/a_short/account_state_csv/`(空模板已搭好)填 `account.csv`+`positions.csv` → 跑 `runners/a_short_account_state_from_manual_tables.py` 产 `account_state.json` → 周报 `-Account` 传入。**无待办**。
3. ✅ **A-short 持仓恒列入 M6.7 — S1 已提交(2026-06-16,`3e81200`,详 §0)**:账户持仓恒进 M6.7 + 三档 Tier 路由 + 诚实覆盖(注入持仓 `coverage=partial`、现价取价格钟、每只标语义未核查、Tier-3 不伪造 veto/clean)。**(S3a 系统算止损/止盈被动显示 + schema v1.1 已实现,见下;持仓语义扩展 + S3b 主动管理见本条末「剩余两大块」)**。**账户周报隐私输出路由已固化**(`155e3ad`,2026-06-16,详 §0):带 `-Account` 的周报自动落 gitignored `state/a_short/weekly_private/<as_of>/`,pipeline `git check-ignore` 硬护栏防持仓被提交泄漏。**S3a 设计稿已定稿**(2026-06-16,Codex re-`审查 PASS`):系统算持仓**跟踪止损**(ratchet,近20日高−ATR×倍数)+ 止盈**被动显示**(动作恒持有、不动"禁止加仓")+ account_state **v1.1**(stop 降可选参考、`enum`+`if/then` 向后兼容)+ validator 持有放开;**已实现并提交**(`d79174e`,2026-06-16,Codex re-`审查 PASS`;设计 `docs/a_short_holdings_s3_system_levels_design.md`)。价格计算优化提案见 `docs/a_short_m67_price_calc_optimization_proposal.md`。**价格 Slice 0(side-aware A股 0.01 tick + post-tick 重校验 + ticked-entry sizing)已提交**(`1e3fa74`,2026-06-16,Codex re-`审查 PASS`):建仓价位统一 0.01 可执行价、取整后用最终价复校 RR/结构(破则转观察)、单票 sizing 按 `entry_t` 防"按真实价买不起"的建仓;helpers 备给 S3a 复用。**系统级永久**。**价格 #2+#3+#4(入场区间 低吸/突破 + 最不利价 entry_high RR 门/定量 + 全局现金分配 `--account` 确定性消耗·不足转观察·cash_allocation 摘要 + no-dangling 精确短语护栏 + validate_weekly_report 的 run_lineage⟺cash 双向不变式·sized 分配审计-math 对账)已提交**(`5e63433`,2026-06-16,Codex re-`审查 PASS`,3 轮 lineage→audit-math;全量 2034 OK)。**价格 #5 有效支撑(抗单日极值结构位:最低 low 比次低低 >1×ATR 判插针取次低 + strong/weak/fallback_extreme 质量标记 + advice 精确「结构支撑/质量」no-dangling)已提交**(`d8d6c8e`,2026-06-16,Codex re-`审查 PASS`,2 轮;全量 2042 OK):**只动建仓侧 support**(stop/低吸带/RR),`resistance` 保持 raw max20 → 持仓跟踪止损零改。**价格 #6-i 突破型更高 RR floor(V14.2 迁移首项:`BREAKOUT_RR_BONUS=0.5`,突破 rr_floor=regime+0.5 贯穿参考价/最不利价/t1 fallback;advice 门槛文案 type-aware;validator 强制「门槛 {rr_floor}」no-dangling)已提交**(`98e6351`,2026-06-16,Codex re-`审查 PASS`,2 轮;全量 2047 OK):**只建仓侧、持仓零改、低吸不变**;strategy-grade 可调参(进 GOVERNANCE),非 v14.2 逐字常量。**M6.7 价格 roadmap(Slice 0→S3a→#2+#3+#4→#5→#6-i→#1 除权提示 `d60bf07d`→#6 IV-HV advisory `39c53e00`→#6 resistance 有效化 `d22b3e6e`)结构项完成**;**#6 更细结构止损 = 放弃**(2026-06-17 用户定:周频 + 手动执行下精度微调 ROI 不足,**不做**)→ **价格层到此收尾**。持仓/风险侧 **4.2(全 5 轮缺口数据 + 语义 advisory → `operation_impact`/`exclusion_summary` → M6.7)与 S3b(R1-R4b 持仓主动管理:持仓处置/减仓价/清仓价/到价提示/移保本/跨周持久化 ratchet)均已实现并提交**(详 §0;设计 `桌面/4.2.md` + `docs/a_short_holdings_s3_system_levels_design.md`)。剩余只有:语义→生产硬否决升级(Slice 3 已 land;唯一剩 cninfo 真硬否决 = opt-in (b),见下条)、4.2 字段转正治理(`docs/a_short_gap_data_promotion_governance_20260619.md`,达 12 effective live weeks 触发独立 review)、V14.3/overlay comparison evidence clock(item 5)、route-doc/legacy deferred(见 register「Remaining open」)。**#6 IV-HV(2026-06-17,Codex `审查` PASS)**:市场级 IV(50ETF 隐含)vs HV(50ETF 已实现,feed 内算无新 fetch)regime 标签(iv_rich/iv_inline/iv_cheap/unknown)落 M6.7 波动率状态+machine.iv_gate,**纯 advisory 不翻 decision**(Rule3 分位闸门不变);feed schema 1.1.0(+hv_value/hv_window),validate 把 raw→ratio→regime 经 iv_hv_tag 单一来源重算绑死。**#6 resistance/压力 有效化(2026-06-17,Codex `审查` PASS,3 轮)**:`effective_resistance` 把 #5 抗单日插针对称到近20日最高 → 建仓 `t1`/RR 门**分子** + 持仓跟踪止损都去插针(用户已确认接受**改持仓止损口径**);plan 加 `t1_basis`、advice 按 structural/RR兜底分支、validator **双向绑死** `t1==tick_down(res)⟺structural`;退役 `support_resistance`;复用 SR_SPIKE_ATR/SR_QUALITY,无 governance/preset/schema 改。
4. **A-short 语义项 → 生产硬规则升级(Slice 3 已 land;**不是 4.2**)** — 监管48h / 行业景气 / 媒体负面 / 隐蔽风险 这些语义项的**自动评估 + 落 M6.7(advisory)已归 4.2 Slice B**(复用 `a_short_semantic_risk` → `operation_impact`/`m67_advisory_veto`;故不再单列"分析层语义自动化评估")。**唯一剩下的独立项 = 是否把它们升级成生产硬规则(真否决、动 EGS 选股)**——4.2 §2.2/§3.3 明确不做(语义永久 advisory)。**Slice 3 已 land(2026-06-20)**:legacy 生产硬否决已拆/降——egs_main stage3 的 DeepSeek `POL-RISK-VETO` **整段移除**(web/LLM 永不自动生产硬否决)、cninfo `REGULATOR-VETO` **降为 advisory**(`REGULATOR-ADVISORY`,不删生产候选)+ 修「空公告=通过」假清白(→未核查)。**唯一剩 = 把 cninfo 官方命中做成『真』生产监管硬否决 = 主动 opt-in (b)**(修请求形态 `code,orgId` + PIT + governance + 测;前置同 `SR-PROVIDER-001` + 攒 ~4 周真实 advisory),**等用户将来主动决定再开**。无前置不动,**不得**硬编"负面媒体→硬否决"(误否决 + look-ahead)。排在 A-long 遗留之前。
5. ⏳ **Comparison-only / forward 轨的常驻提醒(已固化进 `docs/system_risk_register.md`「comparison-only tracks — standing promote/retire reminder index」;register 在 `执行` 时强制读 → 任何 LLM 到点都会看到、再提醒用户决定升级/放弃)**:
   - **① V14.3 regime** — 时钟在走(每周 increment);≥12 周复审:达标→提示进生产切换审查 / 无改善→提示退役;+ V14.2-sourcing 微接线触发。
   - **② overlay(Slice A 赛道热度对照)— 已接线 + (b) 已做(2026-06-16)**:`A-EGS/egs_main.py`(score_l5 后)用内存 top50+all_daily+L3概念+sw_map 装配写 `result/a_short/<as_of>/overlay.json`。**(b) 改:`overlay_emit_allowed` 放开 `pit`+`today` 双模式**——pit→concept 标 'pit'(回放);**live `today`→标 'forward'(决策当日 live 成员,无 look-ahead)→ overlay 现在在 live weekly 自然 forward 累积**(≥12 周升级时钟开始走);neutralize/无概念仍跳过。**提醒已升级成数据驱动 + 运行时横幅(2026-06-17,`R-ASHORT-OVERLAY-EVAL-READINESS-REMINDER`):** `runners/a_short_overlay_eval.py` + `weekly_screening.ps1` Stage 6 每次实盘数 forward obs,≥12 即打醒目横幅 + 写 `overlay_eval_summary.json`(`promotion_review_due`)——**不管哪个 AI 跑都提醒**,不再靠 LLM 记得读 register。下一步=到点(banner 触发)后:先冻 governance `stable_win_margin`、再起草 §6 指标切片(`R-ASHORT-OVERLAY-EVAL-METRICS-FOLLOWUP`,算 overlay vs baseline 跑赢度)、据此决定升级/退役。

> ⏳ **overlay 待决已闭(选 (b))**:此前列的"先接线/pit-gated 待决"已解决——(b) 让 live 实盘自然累积 forward 证据。无新建造待办,只待实盘攒够周数复审。
   - **③ egs_weight(legacy/balanced/aggressive/双提)** — 时钟在走(每次 EGS run 产 `egs_weight_comparison.json`);双提升级/退役条件见 register 的 egs weight forward-item。
   - **④ A-long 2 clue(`cash_flow_to_circ_mv`/`sales_to_circ_mv`)forward-live** — 研究级 OOS harness **未搭**;下一步动作 = 写新 reviewed prereg 定窗口/指标再跑,跑完评 OOS,不确认则记 falsified。
   - 全部**只对照、绝不自动切生产**;切生产永远另走 reviewed governance + 用户确认。

### P0 / P1 - Post redesigned outcome boundary（A-long 遗留:排在上面 A-short 收尾之后）
- Read `docs/system_risk_register.md` before choosing the next `执行`.
- All five A-long large-cap single-factor lines — full-main-board, large-cap pure-quality (`cfb4058`), `cash_conversion` (`527ac58`), `low_volatility` (`20bd22e`), and `ep_value` (`d6bedfd`) — plus the pre-registered batch + BH-FDR multi-factor round (runner `50d94e3`, executed) are executed and closed; do not route work back to any of them. Verdicts, metrics, and spent-ledger state live in §0 + `docs/SESSION_LOG.md` top + each `research/results/.../execution_summary.json`. The batch is NOT dry — `cash_flow_to_circ_mv` and `sales_to_circ_mv` are research-only statistical-alpha clues (NOT tradeable). The next A-long step is **forward-live validation of those two clues**; any in-sample follow-up (incl. a fresh batch) needs a NEW reviewed prereg + ledger and must not rerun / change definitions / change q / drop losers and re-run FDR / rescue with diagnostics.
- Do not run `research/preregistrations/a_share_minimal_data_burst_20260531.json`; it remains `BLOCKED_DO_NOT_RUN`.
- The full-universe redesigned outcome / excess slice has failed its registered thresholds; do not rerun EGS, change preregistered parameters, full-refresh forward_daily, or reinterpret it as production evidence.
- Any further redesigned A-share burst test must append a planned test to `research/ledgers/a_share_burst_program_test_budget_ledger_20260531.json` and create a new reviewed preregistration before it runs.
### P1 - US active-only + forward；A-share alpha priority

- Current US model is active-only universe + forward-live validation only. Historical US backtests are exploration / idea-only forever; they cannot prove alpha, support ship-gate, unlock full-size, or authorize DataHub / production.
- Forward universe must be frozen point-in-time at the forward start date; real delisting / halt / merger / bankruptcy / no-trade outcomes during the forward window must be captured, not deleted.
- No further inactive / delisted historical coverage work or paid / specialized US data purchase is required now. Remaining provider work is only license / storage, active-PIT if used, active price / corporate actions if used, SEC parser / mapping if used, fallback / stability, and production-readiness gates.

### P2 - DataHub local resource boundary

- Future Phase 7c / runner implementation must call `engine.datahub.job_spec_contract.validate_datahub_job_spec_contract` / `validate_datahub_job_spec_file` and consume the shared-layer / report / reproducibility / data-quality / minimal-read-path contracts; default all-system / all-market / all-lane / full-refresh runs remain disallowed unless explicit approval + reviewed job spec says otherwise.

### P2 - A-short maintenance line

- 继续 weekly forward capture / comparison-track accumulator / forward evidence accumulation；12 期新增 forward 样本后再重新审查 score / ESP / veto overlap；any new alpha search needs a new reviewed preregistration.

---

## 6. 常用命令

```powershell
# 24 月 production rank 回测
python runners\backtest_rank.py --mode production --periods 24 --freq monthly --end-date 20260301 --split-date 20250101 --refresh-forward-daily

# Stats-only 重统计
python runners\backtest_rank.py --stats-only --mode production --periods 24 --freq monthly --end-date 20260301

# 每周五实时选股
python A-EGS\egs_main.py --as-of <YYYYMMDD>

# 周五一键
.\runners\weekly_screening.ps1 -AsOf 20260530 -L3Mode neutralize
.\runners\weekly_screening.ps1 -SkipCanary

# 周实盘 cadence(2026-06-15 定方向 / 2026-06-22 加 canonical 解析器):窗口内[周五收盘后→周一收盘前]任意时刻/多次跑
# 都收敛到同一 canonical 决策日(即将到来的交易日)。**直接跑、省略 -AsOf** 即自动解析(不传 -L3Mode);
# live(as_of>=运行日:今日 或 前瞻 canonical)→ 选股回退节前收盘 + 新闻/web·LLM 窗口到运行时刻(含周末突发);
# 真·过去回放(as_of<运行日)须显式 -L3Mode pit/neutralize。机理/标志详见 weekly_screening.ps1 顶部「运行 cadence」。
.\runners\weekly_screening.ps1
```

---

## 7. 雷区

- 不接券商 / OS automation / 自动下单；所有交易动作仍由用户手动执行。
- 不因 Phase 7a audit、Phase 7b provider evidence 或 DataHub 工程放松 ship gate；full-size 必须满足 monthly alpha t-stat >= 2.0、Sharpe >= 1.0、max drawdown <= 15%、forward live data >= 12 个月。
- A/US cash 默认不互通；跨市场资金转移必须显式人工决策或后续 coordinator 规则。
- 不把 paper evidence 当 ship-gate evidence；ship gate 只接受 live-normalized evidence。
- 不用旧 12p findings 或旧 `_cc.md` 结论。
- 改 `A-EGS/egs_main.py` 前必须先 view 当前文件。

---

## 维护规则

- `CURRENT.md` 保持短 snapshot，目标 <150 行；最新状态放这里，过程 / review verdict / rejected alternatives 放 `SESSION_LOG.md`。
- 新 handoff 高门槛；默认追加到当前 phase 主 handoff，旧 handoff 不重组；新增文档必须先在 `docs/README.md` routing table 中说明 owner role。
- Material audit findings must be fixed in-round or entered in `docs/system_risk_register.md`.
