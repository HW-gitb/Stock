preset: a_short
market: A
horizon: short
schema_version: 1.0.0
data_provider: tushare
status: active

capital:
  market: A
  horizon: short
  bucket: short
  capital_basis: bucket_capital
  bucket_target_pct: 0.333333
  bucket_ceiling_pct: 0.333333
  portfolio_allocation_policy: p0c_user_confirmed_20260526

paths:
  result_dir: result/a_short
  state_dir: state/a_short

notes:
  - Phase 1 uses A-EGS/egs_main.py as the current screening entry.
  - Screening threshold governance lives in presets/a_short_screening_threshold_governance_20260602.json.
  - Current runtime still reads A-EGS/egs_main.py::CONF; parity is enforced by tests/schema/test_a_short_screening_threshold_governance_schema.py until a reviewed preset-loader migration.

screening_threshold_governance:
  schema_ref: schemas/a_short_screening_threshold_governance.schema.json
  artifact_ref: presets/a_short_screening_threshold_governance_20260602.json
  source_code_ref: A-EGS/egs_main.py::CONF
  parity_test_ref: tests/schema/test_a_short_screening_threshold_governance_schema.py
  status: current_conf_mirrored_no_runtime_behavior_change
