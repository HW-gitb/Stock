{
  "schema_name": "a_short_theme_overlay_governance",
  "schema_version": "1.0.0",
  "generated_at": "2026-06-10T00:00:00+00:00",
  "scope": {
    "phase": "a_short_theme_overlay_slice_a",
    "purpose": "freeze overlay weights/thresholds; parity-mirror the runner constants",
    "contract_status": "comparison_track_non_production",
    "production_ranking_changed_by_this_artifact": false,
    "egs_main_runtime_changed": true,
    "egs_main_runtime_change_note": "non-production overlay SIDE-OUTPUT added to the EGS run (2026-06-11, A 方案: writes overlay.json into the run bundle; emit modes updated 2026-06-16 (b): BOTH --l3-mode pit AND live today emit -- pit labels concept_membership='pit' (replay), live today labels 'forward' (decision-day live members, no look-ahead); neutralize / no-snapshot skip); the production scoring path (final_score / tier / admission) is untouched",
    "egs_main_production_behavior_changed": false,
    "data_fetch_allowed": false,
    "ship_gate_claim_allowed": false,
    "broker_or_order_automation_allowed": false
  },
  "source_refs": [
    {"artifact_id": "overlay_runner", "path": "runners/a_short_theme_overlay_comparison.py",
     "consumption_role": "Runtime source of overlay constants; parity test mirrors EMITTED_THRESHOLDS + OVERLAY_WEIGHTS literally."},
    {"artifact_id": "slice_a_design", "path": "docs/a_short_theme_overlay_slice_a_design_20260610.md",
     "consumption_role": "Frozen design this artifact pins (commit 4ba5617)."},
    {"artifact_id": "overlay_schema", "path": "schemas/a_short_theme_overlay_comparison.schema.json",
     "consumption_role": "Output artifact contract; const-pins the same weights/thresholds."}
  ],
  "overlay_weights": {"esp": 0.15, "l4": 0.45, "theme": 0.25, "industry": 0.15},
  "crowding_handling": "one hard action: crowding_hit strips the theme/industry bonus (overlay falls back to esp+l4 base); heat cannot rescue a crowded candidate",
  "thresholds": {
    "theme_window_blend": {"d5": 0.5, "d20": 0.5},
    "pass_percentile": 70.0,
    "breadth_up_frac_min": 0.5,
    "breadth_vol_frac_min": 0.4,
    "persistence_top_quantile": 0.3,
    "persistence_window_days": 5,
    "fit_floor": 0.4,
    "eligibility_min_pass": 2
  },
  "parity_rules": {
    "runtime_source": "runners/a_short_theme_overlay_comparison.py",
    "exact_value_match_required": true,
    "threshold_change_requires_reviewed_artifact_update": true
  },
  "limitations": [
    "Comparison-track only; does not change EGS production final_score / tier / admission.",
    "Overlay artifact authorizes no buy advice, real money, ship-gate claim, or promotion decision.",
    "Any future overlay-policy activation must use the separate P4a adjudication route and a separately reviewed user decision.",
    "PIT: concept membership uses L3 snapshots; SW industry mapping has no historical snapshot yet, so the SW dimension is forward-only until snapshots accumulate."
  ]
}
